---
phase: 16-stardist-tool-environment
plan: 05
type: execute
wave: 4
depends_on: ["16-04"]
files_modified:
  - tests/integration/test_stardist_adapter_e2e.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "StarDist E2E integration test runs from the core server environment (Py3.13) and never imports StarDist/tool entrypoint directly."
    - "The StarDist E2E test exercises real tool execution via the subprocess worker path (PersistentWorkerManager -> tool entrypoint in bioimage-mcp-stardist env)."
    - "The E2E test performs a real two-step flow: from_pretrained -> predict_instances, reusing the same worker/session so ObjectRef handoff works."
  artifacts:
    - path: tests/integration/test_stardist_adapter_e2e.py
      provides: "Core-env integration test that validates StarDist execution via persistent worker subprocess"
  key_links:
    - from: tests/integration/test_stardist_adapter_e2e.py
      to: src/bioimage_mcp/runtimes/persistent.py
      via: PersistentWorkerManager.get_worker + WorkerProcess.execute
      pattern: "PersistentWorkerManager"
    - from: tests/integration/test_stardist_adapter_e2e.py
      to: src/bioimage_mcp/api/execution.py
      via: execute_step (subprocess tool invocation)
      pattern: "execute_step\("
    - from: tests/integration/test_stardist_adapter_e2e.py
      to: tools/stardist/bioimage_mcp_stardist/entrypoint.py
      via: manifest-resolved entrypoint executed in tool env
      pattern: "bioimage-mcp-stardist"
---

<objective>
Fix the Phase 16 UAT gap where the StarDist end-to-end integration test fails due to running inside the StarDist tool environment and importing core server code.

Purpose: Keep tool-pack dependency isolation (Py3.11 StarDist env) while still validating the real execution path from the core server runtime.
Output: A rewritten E2E test that runs in the core env and drives StarDist via the persistent worker subprocess interface.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tests/integration/test_stardist_adapter_e2e.py
@tests/integration/conftest.py
@tests/integration/test_persistent_worker.py
@src/bioimage_mcp/api/execution.py
@src/bioimage_mcp/runtimes/executor.py
@src/bioimage_mcp/runtimes/persistent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite StarDist E2E test to run from core env via persistent worker subprocess</name>
  <files>tests/integration/test_stardist_adapter_e2e.py</files>
  <action>
- Remove the requirement to run the test inside `bioimage-mcp-stardist` (delete the module-level `pytest.importorskip("stardist")` and any direct imports from `bioimage_mcp_stardist.entrypoint`).
- Keep the env gate: ensure the test is still marked with `@pytest.mark.requires_env("bioimage-mcp-stardist")`.
- Implement the E2E flow by executing StarDist functions through the core runtime:
  - Use `mcp_services` (from `tests/integration/conftest.py`) to get a `Config` with `tool_manifest_roots` and fs allowlists.
  - Create a `PersistentWorkerManager(manifest_roots=config.tool_manifest_roots, memory_store=...)` inside the test (or reuse `mcp_services["execution"]._worker_manager` if already present), and choose a fixed `session_id` for the test.
  - Generate a small deterministic OME-TIFF input image in `tmp_path` using `bioio.writers.OmeTiffWriter` (avoid `tifffile` and dataset files with spaces).
  - Call `bioimage_mcp.api.execution.execute_step(...)` twice (same `worker_manager` + same `session_id` + same env inferred from fn_id):
    1) `fn_id="stardist.models.StarDist2D.from_pretrained"` with params `{name: "2D_versatile_fluo"}`; capture the returned `ObjectRef` (its `uri` must be present).
    2) `fn_id="stardist.models.StarDist2D.predict_instances"` with inputs `{model: <ObjectRef>, image: <BioImageRef file://...>}` and params `{prob_thresh: 0.5}`.
  - Use a generous `timeout_seconds` for the prediction call to allow model download and CPU inference.
- Assert the result of the second call:
  - `ok` is true
  - outputs include `labels` (type `LabelImageRef`, format `OME-Zarr`, path exists)
  - outputs include `details` (type `NativeOutputRef`, path exists, JSON-decodable)
  </action>
  <verify>pytest tests/integration/test_stardist_adapter_e2e.py -v</verify>
  <done>
The StarDist integration test runs under the core server environment and validates a real two-step StarDist execution via the subprocess worker runtime.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add explicit assertions that the test uses the subprocess worker path (not direct imports)</name>
  <files>tests/integration/test_stardist_adapter_e2e.py</files>
  <action>
- Add assertions that demonstrate the persistent worker subprocess path is actually being used:
  - After the first `execute_step` call, assert `worker_manager.is_worker_alive(session_id, "bioimage-mcp-stardist")` is true.
  - Capture the worker PID after step 1 and assert it is reused for step 2 by calling `worker_manager.get_worker(session_id, "bioimage-mcp-stardist").process_id` before and after.
- Remove/update the docstring guidance that says to run the test via `conda run -n bioimage-mcp-stardist ...`; replace with a core-env invocation.
  </action>
  <verify>pytest tests/integration/test_stardist_adapter_e2e.py -v</verify>
  <done>
Test failures can no longer be caused by importing core server modules inside the StarDist env; the test proves it spawned/reused a StarDist worker subprocess.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/integration/test_stardist_adapter_e2e.py -v`
- If the StarDist env is not installed, the test is skipped via `requires_env("bioimage-mcp-stardist")` (no import-time crashes).
</verification>

<success_criteria>
- UAT “End-to-End Workflow Test” passes without requiring the StarDist tool environment to import any core server modules.
- The rewritten test executes StarDist via the same path used by the core server (PersistentWorkerManager spawning the tool env entrypoint as a subprocess).
</success_criteria>

<output>
After completion, create `.planning/phases/16-stardist-tool-environment/16-05-SUMMARY.md`
</output>
