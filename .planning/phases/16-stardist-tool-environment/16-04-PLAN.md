---
phase: 16-stardist-tool-environment
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-03"]
files_modified:
  - tests/contract/test_stardist_manifest_contract.py
  - tests/contract/test_stardist_meta_describe.py
  - tests/integration/test_stardist_adapter_e2e.py
  - docs/reference/tools.md
autonomous: true

must_haves:
  truths:
    - "Contract tests validate the StarDist manifest shape and meta.describe payload."
    - "Integration test can run (when env installed) and exercises discovery + a minimal inference path."
    - "Docs list StarDist as an optional tool environment."
  artifacts:
    - path: tests/contract/test_stardist_manifest_contract.py
      provides: "Tool manifest contract coverage for tools.stardist"
    - path: tests/integration/test_stardist_adapter_e2e.py
      provides: "E2E integration coverage for StarDist tool pack"
  key_links:
    - from: tests/integration/test_stardist_adapter_e2e.py
      to: envs/bioimage-mcp-stardist.yaml
      via: requires_env marker
      pattern: "requires_env\(\"bioimage-mcp-stardist\"\)"
---

<objective>
Add tests and minimal docs updates to lock in the StarDist tool pack integration and prevent regressions.

Purpose: Ensure StarDist stays discoverable/executable and the manifest remains schema-valid.
Output: Contract + integration tests and docs update.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/contract/test_cellpose_manifest_contract.py
@tests/contract/test_cellpose_meta_describe.py
@tests/integration/test_cellpose_adapter_e2e.py
@docs/reference/tools.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add StarDist manifest contract tests</name>
  <files>tests/contract/test_stardist_manifest_contract.py</files>
  <action>
- Create `tests/contract/test_stardist_manifest_contract.py` following `tests/contract/test_cellpose_manifest_contract.py`.
- Validate:
  - Manifest exists at `tools/stardist/manifest.yaml`.
  - Manifest validates via `ToolManifest` after injecting `manifest_path` + `manifest_checksum`.
  - `env_id` is exactly `bioimage-mcp-stardist` and starts with `bioimage-mcp-`.
  - Manifest includes `meta.describe` in `functions`.
  - Manifest defines executable functions:
    - `stardist.models.StarDist2D.from_pretrained`
    - `stardist.models.StarDist2D.predict_instances`
  </action>
  <verify>pytest tests/contract/test_stardist_manifest_contract.py -v</verify>
  <done>
Contract test suite validates the StarDist manifest structure and key identifiers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add StarDist meta.describe contract tests</name>
  <files>tests/contract/test_stardist_meta_describe.py</files>
  <action>
- Create `tests/contract/test_stardist_meta_describe.py` mirroring `tests/contract/test_cellpose_meta_describe.py`.
- Validate the meta.describe payload schema (ok/result with `params_schema`, `tool_version`, `introspection_source`).
- Add a DiscoveryService-level assertion that ObjectRef inputs remain ports (not params_schema), by modeling a representative stardist function with `model: ObjectRef` + `image: BioImageRef` and confirming those are filtered from params_schema.
  </action>
  <verify>pytest tests/contract/test_stardist_meta_describe.py -v</verify>
  <done>
Contract coverage exists for stardist meta.describe response shape and artifact port filtering behavior.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add env-gated E2E integration test + docs entry</name>
  <files>
    tests/integration/test_stardist_adapter_e2e.py
    docs/reference/tools.md
  </files>
  <action>
- Create `tests/integration/test_stardist_adapter_e2e.py` similar to `tests/integration/test_cellpose_adapter_e2e.py`, but keep it minimal:
  - Mark with `@pytest.mark.requires_env("bioimage-mcp-stardist")`.
  - Spin up the server components needed for a single tool execution, then:
    1) ensure the tool pack is discoverable (list/describe path)
    2) execute `from_pretrained` to get an ObjectRef
    3) execute `predict_instances` on a small test image (use an existing small synthetic dataset from repo, if available; otherwise skip with clear message)
    4) assert outputs include a LabelImageRef (OME-Zarr) and a details NativeOutputRef.
- Update `docs/reference/tools.md` to include a short section for `Tools.StarDist` with env name `bioimage-mcp-stardist` and a note that it is optional/heavy.
  </action>
  <verify>pytest tests/integration/test_stardist_adapter_e2e.py -v</verify>
  <done>
When the stardist env is installed, the E2E test exercises discovery + inference; docs list the tool.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/contract/test_stardist_manifest_contract.py -v`
- `pytest tests/contract/test_stardist_meta_describe.py -v`
- (Optional when env installed) `pytest tests/integration/test_stardist_adapter_e2e.py -v`
</verification>

<success_criteria>
- Contract tests for StarDist manifest and meta.describe pass.
- Integration test is present and correctly env-gated.
- `docs/reference/tools.md` mentions StarDist tool environment.
</success_criteria>

<output>
After completion, create `.planning/phases/16-stardist-tool-environment/16-04-SUMMARY.md`
</output>
