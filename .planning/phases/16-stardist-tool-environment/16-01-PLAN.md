---
phase: 16-stardist-tool-environment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - envs/bioimage-mcp-stardist.yaml
  - envs/bioimage-mcp-stardist.lock.yml
autonomous: true

must_haves:
  truths:
    - "A reproducible 'bioimage-mcp-stardist' environment can be installed from repo state."
    - "The StarDist tool environment contains StarDist + TensorFlow + bioio OME-Zarr support."
  artifacts:
    - path: envs/bioimage-mcp-stardist.yaml
      provides: "Conda environment definition for StarDist tool pack"
    - path: envs/bioimage-mcp-stardist.lock.yml
      provides: "Pinned lockfile for bioimage-mcp-stardist"
  key_links:
    - from: envs/bioimage-mcp-stardist.yaml
      to: envs/bioimage-mcp-stardist.lock.yml
      via: conda-lock
      pattern: "conda-lock.*bioimage-mcp-stardist"
---

<objective>
Add a pinned, reproducible conda environment for StarDist so the tool pack can run in isolation.

Purpose: Establish the isolated runtime that matches the tool-pack pattern (like Cellpose/Trackpy).
Output: StarDist env spec + lockfile under envs/.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/16-CONTEXT.md
@.planning/phases/16-stardist-tool-environment/16-RESEARCH.md
@envs/bioimage-mcp-cellpose.yaml
@src/bioimage_mcp/bootstrap/install.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StarDist tool environment definition</name>
  <files>envs/bioimage-mcp-stardist.yaml</files>
  <action>
- Create `envs/bioimage-mcp-stardist.yaml` following the conventions of `envs/bioimage-mcp-cellpose.yaml` / `envs/bioimage-mcp-trackpy.yaml`.
- Use Python 3.11.
- Include conda-forge dependencies needed for StarDist inference:
  - `stardist` pinned to `0.9.2`
  - `csbdeep` pinned to `0.8.2`
  - `tensorflow` (not a hand-rolled TF install; prefer conda-forge)
  - `numpy`, `scikit-image`, `numba`
  - `bioio`, `bioio-ome-tiff`, `bioio-ome-zarr`
  - `pydantic>=2.0`
  - `pytest>=8.0` (so env-gated tests can run inside the env)
- Do NOT add heavyweight deps to the core server; this env is the isolation boundary.
  </action>
  <verify>python -c "import yaml; p='envs/bioimage-mcp-stardist.yaml'; d=yaml.safe_load(open(p)); assert d['name']=='bioimage-mcp-stardist'; assert 'dependencies' in d; print('ok')"</verify>
  <done>
`envs/bioimage-mcp-stardist.yaml` exists and pins StarDist/CSBDeep versions while including bioio OME-Zarr support.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate pinned lockfile for bioimage-mcp-stardist</name>
  <files>envs/bioimage-mcp-stardist.lock.yml</files>
  <action>
- Generate `envs/bioimage-mcp-stardist.lock.yml` using conda-lock, consistent with existing envs.
- Command (linux-64 lockfile like the existing lockfiles):
  - `conda-lock lock -f envs/bioimage-mcp-stardist.yaml -p linux-64 --lockfile envs/bioimage-mcp-stardist.lock.yml`
- If conda-lock is missing, install it into the current dev environment (NOT into the tool env):
  - `python -m pip install conda-lock`
  </action>
  <verify>python -c "from pathlib import Path; p=Path('envs/bioimage-mcp-stardist.lock.yml'); print(p.exists(), p.stat().st_size>0)"</verify>
  <done>
`envs/bioimage-mcp-stardist.lock.yml` exists and is non-empty, enabling reproducible installs.
  </done>
</task>

</tasks>

<verification>
- `python -c "import yaml; yaml.safe_load(open('envs/bioimage-mcp-stardist.yaml'))"`
</verification>

<success_criteria>
- StarDist environment is defined in `envs/bioimage-mcp-stardist.yaml`.
- A pinned lockfile exists at `envs/bioimage-mcp-stardist.lock.yml`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-stardist-tool-environment/16-01-SUMMARY.md`
</output>
