---
phase: 08-statistical-analysis
plan: 3
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - tests/contract/test_scipy_stats_adapter.py
  - tests/unit/registry/dynamic/test_scipy_stats_execute.py
autonomous: true

must_haves:
  truths:
    - "Contract tests verify scipy.stats wrappers and distributions are discoverable with correct io_patterns"
    - "Unit tests verify stats execution produces JSON payloads with stable keys (statistic/pvalue, selected_column)"
  artifacts:
    - path: tests/contract/test_scipy_stats_adapter.py
      provides: "Discovery contract coverage for stats integration"
      contains: "ScipyStatsAdapter"
    - path: tests/unit/registry/dynamic/test_scipy_stats_execute.py
      provides: "Hermetic execution tests for stats wrappers and distribution methods"
      contains: "ttest"
  key_links:
    - from: tools/base/manifest.yaml
      to: src/bioimage_mcp/registry/loader.py
      via: "manifest load -> dynamic discovery -> port mapping"
      pattern: "dynamic_sources"
---

<objective>
Lock in Phase 8 behavior with hermetic contract + unit tests so stats integration remains stable while Phase 9 adds additional SciPy modules.

Purpose: Prevent regressions in discovery metadata and JSON output structure for statistics.
Output: New tests covering discovery and execution for scipy.stats integration.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/bioimage_mcp/registry/dynamic/adapters/scipy.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_stats.py
@src/bioimage_mcp/registry/dynamic/object_cache.py
@tests/contract/test_scipy_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Add contract tests for stats discovery</name>
  <files>tests/contract/test_scipy_stats_adapter.py</files>
  <action>
Create `tests/contract/test_scipy_stats_adapter.py` verifying `ScipyStatsAdapter.discover()` produces entries for:
- Summary stats wrappers: `describe_table`, `mean_table`, `skew_table`, `kurtosis_table`
- Test wrappers: `ttest_1samp_table`, `ttest_ind_table`, `ttest_rel_table`, `f_oneway_table`, `ks_2samp_table`
- Distribution methods: `scipy.stats.norm.cdf` and `scipy.stats.poisson.pmf` (as representative continuous+discrete)

Assertions:
- fn_ids are correct (prefix `scipy.stats.`)
- io_patterns are the expected new patterns (`TABLE_TO_JSON`, `TABLE_PAIR_TO_JSON`, `MULTI_TABLE_TO_JSON`, `PARAMS_TO_JSON`)

Also add an integration contract for the composite adapter:
- Instantiate `ScipyAdapter` and call `discover()` with a `module_config` that includes both `scipy.ndimage` and `scipy.stats` in `modules`.
- Assert the returned `FunctionMetadata.fn_id` list includes at least one ndimage fn_id (e.g., `scipy.ndimage.sobel`) and the new stats wrapper fn_ids (e.g., `scipy.stats.ttest_ind_table`) to prove the composite adapter delegates discovery correctly.

Mark the module with `pytest.mark.requires_base` so it runs in the tool env that contains SciPy.

Keep tests hermetic: do not require real data files.
  </action>
  <verify>conda run -n bioimage-mcp-base pytest -m requires_base tests/contract/test_scipy_stats_adapter.py -q</verify>
  <done>Contract tests pass and confirm discovery coverage for STATS-01/02/03.</done>
</task>

<task type="auto">
  <name>Add unit tests for stats execution JSON payloads</name>
  <files>tests/unit/registry/dynamic/test_scipy_stats_execute.py</files>
  <action>
Create `tests/unit/registry/dynamic/test_scipy_stats_execute.py` with hermetic tests using in-memory ObjectRefs via `OBJECT_CACHE`:

Test 1: `ttest_ind_table` on two input tables
- Put two pandas DataFrames (single numeric column) into `OBJECT_CACHE` under `obj://...` URIs.
- Call `ScipyStatsAdapter.execute(fn_id='scipy.stats.ttest_ind_table', inputs=[('table_a', {...}), ('table_b', {...})], params={...}, work_dir=tmp_path)`.
- Patch `scipy.stats.ttest_ind` to return an object with `statistic` and `pvalue` (and optional `df`).
- Assert output is a JSON artifact dict (type `NativeOutputRef`), and the file contains keys `statistic` + `pvalue`.

Test 2: `describe_table` selects a deterministic column
- Provide a DataFrame with 2 numeric columns and no `column` param.
- Assert payload includes `selected_column` and it is stable (first numeric column).

Test 3: distribution method execution
- Patch `scipy.stats.norm` with a fake object that returns a frozen object exposing `.cdf`.
- Execute `fn_id='scipy.stats.norm.cdf'` with params containing `x` list.
- Assert payload is list-shaped and JSON-serializable.

Keep tests fast: use mocks/patches instead of requiring SciPy numerical results.

Mark the module with `pytest.mark.requires_base` (it may import `scipy.stats` for patching).
  </action>
  <verify>conda run -n bioimage-mcp-base pytest -m requires_base tests/unit/registry/dynamic/test_scipy_stats_execute.py -q</verify>
  <done>Unit tests pass and validate JSON output shape + key fields for stats wrappers and distributions.</done>
</task>

</tasks>

<verification>
- `conda run -n bioimage-mcp-base pytest -m requires_base tests/contract/test_scipy_stats_adapter.py -q`
- `conda run -n bioimage-mcp-base pytest -m requires_base tests/unit/registry/dynamic/test_scipy_stats_execute.py -q`
</verification>

<success_criteria>
- Stats discovery and execution are covered by automated tests
- Tests run hermetically (no external files or network) and pass on CI
</success_criteria>

<output>
After completion, create `.planning/phases/08-statistical-analysis/08-03-SUMMARY.md`
</output>
