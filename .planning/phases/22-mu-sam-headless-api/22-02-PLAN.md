---
phase: 22-mu-sam-headless-api
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/__init__.py
  - src/bioimage_mcp/registry/dynamic/adapters/microsam.py
autonomous: true

must_haves:
  truths:
    - "meta.list includes micro_sam.* callables except micro_sam.sam_annotator"
    - "meta.describe returns Introspector-derived JSON schema for a known micro_sam function"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      provides: "MicrosamAdapter discovery (and placeholder execution)"
      contains: "class MicrosamAdapter"
    - path: src/bioimage_mcp/registry/dynamic/adapters/__init__.py
      provides: "Adapter registry includes microsam"
  key_links:
    - from: tools/microsam/manifest.yaml
      to: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      via: "dynamic_sources.adapter: microsam"
      pattern: "adapter: microsam"
---

<objective>
Implement MicrosamAdapter discovery so micro_sam APIs are exposed through MCP list/describe.

Purpose: Phase 22 requires dynamic introspection (Introspector) of upstream micro_sam callables while excluding micro_sam.sam_annotator.
Output: MicrosamAdapter registered in the dynamic adapter registry and producing FunctionMetadata for micro_sam.*.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/22-mu-sam-headless-api/22-CONTEXT.md
@src/bioimage_mcp/registry/dynamic/introspection.py
@src/bioimage_mcp/registry/dynamic/discovery.py
@src/bioimage_mcp/registry/dynamic/models.py
@src/bioimage_mcp/registry/dynamic/adapters/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MicrosamAdapter.discover with upstream filtering</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/microsam.py</files>
  <action>
- Add new adapter at src/bioimage_mcp/registry/dynamic/adapters/microsam.py implementing:
  - class MicrosamAdapter with __init__ creating an Introspector.
  - discover(module_config) -> list[FunctionMetadata]
    - module_config comes from a DynamicSource (adapter=microsam, prefix=micro_sam, modules=[] in Plan 22-01).
    - Enumerate micro_sam modules at runtime (pkgutil.walk_packages over micro_sam.__path__).
    - Exclude any module under micro_sam.sam_annotator and any private/test modules.
    - Within each module, expose public functions (inspect.isfunction / inspect.isbuiltin) that do not start with underscore.
    - Use Introspector.introspect() to generate FunctionMetadata (descriptions + docstring-derived param info).
    - Set meta.fn_id to the fully qualified upstream id (e.g., micro_sam.prompt_based_segmentation.segment_from_points).
    - Assign io_pattern best-effort:
      - micro_sam.prompt_based_segmentation.* -> image_to_labels
      - micro_sam.instance_segmentation.* -> image_to_labels
      - default -> generic
    - Do NOT import torch or load models explicitly; discovery must avoid any expensive initialization beyond importing modules.
  - resolve_io_pattern(func_name, signature) returning patterns consistent with above.
- Keep execute() as a stub raising NotImplementedError (execution is Plan 22-03).
  </action>
  <verify>
python -m compileall src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </verify>
  <done>
- MicrosamAdapter.discover returns FunctionMetadata for at least prompt_based_segmentation + instance_segmentation when micro_sam is installed.
- Any micro_sam.sam_annotator callables are excluded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register microsam adapter for lazy population</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/__init__.py</files>
  <action>
- Add "microsam" to KNOWN_ADAPTERS.
- Update populate_default_adapters() to import and register MicrosamAdapter under ADAPTER_REGISTRY["microsam"].
- Keep lazy loading behavior (do not populate on module import).
  </action>
  <verify>
python -c "from bioimage_mcp.registry.dynamic.adapters import KNOWN_ADAPTERS; assert 'microsam' in KNOWN_ADAPTERS"
  </verify>
  <done>
- A tool entrypoint calling discover_functions() with adapter=microsam can auto-populate and run MicrosamAdapter.discover.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for microsam adapter filtering and id formatting</name>
  <files>tests/unit/registry/test_microsam_adapter_discovery.py</files>
  <action>
- Create tests/unit/registry/test_microsam_adapter_discovery.py that does NOT require micro_sam to be installed.
- Use monkeypatch to stub:
  - importlib.import_module (return stub modules)
  - pkgutil.walk_packages (return a mix of module names including micro_sam.sam_annotator.* and micro_sam.prompt_based_segmentation)
- Provide at least one stub function with a NumPy-style docstring so Introspector can extract description.
- Assert:
  - discover() excludes any callable whose module path includes micro_sam.sam_annotator
  - returned FunctionMetadata.fn_id starts with "micro_sam."
  - io_pattern is image_to_labels for prompt_based_segmentation module.
  </action>
  <verify>
pytest tests/unit/registry/test_microsam_adapter_discovery.py -q
  </verify>
  <done>
- Unit tests pass without requiring the microsam conda env.
  </done>
</task>

</tasks>

<verification>
- ruff check .
- pytest tests/unit/registry/test_microsam_adapter_discovery.py -q
</verification>

<success_criteria>
- meta.list can discover micro_sam.* callables in the microsam env.
- micro_sam.sam_annotator is excluded at discovery time.
</success_criteria>

<output>
After completion, create `.planning/phases/22-mu-sam-headless-api/22-02-SUMMARY.md`
</output>
