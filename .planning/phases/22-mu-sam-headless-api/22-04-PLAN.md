---
phase: 22-mu-sam-headless-api
plan: 04
type: execute
wave: 4
depends_on: ["22-03"]
files_modified:
  - tests/smoke/test_microsam_headless_live.py
autonomous: true

must_haves:
  truths:
    - "bioimage-mcp list shows micro_sam.prompt_based_segmentation.segment_from_points"
    - "bioimage-mcp describe returns a JSON object schema for that function"
    - "bioimage-mcp run executes prompt-based and automatic segmentation end-to-end"
  artifacts:
    - path: tests/smoke/test_microsam_headless_live.py
      provides: "End-to-end smoke coverage for Phase 22"
  key_links:
    - from: tests/smoke/test_microsam_headless_live.py
      to: micro_sam.compute_embedding
      via: "live_server.call_tool('run', ...)"
      pattern: "micro_sam\.compute_embedding"
---

<objective>
Add end-to-end smoke tests for micro_sam headless execution (list/describe/run).

Purpose: Phase 22 must be verified end-to-end via the live MCP server and the isolated microsam environment.
Output: Smoke tests gated by requires_env("bioimage-mcp-microsam") covering prompt-based and automatic segmentation.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@tests/smoke/conftest.py
@tests/smoke/test_equivalence_stardist.py
@tests/smoke/utils/native_executor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live smoke tests for headless microsam segmentation</name>
  <files>tests/smoke/test_microsam_headless_live.py</files>
  <action>
- Create tests/smoke/test_microsam_headless_live.py with two async tests using the live_server fixture:
  1) Prompt-based segmentation test
    - Markers: smoke_extended, uses_minimal_data, requires_env("bioimage-mcp-microsam"), anyio.
    - Create a tiny synthetic 2D image (e.g., a bright disk on dark background) and write it to an allowlisted path.
    - Load via base.io.bioimage.load to get a BioImageRef.
    - Call micro_sam.compute_embedding (if required by your implementation) and then call micro_sam.prompt_based_segmentation.segment_from_points.
    - Provide at least one positive point prompt near the object (and optionally negative prompts).
    - Assert:
      - status == success
      - output contains a LabelImageRef
      - label image exists on disk and max label is >= 1
  2) Automatic/instance segmentation test
    - Same markers.
    - Call a stable micro_sam.instance_segmentation.* function implemented in 22-03.
    - Assert output is LabelImageRef and produces at least one object.
- Add a lightweight assertion that list output does not contain micro_sam.sam_annotator.
  </action>
  <verify>
pytest tests/smoke/test_microsam_headless_live.py -m smoke_extended -q
  </verify>
  <done>
- Tests pass when bioimage-mcp-microsam environment + models are installed.
- Tests are skipped (not failed) when the environment is missing.
  </done>
</task>

</tasks>

<verification>
- pytest tests/smoke/test_microsam_headless_live.py -m smoke_extended -q
</verification>

<success_criteria>
- Phase 22 has end-to-end proof for list/describe/run for headless micro_sam segmentation.
</success_criteria>

<output>
After completion, create `.planning/phases/22-mu-sam-headless-api/22-04-SUMMARY.md`
</output>
