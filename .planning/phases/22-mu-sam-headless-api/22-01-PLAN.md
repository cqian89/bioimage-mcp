---
phase: 22-mu-sam-headless-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/microsam/manifest.yaml
  - tools/microsam/bioimage_mcp_microsam/entrypoint.py
  - src/bioimage_mcp/api/execution.py
  - tests/unit/api/test_microsam_tool_config.py
autonomous: true

must_haves:
  truths:
    - "Tool id is tools.micro_sam and micro_sam.* IDs are routable"
    - "microsam tool pack supports persistent NDJSON protocol (ready/execute/shutdown)"
    - "meta.list and meta.describe work in the micro_sam tool environment"
  artifacts:
    - path: tools/microsam/manifest.yaml
      provides: "micro_sam tool manifest with dynamic_sources"
    - path: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      provides: "NDJSON tool entrypoint with meta.list/meta.describe handlers"
    - path: src/bioimage_mcp/api/execution.py
      provides: "tool_config injection for micro_sam device selection"
  key_links:
    - from: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      to: src/bioimage_mcp/registry/dynamic/discovery.py
      via: "meta.list/meta.describe call discover_functions"
      pattern: "discover_functions\("
    - from: src/bioimage_mcp/api/execution.py
      to: tools/microsam
      via: "manifest.tool_id check injects tool_config"
      pattern: "tool_config"
---

<objective>
Make the micro_sam tool pack discoverable and runnable by the MCP runtime.

Purpose: Phase 22 depends on tool-side meta.list/meta.describe and persistent-worker compatibility before we can expose headless micro_sam segmentation.
Output: A correctly named tool manifest and a persistent NDJSON-capable microsam entrypoint with working meta.list/meta.describe.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-mu-sam-headless-api/22-CONTEXT.md
@tools/microsam/manifest.yaml
@tools/microsam/bioimage_mcp_microsam/entrypoint.py
@src/bioimage_mcp/api/execution.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename tool_id to tools.micro_sam and update call sites</name>
  <files>
tools/microsam/manifest.yaml
src/bioimage_mcp/api/execution.py
tests/unit/api/test_microsam_tool_config.py
tools/microsam/bioimage_mcp_microsam/entrypoint.py
  </files>
  <action>
- In tools/microsam/manifest.yaml, change tool_id from tools.microsam to tools.micro_sam.
- Update any hard-coded references to tools.microsam to tools.micro_sam:
  - src/bioimage_mcp/api/execution.py tool_config injection gate (microsam.device).
  - tests/unit/api/test_microsam_tool_config.py expected tool_id.
  - tools/microsam/bioimage_mcp_microsam/entrypoint.py error messages (only strings).
- Keep the existing config namespace microsam.device unchanged (this is tool_config payload name, not tool_id).
  </action>
  <verify>
pytest tests/unit/api/test_microsam_tool_config.py -q
  </verify>
  <done>
- Manifest loads with tool_id tools.micro_sam.
- Unit test asserting tool_config injection passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add dynamic_sources and upgrade microsam entrypoint to persistent NDJSON</name>
  <files>
tools/microsam/manifest.yaml
tools/microsam/bioimage_mcp_microsam/entrypoint.py
  </files>
  <action>
- In tools/microsam/manifest.yaml, add a dynamic_sources block for upstream micro_sam discovery:
  - Use adapter: microsam
  - Use prefix: micro_sam
  - Set modules: [] (forces core discovery to use runtime meta.list fallback).
  - Keep include_patterns/exclude_patterns broad; micro_sam.sam_annotator exclusion is enforced in the MicrosamAdapter (Plan 22-02).
- Rewrite tools/microsam/bioimage_mcp_microsam/entrypoint.py to match the repo's persistent worker NDJSON protocol used by tools/base:
  - On startup (persistent mode), print a JSON {"command":"ready","version":...} handshake.
  - Support commands:
    - execute: route meta.list and meta.describe; for non-meta IDs return structured NOT_IMPLEMENTED.
    - shutdown: clear any in-process caches and return shutdown_ack.
    - materialize/evict: implement compatible handlers (can be copied/adapted from tools/base entrypoint) even if Phase 22 doesn't use them heavily.
  - Implement meta.list:
    - Load tools/microsam/manifest.yaml as ToolManifest.
    - Call bioimage_mcp.registry.dynamic.discovery.discover_functions(manifest, ADAPTER_REGISTRY, cache=..., project_root=...).
    - Return function entries with id/name/module/summary/io_pattern/params_schema (same shape as tools/base).
  - Implement meta.describe:
    - Prefer dynamic discovery metadata (same as tools/base) and return params_schema.
  - Preserve the existing microsam.device selection behavior from Phase 21 (tool_config payload) and ensure meta.* requests do not fail if accelerators are unavailable.
  </action>
  <verify>
python -m compileall tools/microsam/bioimage_mcp_microsam/entrypoint.py
  </verify>
  <done>
- tools/microsam entrypoint can run in persistent mode without crashing.
- meta.list and meta.describe code paths exist and return ok=true payloads (even if discovery yields 0 functions before adapter implementation).
  </done>
</task>

</tasks>

<verification>
- ruff check .
- pytest tests/unit/api/test_microsam_tool_config.py -q
</verification>

<success_criteria>
- Tool id is tools.micro_sam and no references to tools.microsam remain in code/tests.
- microsam entrypoint supports NDJSON protocol and meta.list/meta.describe handlers.
</success_criteria>

<output>
After completion, create `.planning/phases/22-mu-sam-headless-api/22-01-SUMMARY.md`
</output>
