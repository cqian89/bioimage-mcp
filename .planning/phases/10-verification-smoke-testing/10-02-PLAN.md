---
phase: 10-verification-smoke-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/smoke/test_equivalence_scipy.py
  - tests/smoke/test_equivalence_scipy_stats.py
  - tests/smoke/reference_scripts/scipy_baseline.py
  - tests/smoke/reference_scripts/scipy_stats_baseline.py
autonomous: true

must_haves:
  truths:
    - "Gaussian blur output from MCP matches native SciPy output bit-for-bit"
    - "T-test output from MCP matches native SciPy output bit-for-bit"
  artifacts:
    - path: tests/smoke/test_equivalence_scipy.py
      provides: "Bit-for-bit gaussian_filter equivalence test (live server vs native script)"
    - path: tests/smoke/test_equivalence_scipy_stats.py
      provides: "Bit-for-bit ttest_ind_table equivalence test (live server vs native script)"
    - path: tests/smoke/reference_scripts/scipy_baseline.py
      provides: "Native SciPy baseline for gaussian_filter (deterministic dtype contract)"
    - path: tests/smoke/reference_scripts/scipy_stats_baseline.py
      provides: "Native SciPy baseline for t-test (stable JSON contract)"
  key_links:
    - from: tests/smoke/test_equivalence_scipy.py
      to: tests/smoke/reference_scripts/scipy_baseline.py
      via: NativeExecutor.run_script
      pattern: "run_script\(.*scipy_baseline\.py"
    - from: tests/smoke/test_equivalence_scipy_stats.py
      to: tests/smoke/reference_scripts/scipy_stats_baseline.py
      via: NativeExecutor.run_script
      pattern: "run_script\(.*scipy_stats_baseline\.py"
    - from: tests/smoke/test_equivalence_scipy_stats.py
      to: MCP function
      via: fn_id
      pattern: "base\\.scipy\\.stats\\.ttest_ind_table"
---

<objective>
Enforce strict parity tests for two deterministic reference operations: gaussian blur and two-sample t-test.

Purpose: Provide regression-proof equivalence checks that validate MCP execution matches native SciPy outputs exactly for key deterministic operations.
Output: Two smoke_full equivalence tests backed by native reference scripts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-verification-smoke-testing/10-CONTEXT.md

@tests/smoke/test_equivalence_scipy.py
@tests/smoke/utils/native_executor.py
@tests/smoke/utils/data_equivalence.py
@tests/smoke/reference_scripts/scipy_baseline.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_stats.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten gaussian_filter equivalence to bit-for-bit</name>
  <files>
tests/smoke/test_equivalence_scipy.py
tests/smoke/reference_scripts/scipy_baseline.py
  </files>
  <action>
Update the gaussian equivalence path to enforce strict equality:

- In `tests/smoke/reference_scripts/scipy_baseline.py`:
  - Force a deterministic dtype contract: load the image, convert `data = np.asarray(data, dtype=np.float32)`.
  - Run `scipy.ndimage.gaussian_filter` on that float32 array.
  - Save the result as float32 (`result = np.asarray(result, dtype=np.float32)` before `np.save`).
  - Keep the JSON output contract stable (status, output_path, shape, dtype).

- In `tests/smoke/test_equivalence_scipy.py`:
  - Replace tolerance-based comparison with strict bit-for-bit: `np.testing.assert_array_equal(mcp_data, expected_data)`.
  - Ensure both arrays are loaded as the same dtype before comparison (float32).
  - Keep existing diagnostic behavior on MCP failure (log preview + server stderr).

Do not relax parity via tolerances; this test is the "deterministic strict" anchor.
  </action>
  <verify>pytest tests/smoke/test_equivalence_scipy.py::test_scipy_ndimage_gaussian_filter_equivalence --smoke-full -m smoke_full -v</verify>
  <done>
The gaussian_filter equivalence test fails on any single-bit mismatch (after explicit dtype normalization to float32 on both sides).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add bit-for-bit t-test equivalence (ttest_ind_table)</name>
  <files>
tests/smoke/test_equivalence_scipy_stats.py
tests/smoke/reference_scripts/scipy_stats_baseline.py
  </files>
  <action>
Add a new native reference script and a matching smoke_full equivalence test.

1) Create `tests/smoke/reference_scripts/scipy_stats_baseline.py`:
- CLI args: `--table-a`, `--table-b`, `--column`, `--equal-var`.
- Load both CSVs with pandas, extract the numeric column as numpy arrays.
- Run `scipy.stats.ttest_ind(a, b, equal_var=..., nan_policy="propagate", alternative="two-sided")`.
- Write a JSON file to a temp path containing ONLY stable fields needed for strict compare:
  - `statistic`, `pvalue`, `equal_var`, `column`, and `n_a`, `n_b`.
- Print a JSON contract to stdout with `status: success` and `output_path`.

2) Create `tests/smoke/test_equivalence_scipy_stats.py`:
- Markers: `@pytest.mark.smoke_full`, `@pytest.mark.uses_minimal_data`, `@pytest.mark.requires_env("bioimage-mcp-base")`, `@pytest.mark.anyio`.
- Use deterministic inputs:
  - Write two tiny CSVs in `tmp_path` with a numeric column `val` (no randomness).
  - Load them into MCP via `base.io.table.load` to get two `TableRef`s.
- Call MCP:
  - `fn_id`: `base.scipy.stats.ttest_ind_table`
  - inputs: `table_a`, `table_b`
  - params: `{ "column": "val", "equal_var": true, "alternative": "two-sided" }`
- Extract the MCP JSON output by parsing `outputs.output.uri` (file path) and loading the JSON.
- Run native baseline via `NativeExecutor.run_script("bioimage-mcp-base", ...)` and load its output JSON.
- Enforce bit-for-bit semantics by exact dict equality after parsing JSON (no numeric tolerances).

Follow the equivalence marker conventions enforced by `tests/smoke/test_smoke_markers.py`.
  </action>
  <verify>pytest tests/smoke/test_equivalence_scipy_stats.py --smoke-full -m smoke_full -v</verify>
  <done>
The t-test equivalence test compares MCP vs native output JSON exactly (all fields equal), and fails on any mismatch.
  </done>
</task>

</tasks>

<verification>
- Run both equivalence tests: `pytest --smoke-full -m smoke_full tests/smoke/test_equivalence_scipy*.py -v`
</verification>

<success_criteria>
- gaussian_filter: strict bit-for-bit equivalence against native SciPy baseline (after explicit float32 normalization).
- ttest_ind_table: strict JSON equality against native SciPy baseline on deterministic inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/10-verification-smoke-testing/10-02-SUMMARY.md`
</output>
