---
phase: 10-verification-smoke-testing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/smoke/test_smoke_scipy_datasets.py
  - tests/smoke/test_smoke_scipy_discovery.py
autonomous: true

must_haves:
  truths:
    - "Synthetic and standard datasets required for smoke tests exist in-repo"
    - "SciPy tool surface is discoverable (list + describe) for each major submodule"
  artifacts:
    - path: tests/smoke/test_smoke_scipy_datasets.py
      provides: "Dataset presence checks for reproducible smoke runs"
    - path: tests/smoke/test_smoke_scipy_discovery.py
      provides: "Live-server discovery checks for SciPy submodules"
  key_links:
    - from: tests/smoke/test_smoke_scipy_discovery.py
      to: MCP catalog
      via: list/describe tools
      pattern: "call_tool\(\"(list|describe)\""
---

<objective>
Add guardrail smoke tests that assert required datasets exist and SciPy tools are discoverable.

Purpose: Prevent flaky verification runs caused by missing datasets or discovery regressions.
Output: Two lightweight smoke_minimal tests (dataset presence + describe checks).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-verification-smoke-testing/10-CONTEXT.md

@tests/smoke/conftest.py
@tests/smoke/test_smoke_minimal_sanity.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dataset presence smoke test (reproducibility guardrail)</name>
  <files>tests/smoke/test_smoke_scipy_datasets.py</files>
  <action>
Create `tests/smoke/test_smoke_scipy_datasets.py` with smoke_minimal tests that assert key dataset files exist (no skipping):

- Markers: `@pytest.mark.smoke_minimal`, `@pytest.mark.uses_minimal_data`.
- Assert these paths exist:
  - `datasets/synthetic/test.tif`
  - `datasets/sample_data/measurements.csv`

Keep this test fast and purely filesystem-based (no server start required).
  </action>
  <verify>pytest tests/smoke/test_smoke_scipy_datasets.py -m smoke_minimal -v</verify>
  <done>
Missing baseline datasets fail fast with a clear assertion error (not skip), ensuring reproducible smoke testing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SciPy discovery smoke test (list + describe)</name>
  <files>tests/smoke/test_smoke_scipy_discovery.py</files>
  <action>
Create `tests/smoke/test_smoke_scipy_discovery.py` that validates discovery on a live MCP server:

- Markers: `@pytest.mark.smoke_minimal`, `@pytest.mark.uses_minimal_data`, `@pytest.mark.requires_env("bioimage-mcp-base")`, `@pytest.mark.anyio`.
- Call `list` with `include_counts: true` and assert the catalog contains a SciPy subtree (search returned items recursively for a node with id/path that includes `scipy`).
- Call `describe` (tool) for one representative function per submodule and assert `inputs`, `outputs`, and `params_schema` exist:
  - `base.scipy.ndimage.gaussian_filter`
  - `base.scipy.stats.ttest_ind_table`
  - `base.scipy.spatial.distance.cdist`
  - `base.scipy.signal.periodogram`

This test should validate discoverability even before executing the functions.
  </action>
  <verify>pytest tests/smoke/test_smoke_scipy_discovery.py -m smoke_minimal -v</verify>
  <done>
SciPy tools are discoverable via list/describe on a live server for all four submodules.
  </done>
</task>

</tasks>

<verification>
- Dataset guardrail: `pytest -m smoke_minimal tests/smoke/test_smoke_scipy_datasets.py -v`
- Discovery guardrail: `pytest -m smoke_minimal tests/smoke/test_smoke_scipy_discovery.py -v`
</verification>

<success_criteria>
- Dataset guardrail fails fast if required in-repo datasets are missing.
- Discovery guardrail proves list/describe returns usable schemas for one representative tool per SciPy submodule.
</success_criteria>

<output>
After completion, create `.planning/phases/10-verification-smoke-testing/10-03-SUMMARY.md`
</output>
