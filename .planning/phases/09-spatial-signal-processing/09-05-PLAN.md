---
phase: 09-spatial-signal-processing
plan: 5
type: execute
wave: 4
depends_on: ["09-03", "09-04"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_signal.py
  - src/bioimage_mcp/registry/dynamic/adapters/pandas.py
  - tests/unit/registry/dynamic/test_scipy_spatial_execute.py
  - tests/unit/registry/dynamic/test_scipy_signal_execute.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "KDTree build returns a schema-valid ObjectRef (including ref_id) so MCP validation does not fail"
    - "scipy.signal.periodogram executes when the input artifact is provided as a plain string URI/path (no TypeError)"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      provides: "KDTree build returns ObjectRef with ref_id"
      contains: "_execute_kdtree_build"
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_signal.py
      provides: "Spectral analysis accepts string inputs via ANY_TO_TABLE"
      contains: "scipy.signal.periodogram"
    - path: src/bioimage_mcp/registry/dynamic/adapters/pandas.py
      provides: "Table loader can accept string uri/path inputs"
      contains: "def _load_table"
    - path: tests/unit/registry/dynamic/test_scipy_spatial_execute.py
      provides: "Regression test for ObjectRef validation (ref_id required)"
      contains: "test_execute_kdtree_lifecycle"
    - path: tests/unit/registry/dynamic/test_scipy_signal_execute.py
      provides: "Regression test for periodogram with string input"
      contains: "test_scipy_signal_periodogram"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      to: src/bioimage_mcp/artifacts/models.py
      via: "ObjectRef model_validate requires ref_id"
      pattern: "ref_id"
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_signal.py
      to: src/bioimage_mcp/registry/dynamic/adapters/pandas.py
      via: "PandasAdapterForRegistry._load_table input normalization"
      pattern: "_load_table"
---

<objective>
Close the Phase 09 execution gaps identified in the SciPy spatial/signal adapters: (1) KDTree build returns an invalid ObjectRef (missing ref_id), and (2) periodogram execution can crash when the input is passed as a plain string.

Purpose: These failures surface as runtime schema validation errors and TypeErrors in real MCP client usage, even when unit tests pass.
Output: Schema-valid KDTree ObjectRefs and robust ANY_TO_TABLE handling for periodogram string inputs, with regression tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/debug/scipy-spatial-kdtree-ref-id.md
@.planning/debug/scipy-signal-periodogram-typeerror.md

@src/bioimage_mcp/artifacts/models.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_signal.py
@src/bioimage_mcp/registry/dynamic/adapters/pandas.py
</context>

<tasks>

<task type="auto">
  <name>Fix KDTree build output: add ref_id to ObjectRef</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py, tests/unit/registry/dynamic/test_scipy_spatial_execute.py</files>
  <action>
In `src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py`, update `_execute_kdtree_build` so the returned ObjectRef dict includes a valid `ref_id` string.

Implementation constraints:
- Use the same identifier already generated for the object URI segment (the `uid = uuid.uuid4().hex`) as `ref_id`.
- Do not change the `uri` scheme (`obj://default/scipy_spatial/{uid}`) or `storage_type`.
- Keep existing metadata keys (`n_points`, `n_dims`, `selected_columns`).

In `tests/unit/registry/dynamic/test_scipy_spatial_execute.py`:
- Extend `test_execute_kdtree_lifecycle` to assert `ref_obj["ref_id"]` exists and is a non-empty string.
- Add a strict schema regression by validating the returned dict via the Pydantic model:
  - `from bioimage_mcp.artifacts.models import ObjectRef`
  - `ObjectRef.model_validate(ref_obj)` must not raise.

This aligns behavior with the Pandas adapter, which already returns `ObjectRef` with `ref_id`.
  </action>
  <verify>conda run -n bioimage-mcp-base pytest tests/unit/registry/dynamic/test_scipy_spatial_execute.py -q</verify>
  <done>KDTree build returns an ObjectRef containing `ref_id`, and ObjectRef validation passes in the unit test.</done>
</task>

<task type="auto">
  <name>Fix periodogram execution: accept string inputs (URI/path) for ANY_TO_TABLE</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_signal.py, src/bioimage_mcp/registry/dynamic/adapters/pandas.py, tests/unit/registry/dynamic/test_scipy_signal_execute.py</files>
  <action>
Address the diagnosis in `.planning/debug/scipy-signal-periodogram-typeerror.md` by making the spectral ANY_TO_TABLE path resilient to string inputs.

In `src/bioimage_mcp/registry/dynamic/adapters/pandas.py` (`PandasAdapterForRegistry._load_table`):
- Add a first-class normalization branch for `artifact` being a plain `str`.
  - Treat the string as a `uri`/path and reuse the existing parsing logic (including `obj://` cache lookup).
  - Do not require callers to wrap string values into dict-like refs.

In `src/bioimage_mcp/registry/dynamic/adapters/scipy_signal.py` (spectral branch for `periodogram`/`welch`):
- Ensure the code path does not assume dict/object refs when checking artifact type.
- When `art_ref` is a string, route it through the table path (i.e., call `_load_table` with the string) and surface a clear error if it cannot be loaded as a table.

In `tests/unit/registry/dynamic/test_scipy_signal_execute.py`:
- Add a regression test that passes a plain string input (e.g. `"file:///tmp/table.parquet"`) as the `("input", <string>)` value to `adapter.execute("scipy.signal.periodogram", ...)`.
- Patch `PandasAdapterForRegistry._load_table` to return a known DataFrame and assert:
  - no exception is raised (specifically: no `TypeError: string indices must be integers, not 'str'`)
  - `_save_table` is called and the saved DataFrame contains `frequency` and `power` columns.
  </action>
  <verify>conda run -n bioimage-mcp-base pytest tests/unit/registry/dynamic/test_scipy_signal_execute.py -q</verify>
  <done>`scipy.signal.periodogram` accepts a plain string URI/path input and returns a TableRef without raising a TypeError.</done>
</task>

<task type="auto">
  <name>Run focused regression suite for Phase 09 gap closures</name>
  <files>tests/unit/registry/dynamic/test_scipy_spatial_execute.py, tests/unit/registry/dynamic/test_scipy_signal_execute.py</files>
  <action>
Run the focused unit tests that cover the two gap closures (KDTree ObjectRef schema validity + periodogram string input normalization).

If failures occur, fix forward (do not revert unrelated changes).
  </action>
  <verify>conda run -n bioimage-mcp-base pytest tests/unit/registry/dynamic/test_scipy_spatial_execute.py tests/unit/registry/dynamic/test_scipy_signal_execute.py -q</verify>
  <done>Both unit test files pass in the `bioimage-mcp-base` environment.</done>
</task>

</tasks>

<verification>
- `conda run -n bioimage-mcp-base pytest tests/unit/registry/dynamic/test_scipy_spatial_execute.py tests/unit/registry/dynamic/test_scipy_signal_execute.py -q`
</verification>

<success_criteria>
- KDTree build (`scipy.spatial.cKDTree`) no longer fails ObjectRef validation due to missing `ref_id`.
- Periodogram execution (`scipy.signal.periodogram`) accepts string artifact inputs (URI/path) without TypeError and returns a persisted TableRef.
</success_criteria>

<output>
After completion, create `.planning/phases/09-spatial-signal-processing/09-05-SUMMARY.md`
</output>
