---
phase: 09-spatial-signal-processing
plan: 2
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
  - tests/contract/test_scipy_spatial_adapter.py
  - tests/unit/registry/dynamic/test_scipy_spatial_execute.py
autonomous: true

must_haves:
  truths:
    - "Distance metrics (euclidean/cosine/mahalanobis) can be computed between two point-set tables and returned as a .npy NativeOutputRef"
    - "Voronoi and Delaunay tessellations can be computed from a point-set table and returned as JSON"
    - "Spatial wrappers are discoverable through the scipy dynamic adapter"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      provides: "Curated spatial wrapper discovery + execution"
      contains: "cdist_table"
    - path: tests/contract/test_scipy_spatial_adapter.py
      provides: "Discovery contract coverage for spatial wrappers"
      contains: "test_scipy_spatial_adapter_discovers"
    - path: tests/unit/registry/dynamic/test_scipy_spatial_execute.py
      provides: "Hermetic execution tests for distance/tessellation wrappers"
      contains: "test_"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      to: src/bioimage_mcp/registry/dynamic/adapters/pandas.py
      via: "PandasAdapterForRegistry._load_table"
      pattern: "_load_table"
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      to: "scipy.spatial.distance.cdist"
      via: "distance computation"
      pattern: "cdist"

<objective>
Implement curated `scipy.spatial` wrappers for distance metrics and tessellations, using TableRef inputs with artifact-friendly outputs.

Purpose: Spatial analysis in Phase 09 depends on robust point-set I/O and predictable artifacts (distance matrices as `.npy`; tessellations as JSON) for downstream reasoning.
Output: Spatial wrapper discovery + execution + tests (distance + Voronoi + Delaunay).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/07-transforms-measurements/07-01-SUMMARY.md
@.planning/phases/08-statistical-analysis/08-01-SUMMARY.md

@src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
@src/bioimage_mcp/registry/dynamic/adapters/pandas.py
@src/bioimage_mcp/registry/dynamic/object_cache.py
</context>

<tasks>

<task type="auto">
  <name>Implement distance + tessellation wrapper discovery metadata</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py</files>
  <action>
In `src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py`, implement `ScipySpatialAdapter.discover()` to return curated wrapper metadata when the module list includes `scipy.spatial`.

Add `FunctionMetadata` entries (matching Phase 08 style) with `source_adapter="scipy_spatial"`:

1) `scipy.spatial.distance.cdist_table`
- `io_pattern`: `IOPattern.TABLE_PAIR_TO_FILE`
- inputs: `table_a`, `table_b`
- params:
  - `metric` (string enum: ["euclidean", "cosine", "mahalanobis"], default "euclidean")
  - `columns_a` (array of strings, optional): coordinate columns for table_a
  - `columns_b` (array of strings, optional): coordinate columns for table_b
  - `vi_strategy` (string enum: ["auto", "from_a", "from_ab", "from_param"], default "auto")
  - `vi` (array of arrays of numbers, optional): inverse covariance matrix for mahalanobis when `vi_strategy="from_param"`

2) `scipy.spatial.voronoi_table`
- `io_pattern`: `IOPattern.TABLE_TO_JSON`
- params:
  - `columns` (array of strings, optional)

3) `scipy.spatial.delaunay_table`
- `io_pattern`: `IOPattern.TABLE_TO_JSON`
- params:
  - `columns` (array of strings, optional)

Descriptions/tags:
- cdist_table tags include: ["spatial", "distance"]
- voronoi/delaunay tags include: ["spatial", "tessellation"]

Do not add KDTree wrappers in this plan (Plan 09-03).
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py</verify>
  <done>Discovery returns the three wrapper fn_ids with the specified io_patterns and parameter schemas.</done>
</task>

<task type="auto">
  <name>Implement distance + tessellation execution and add tests</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py, tests/contract/test_scipy_spatial_adapter.py, tests/unit/registry/dynamic/test_scipy_spatial_execute.py</files>
  <action>
In `src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py`, implement `execute()` for the three curated wrapper fn_ids:

Shared table loading/column selection:
- Use `PandasAdapterForRegistry()._load_table(artifact)` to get a DataFrame for `TableRef` or `ObjectRef`.
- If `columns*` param is provided, select exactly those columns.
- Else select all numeric columns and use them as coordinates (order preserved).
- Convert to `np.asarray(df[selected_cols].to_numpy())`.
- Raise a clear `ValueError` if fewer than 2 numeric columns are available.

1) `scipy.spatial.distance.cdist_table`
- Import and call `scipy.spatial.distance.cdist(XA, XB, metric=metric, **kwargs)`.
- For `metric="mahalanobis"`, compute or use `VI`:
  - `from_param`: `VI = np.asarray(params["vi"])`
  - `from_a`: `VI = np.linalg.inv(np.cov(XA.T))`
  - `from_ab`: compute covariance from `np.vstack([XA, XB])`
  - `auto`: use `from_ab`
- Persist the raw distance matrix as `.npy` (NativeOutputRef, `format: "npy"`):
  - `path = work_dir / "cdist.npy"` (ensure `work_dir` exists)
  - `np.save(path, distances)`
  - Return a `NativeOutputRef` dict with:
    - `type: "NativeOutputRef"`
    - `format: "npy"`
    - `uri`, `path`
    - `metadata` includes: `metric`, `shape`, and `selected_columns`

2) `scipy.spatial.voronoi_table`
- Construct `scipy.spatial.Voronoi(points)`.
- Build a JSON-serializable payload (convert numpy arrays to lists):
  - `vertices`, `ridge_points`, `ridge_vertices`, `regions`, `point_region`
  - `selected_columns`
- Persist the payload as a JSON artifact via the existing `_save_json(...)` helper (as used in `ScipyStatsAdapter`), and return the resulting artifact ref for `IOPattern.TABLE_TO_JSON`:
  - `ref = self._save_json(payload, work_dir=work_dir, filename="voronoi.json", metadata_override={"fn_id": fn_id, "selected_columns": selected_columns})`
  - `return [ref]` (ref is a JSON artifact ref dict, typically `NativeOutputRef` with `format: "json"`)

3) `scipy.spatial.delaunay_table`
- Construct `scipy.spatial.Delaunay(points)`.
- Build a JSON-serializable payload:
  - `simplices`, `neighbors`
  - `selected_columns`
- Persist the payload as a JSON artifact via `_save_json(...)` and return the artifact ref for `IOPattern.TABLE_TO_JSON`:
  - `ref = self._save_json(payload, work_dir=work_dir, filename="delaunay.json", metadata_override={"fn_id": fn_id, "selected_columns": selected_columns})`
  - `return [ref]`

Add tests:

- `tests/contract/test_scipy_spatial_adapter.py` (mark `@pytest.mark.requires_base`):
  - discovery includes the three fn_ids
  - io_patterns match expected values
  - composite `ScipyAdapter().discover({"modules": ["scipy.ndimage", "scipy.spatial"]})` includes `scipy.spatial.distance.cdist_table`

- `tests/unit/registry/dynamic/test_scipy_spatial_execute.py` (mark `@pytest.mark.requires_base`):
  - mock table DataFrames via `object_cache.register(uri, df)` and pass inputs as `{ "uri": uri, "type": "TableRef" }`
  - patch `scipy.spatial.distance.cdist`, `scipy.spatial.Voronoi`, `scipy.spatial.Delaunay` to avoid real SciPy compute
  - for `cdist_table`, assert return is `NativeOutputRef` with `format == "npy"` and `path` ending with `.npy`, then `np.load(path)` has the stubbed shape
  - for Voronoi/Delaunay, assert JSON files contain expected keys and `selected_columns` values
  - for mahalanobis `vi_strategy="from_param"`, assert cdist called with `VI=` argument
  </action>
  <verify>conda run -n bioimage-mcp-base pytest tests/contract/test_scipy_spatial_adapter.py tests/unit/registry/dynamic/test_scipy_spatial_execute.py -q</verify>
  <done>`cdist_table` returns a `.npy` NativeOutputRef distance matrix with stable metadata, Voronoi/Delaunay return JSON with stable keys, and the new contract + unit tests pass.</done>
</task>

</tasks>

<verification>
- `conda run -n bioimage-mcp-base pytest tests/contract/test_scipy_spatial_adapter.py tests/unit/registry/dynamic/test_scipy_spatial_execute.py -q`
</verification>

<success_criteria>
- SPATIAL-01 satisfied: euclidean/cosine/mahalanobis distances computed between two point tables
- SPATIAL-03 satisfied: Voronoi + Delaunay computed from point table
- Discovery exposes the wrapper fn_ids under the scipy adapter
</success_criteria>

<output>
After completion, create `.planning/phases/09-spatial-signal-processing/09-02-SUMMARY.md`
</output>
