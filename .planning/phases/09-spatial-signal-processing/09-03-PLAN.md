---
phase: 09-spatial-signal-processing
plan: 3
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
  - tests/contract/test_scipy_spatial_adapter.py
  - tests/unit/registry/dynamic/test_scipy_spatial_execute.py
autonomous: true

must_haves:
  truths:
    - "A KDTree can be built from a point table and returned as an ObjectRef"
    - "A KDTree ObjectRef can be queried against a point table to return nearest-neighbor indices + distances as JSON"
    - "KDTree objects persist via OBJECT_CACHE for the duration of the session"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      provides: "KDTree build/query wrappers"
      contains: "kdtree_build_table"
    - path: src/bioimage_mcp/registry/dynamic/object_cache.py
      provides: "In-memory object persistence"
      contains: "OBJECT_CACHE"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      to: src/bioimage_mcp/registry/dynamic/object_cache.py
      via: "obj:// URI registration"
      pattern: "OBJECT_CACHE"
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
      to: "scipy.spatial.cKDTree"
      via: "build/query"
      pattern: "cKDTree"

<objective>
Implement KDTree creation and nearest-neighbor query wrappers with session persistence via `OBJECT_CACHE`.

Purpose: Spatial nearest-neighbor workflows require object lifecycle: build once, query many times, without reloading data.
Output: KDTree build/query wrappers + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/bioimage_mcp/registry/dynamic/object_cache.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py
@src/bioimage_mcp/registry/loader.py
</context>

<tasks>

<task type="auto">
  <name>Add KDTree wrapper discovery metadata</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py</files>
  <action>
Extend `ScipySpatialAdapter.discover()` to include two additional curated wrapper fn_ids:

1) `scipy.spatial.kdtree_build_table`
- `io_pattern`: `IOPattern.TABLE_TO_OBJECT`
- params:
  - `columns` (array of strings, optional)
  - `leafsize` (integer, optional)
  - `balanced_tree` (boolean, optional)
  - `compact_nodes` (boolean, optional)

2) `scipy.spatial.kdtree_query_table`
- `io_pattern`: `IOPattern.OBJECT_AND_TABLE_TO_JSON`
- inputs: `object` (KDTree ObjectRef), `table` (query points)
- params:
  - `k` (integer, default 1)
  - `eps` (number, default 0.0)
  - `p` (number, default 2.0)
  - `distance_upper_bound` (number, optional)
  - `workers` (integer, optional)
  - `columns` (array of strings, optional)

Tags:
- build: ["spatial", "kdtree", "index"]
- query: ["spatial", "kdtree", "nearest-neighbor"]

Keep wrapper ids stable; do not rename previously added wrappers.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py</verify>
  <done>Discovery includes KDTree build/query wrapper fn_ids with the specified io_patterns and parameters.</done>
</task>

<task type="auto">
  <name>Implement KDTree build/query execution and extend tests</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py, tests/contract/test_scipy_spatial_adapter.py, tests/unit/registry/dynamic/test_scipy_spatial_execute.py</files>
  <action>
In `src/bioimage_mcp/registry/dynamic/adapters/scipy_spatial.py`, implement `execute()` support for:

1) `scipy.spatial.kdtree_build_table`
- Load points from the input `table` (same column-selection rules as Plan 09-02).
- Build `scipy.spatial.cKDTree(points, leafsize=..., balanced_tree=..., compact_nodes=...)`.
- Register the tree in `OBJECT_CACHE` under a unique URI:
  - `uri = f"obj://default/scipy_spatial/{uuid}"` (use `uuid.uuid4().hex`).
- Return a single output dict:
  - `type: "ObjectRef"`
  - `python_class: "scipy.spatial.cKDTree"`
  - `uri: <uri>`
  - `storage_type: "memory"`
  - `metadata` includes: `n_points`, `n_dims`, and `selected_columns`.

2) `scipy.spatial.kdtree_query_table`
- Read the KDTree object from the `object` input:
  - Validate `object["uri"]` starts with `obj://` and exists in `OBJECT_CACHE`, else raise a clear error.
- Load query points from `table`.
- Call `tree.query(query_points, k=k, eps=eps, p=p, distance_upper_bound=..., workers=...)`.
- Return JSON via `_save_json` with keys:
  - `k`, `shape` ([n_query, k])
  - `distances`, `indices` (nested lists)
  - `selected_columns`

Extend tests:

- Update `tests/contract/test_scipy_spatial_adapter.py` to assert the new wrapper fn_ids exist.
- Update `tests/unit/registry/dynamic/test_scipy_spatial_execute.py`:
  - patch `scipy.spatial.cKDTree` to return a mock object with `.query()` returning `(dist, idx)` numpy arrays
  - assert `OBJECT_CACHE` receives an entry for the produced `obj://...` uri
  - assert query reads from `OBJECT_CACHE` and JSON output contains `indices` + `distances`
  </action>
  <verify>conda run -n bioimage-mcp-base pytest tests/contract/test_scipy_spatial_adapter.py tests/unit/registry/dynamic/test_scipy_spatial_execute.py -q</verify>
  <done>KDTree objects can be built from a TableRef and queried against another TableRef, producing stable JSON results; tests pass.</done>
</task>

</tasks>

<verification>
- `conda run -n bioimage-mcp-base pytest tests/contract/test_scipy_spatial_adapter.py tests/unit/registry/dynamic/test_scipy_spatial_execute.py -q`
</verification>

<success_criteria>
- SPATIAL-02 satisfied: build KDTree -> get ObjectRef -> query returns neighbor indices + distances in JSON
- KDTree persistence uses `OBJECT_CACHE` with `obj://` URIs
</success_criteria>

<output>
After completion, create `.planning/phases/09-spatial-signal-processing/09-03-SUMMARY.md`
</output>
