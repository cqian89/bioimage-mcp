---
phase: 21-usam-tool-pack-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/microsam/manifest.yaml
  - tools/microsam/bioimage_mcp_microsam/__init__.py
  - tools/microsam/bioimage_mcp_microsam/entrypoint.py
  - tools/microsam/bioimage_mcp_microsam/install_models.py
autonomous: true

must_haves:
  truths:
    - "Tool registry can load a microsam tool pack manifest without errors"
    - "A runnable script exists to ensure required microsam models are cached"
  artifacts:
    - path: tools/microsam/manifest.yaml
      provides: "Microsam tool pack declaration (env_id + entrypoint)"
      contains: "tool_id: tools.microsam"
    - path: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      provides: "Tool pack entrypoint executable under the microsam env"
    - path: tools/microsam/bioimage_mcp_microsam/install_models.py
      provides: "Model download + cache inspection helper used by installer"
  key_links:
    - from: tools/microsam/manifest.yaml
      to: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      via: "manifest.entrypoint resolves to an existing file"
      pattern: "entrypoint: bioimage_mcp_microsam/entrypoint.py"
---

<objective>
Create the µSAM tool pack scaffold so the core installer can reference a stable tool_id/env_id and run tool-local helper scripts.

Purpose: Establish the on-disk tool pack structure for `microsam` while keeping Phase 21 focused on environment + model readiness (not segmentation APIs yet).
Output: A new `tools/microsam/` tool pack with a valid manifest, entrypoint, and a model-install helper script.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-usam-tool-pack-foundation/21-CONTEXT.md
@.planning/phases/21-usam-tool-pack-foundation/21-RESEARCH.md

@src/bioimage_mcp/registry/loader.py
@src/bioimage_mcp/runtimes/executor.py
@tools/cellpose/manifest.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add microsam tool pack manifest + entrypoint skeleton</name>
  <files>
tools/microsam/manifest.yaml
tools/microsam/bioimage_mcp_microsam/__init__.py
tools/microsam/bioimage_mcp_microsam/entrypoint.py
  </files>
  <action>
- Create a new tool pack directory `tools/microsam/` following the established tool-pack pattern (see `tools/cellpose/manifest.yaml`).
- Add `tools/microsam/manifest.yaml` with:
  - `tool_id: tools.microsam`
  - `env_id: bioimage-mcp-microsam`
  - `entrypoint: bioimage_mcp_microsam/entrypoint.py` (relative path; registry loader will resolve to an absolute path).
  - `platforms_supported` set to Linux and macOS (at minimum `linux-64` and `osx-arm64`).
  - Include a `meta.describe` function definition compatible with existing tool packs (keep it minimal; Phase 21 does not require exposing microsam inference tools yet).
- Add a minimal `tools/microsam/bioimage_mcp_microsam/entrypoint.py` that:
  - Implements the standard JSON-in / JSON-out protocol expected by the runtime executor.
  - Supports `meta.describe` requests by delegating to the shared meta protocol pattern used in other tool packs.
- Keep the entrypoint import surface tiny to avoid importing `torch`/`micro_sam` unless explicitly needed (Phase 21 foundation; heavy imports will be used by model setup script and later phases).
  </action>
  <verify>
python -c "from pathlib import Path; from bioimage_mcp.registry.loader import load_manifest_file; m, d = load_manifest_file(Path('tools/microsam/manifest.yaml')); assert m is not None and m.tool_id == 'tools.microsam'; assert m.env_id == 'bioimage-mcp-microsam'; assert Path(m.entrypoint).exists(); print('ok')"
  </verify>
  <done>
- `load_manifest_file(Path('tools/microsam/manifest.yaml'))` returns a ToolManifest with `tool_id=tools.microsam` and an entrypoint path that exists on disk.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tool-local model ensure-cached script used by the installer</name>
  <files>tools/microsam/bioimage_mcp_microsam/install_models.py</files>
  <action>
- Create `tools/microsam/bioimage_mcp_microsam/install_models.py` as a CLI-friendly script that will be executed inside the `bioimage-mcp-microsam` conda env during `bioimage-mcp install microsam`.
- The script must:
  - Ensure the minimum required models are present in micro-sam's default cache directory: Generalist, LM, EM (use `vit_b` variants as the minimum requirement).
  - Use micro-sam's built-in model registry/download utilities (do not hand-roll HTTP downloads).
  - Print a single JSON object to stdout with at least:
    - `cache_dir` (resolved cache directory)
    - `models` mapping from model_id -> absolute path
    - `ok: true|false` and `error` fields on failure
- Exit code: `0` on success; non-zero on failure.
- Keep script arguments internal/simple (e.g., optional `--json` is fine; no new `bioimage-mcp install` flags are introduced in Phase 21).
  </action>
  <verify>
python -m compileall tools/microsam/bioimage_mcp_microsam >/dev/null
  </verify>
  <done>
- Script file exists and is syntactically valid Python.
- Script prints machine-readable JSON on success/failure when executed in its target env (execution wired in Plan 21-03).
  </done>
</task>

</tasks>

<verification>
- Registry can load the microsam manifest (no diagnostics errors).
- Entrypoint path is resolved to an existing file (absolute path after loader resolution).
</verification>

<success_criteria>
- The repository contains a valid `tools/microsam` tool pack scaffold with a resolvable entrypoint and a model installer script ready to be invoked by the bootstrap installer.
</success_criteria>

<output>
After completion, create `.planning/phases/21-usam-tool-pack-foundation/21-01-SUMMARY.md`
</output>
