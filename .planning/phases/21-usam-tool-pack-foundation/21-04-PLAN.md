---
phase: 21-usam-tool-pack-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - "21-03"
files_modified:
  - src/bioimage_mcp/config/schema.py
  - src/bioimage_mcp/bootstrap/configure.py
  - src/bioimage_mcp/bootstrap/checks.py
  - tests/unit/bootstrap/test_doctor_output.py
autonomous: true

must_haves:
  truths:
    - "User can force microsam device selection via config key `microsam.device` (auto|cuda|mps|cpu)"
    - "`bioimage-mcp doctor` reports missing microsam models as NOT READY with actionable remediation"
  artifacts:
    - path: src/bioimage_mcp/config/schema.py
      provides: "Config schema includes microsam settings"
      contains: "microsam"
    - path: src/bioimage_mcp/bootstrap/checks.py
      provides: "Doctor checks include microsam model presence verification"
    - path: src/bioimage_mcp/bootstrap/configure.py
      provides: "Starter config includes microsam.device override"
  key_links:
    - from: src/bioimage_mcp/bootstrap/install.py
      to: src/bioimage_mcp/bootstrap/checks.py
      via: "install writes a model-cache record that doctor verifies"
      pattern: "microsam_models"
---

<objective>
Add config + doctor verification for the µSAM foundation: device selection configuration and readiness checks for required cached models.

Purpose: Make Phase 21 verifiable and user-controllable (device override) without introducing per-tool parameters.
Output: Config schema update + doctor checks for microsam model readiness.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-usam-tool-pack-foundation/21-CONTEXT.md
@.planning/REQUIREMENTS.md

@src/bioimage_mcp/config/schema.py
@src/bioimage_mcp/config/loader.py
@src/bioimage_mcp/bootstrap/checks.py
@src/bioimage_mcp/bootstrap/configure.py
@src/bioimage_mcp/bootstrap/doctor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `microsam.device` to config schema and starter config</name>
  <files>
src/bioimage_mcp/config/schema.py
src/bioimage_mcp/bootstrap/configure.py
  </files>
  <action>
- Update `src/bioimage_mcp/config/schema.py` to add a new nested config section for microsam:
  - Key path: `microsam.device`
  - Allowed values: `auto|cuda|mps|cpu`
  - Default: `auto`
  - Validation: if value is not in the allowlist, raise a config validation error.
- Update `src/bioimage_mcp/bootstrap/configure.py` starter config template to include a microsam section with the default device setting (so users see the override point).
- Do NOT add per-tool parameters; config-only override as mandated by Phase 21 context.
  </action>
  <verify>
python -c "from bioimage_mcp.config.schema import Config; from pathlib import Path; c=Config.model_validate({'artifact_store_root': str(Path('/tmp/bioimage-mcp-artifacts')), 'tool_manifest_roots': [str(Path('/tmp'))], 'microsam': {'device': 'auto'}}); assert getattr(c, 'microsam').device.value in ('auto','cuda','mps','cpu'); print('ok')"
  </verify>
  <done>
- Config schema accepts `microsam.device` and rejects invalid values.
- Starter config includes a visible `microsam:` section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add doctor check for required microsam models using the installer record</name>
  <files>src/bioimage_mcp/bootstrap/checks.py</files>
  <action>
- Add a new readiness check in `src/bioimage_mcp/bootstrap/checks.py` that verifies the required microsam models are present AFTER install:
  - Read a stable record written by the installer (Plan 21-03) from `.bioimage-mcp/` (recommend: `.bioimage-mcp/state/microsam_models.json`).
  - Validate it contains (at minimum) the required model identifiers for Generalist, LM, and EM `vit_b` variants.
  - Validate that the referenced model files exist on disk.
  - If the record is missing or incomplete, mark `ok=False` and provide remediation:
    - `bioimage-mcp install microsam --profile cpu`
    - indicate the expected cache path (if available)
    - include 2-3 concrete fix suggestions (network, permissions, clear corrupt file)
- Add the check to `run_all_checks()` so `bioimage-mcp doctor` fails (NOT READY) if microsam env is installed but models are missing.
- Keep the check lightweight: filesystem existence checks only; do not import torch or run inference.
  </action>
  <verify>
pytest tests/unit/bootstrap/test_doctor_output.py -q
  </verify>
  <done>
- `bioimage-mcp doctor` includes a microsam model readiness check and prints actionable remediation when models are missing.
  </done>
</task>

</tasks>

<verification>
- Config validation works for microsam.device.
- Doctor reports model readiness problems with clear remediation.
</verification>

<success_criteria>
- Users can configure device selection via `microsam.device`.
- Doctor surfaces missing/corrupt model cache as a blocking readiness issue.
</success_criteria>

<output>
After completion, create `.planning/phases/21-usam-tool-pack-foundation/21-04-SUMMARY.md`
</output>
