---
phase: 21-usam-tool-pack-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - envs/bioimage-mcp-microsam.yaml
  - envs/bioimage-mcp-microsam.lock.yml
autonomous: true

must_haves:
  truths:
    - "A dedicated isolated conda environment spec exists for microsam (napari + micro-sam + torch)"
    - "The microsam environment can be installed reproducibly using a lockfile on supported platforms"
  artifacts:
    - path: envs/bioimage-mcp-microsam.yaml
      provides: "Conda environment spec for bioimage-mcp-microsam"
      contains: "name: bioimage-mcp-microsam"
    - path: envs/bioimage-mcp-microsam.lock.yml
      provides: "Resolved conda-lock for reproducible installs"
  key_links:
    - from: envs/bioimage-mcp-microsam.yaml
      to: envs/bioimage-mcp-microsam.lock.yml
      via: "conda-lock source metadata"
      pattern: "sources:\n  - bioimage-mcp-microsam.yaml"
---

<objective>
Add the conda environment definition required to install the µSAM tool pack in an isolated runtime.

Purpose: Establish the foundational environment for µSAM (napari + micro-sam + torch) and unblock Phase 21 install semantics.
Output: A new `envs/bioimage-mcp-microsam.yaml` and its lockfile.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-usam-tool-pack-foundation/21-CONTEXT.md
@.planning/phases/21-usam-tool-pack-foundation/21-RESEARCH.md

@envs/bioimage-mcp-cellpose.yaml
@envs/bioimage-mcp-stardist.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create microsam conda environment spec (CPU-first, cross-platform)</name>
  <files>envs/bioimage-mcp-microsam.yaml</files>
  <action>
- Create `envs/bioimage-mcp-microsam.yaml`.
- Requirements for this env spec:
  - Name: `bioimage-mcp-microsam`.
  - Pin Python to match other deep-learning tool envs: `python=3.11`.
  - Include `pip` (needed for Plan 21-03 post-install pip-only deps).
  - Include µSAM + napari:
    - Conda package is typically `micro-sam` (Python import is `micro_sam`).
    - Include `napari`.
  - Include PyTorch + torchvision compatible with micro-sam (target torch 2.4+).
  - Include model-caching dependencies used by micro-sam (`pooch`, `python-xxhash`).
  - Include standard image I/O packages used across tool packs (`bioio`, `bioio-ome-tiff`, `bioio-ome-zarr`) to reduce future Phase 22/23 churn.
  - Prefer conda-forge for the base stack; allow additional channels as needed for torch.
- Do NOT add a `pip:` section in this env file.
  - Rationale: current installer prefers conda-lock when possible; pip sections disable lockfile usage in `src/bioimage_mcp/bootstrap/install.py`.
  - Pip-only deps (trackastra + MobileSAM) are installed explicitly by microsam's install orchestration in Plan 21-03.
  </action>
  <verify>
python -c "import yaml; d=yaml.safe_load(open('envs/bioimage-mcp-microsam.yaml')); assert d['name']=='bioimage-mcp-microsam'; deps=[str(x) for x in (d.get('dependencies',[]) or [])]; assert any(x.startswith('micro_sam') or x.startswith('micro-sam') for x in deps); print('ok')"
  </verify>
  <done>
- Environment spec exists and includes micro_sam, napari, torch/torchvision, and model-caching deps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and check in conda-lock lockfile for microsam</name>
  <files>envs/bioimage-mcp-microsam.lock.yml</files>
  <action>
- Generate a lockfile from `envs/bioimage-mcp-microsam.yaml` using `conda-lock`.
- Keep lockfile generation consistent with existing env lockfiles:
  - Output path: `envs/bioimage-mcp-microsam.lock.yml`
  - Lock platforms: `linux-64` and `osx-arm64` (Phase 21 support target).
  - Ensure the lock metadata `sources` includes `bioimage-mcp-microsam.yaml`.
- Note: pip-only deps are installed by Plan 21-03; lockfile is for conda deps only.
  </action>
  <verify>
test -f envs/bioimage-mcp-microsam.lock.yml && python -c "import yaml; d=yaml.safe_load(open('envs/bioimage-mcp-microsam.lock.yml')); md=d.get('metadata',{}); assert 'sources' in md and 'bioimage-mcp-microsam.yaml' in md['sources']; plats=set(md.get('platforms',[]) or []); assert 'linux-64' in plats and 'osx-arm64' in plats; print('ok')"
  </verify>
  <done>
- Lockfile exists in `envs/bioimage-mcp-microsam.lock.yml` and references `bioimage-mcp-microsam.yaml` in metadata.
  </done>
</task>

</tasks>

<verification>
- Environment YAML is valid and references required packages.
- Lockfile exists and is parseable YAML.
</verification>

<success_criteria>
- The repository contains `envs/bioimage-mcp-microsam.yaml` + `envs/bioimage-mcp-microsam.lock.yml` suitable for installation tooling.
</success_criteria>

<output>
After completion, create `.planning/phases/21-usam-tool-pack-foundation/21-02-SUMMARY.md`
</output>
