---
phase: 23-microsam-interactive-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  - src/bioimage_mcp/registry/dynamic/models.py
  - src/bioimage_mcp/registry/engine.py
  - tests/unit/registry/test_microsam_adapter_discovery.py
  - tests/smoke/test_microsam_headless_live.py
autonomous: true

must_haves:
  truths:
    - "bioimage-mcp list exposes micro_sam.sam_annotator.annotator_2d, annotator_3d, and annotator_tracking"
    - "bioimage-mcp describe for sam_annotator entrypoints keeps image/embedding/segmentation as artifact ports, not raw JSON params"
    - "sam_annotator entrypoints use a dedicated I/O pattern so run() can accept image artifacts and optional embedding/label artifacts"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      provides: "Discovery includes micro_sam.sam_annotator callables with entrypoint-aware io_pattern resolution"
    - path: src/bioimage_mcp/registry/dynamic/models.py
      provides: "IOPattern enum includes SAM_ANNOTATOR"
    - path: src/bioimage_mcp/registry/engine.py
      provides: "SAM_ANNOTATOR port mapping for image + optional embedding_path/segmentation_result"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      to: src/bioimage_mcp/registry/dynamic/models.py
      via: "resolve_io_pattern assigns IOPattern.SAM_ANNOTATOR for annotator entrypoints"
      pattern: "SAM_ANNOTATOR"
    - from: src/bioimage_mcp/registry/engine.py
      to: micro_sam.sam_annotator.annotator_2d
      via: "map_io_pattern_to_ports converts SAM_ANNOTATOR into artifact ports"
      pattern: "IOPattern\.SAM_ANNOTATOR"
---

<objective>
Expose the micro_sam interactive annotator API surface through MCP discovery/describe with artifact-safe schemas.

Purpose: Phase 23 starts by making `micro_sam.sam_annotator.*` discoverable without breaking artifact-boundary contracts established in Phase 22.
Output: Annotator entrypoints appear in list/describe with stable I/O pattern mapping for image, optional embedding state, and optional initial segmentation.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-microsam-interactive-bridge/23-CONTEXT.md
@.planning/phases/23-microsam-interactive-bridge/23-RESEARCH.md
@.planning/phases/22-mu-sam-headless-api/22-03-SUMMARY.md
@src/bioimage_mcp/registry/dynamic/adapters/microsam.py
@src/bioimage_mcp/registry/dynamic/models.py
@src/bioimage_mcp/registry/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Include sam_annotator in MicrosamAdapter discovery with entrypoint classification</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </files>
  <action>
- Update `MicrosamAdapter.discover()` to include `micro_sam.sam_annotator` modules instead of excluding them.
- Keep existing safety filters for private/test modules and unstable callables (`**kwargs` exclusion stays in place).
- Preserve fully-qualified IDs (`micro_sam.sam_annotator.<callable>`) in `meta.fn_id`.
- Add explicit entrypoint detection for interactive launch functions (`annotator_2d`, `annotator_3d`, `annotator_tracking`, and `image_series_annotator` if present) so they can be treated differently from helper internals during execution in Plan 23-02.
  </action>
  <verify>
python -m compileall src/bioimage_mcp/registry/dynamic/adapters/microsam.py
python -c "from bioimage_mcp.registry.dynamic.adapters.microsam import MicrosamAdapter; assert MicrosamAdapter.__name__ == 'MicrosamAdapter'"
  </verify>
  <done>
- Discovery output includes `micro_sam.sam_annotator.annotator_2d` when micro_sam is installed.
- No private/test sam_annotator internals are exposed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SAM_ANNOTATOR pattern and artifact port mapping</name>
  <files>
src/bioimage_mcp/registry/dynamic/models.py
src/bioimage_mcp/registry/engine.py
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </files>
  <action>
- Add `SAM_ANNOTATOR` to `IOPattern` in `src/bioimage_mcp/registry/dynamic/models.py`.
- Map `IOPattern.SAM_ANNOTATOR` in `DiscoveryEngine.map_io_pattern_to_ports()` with:
  - Inputs: `image` (`BioImageRef`, required), `embedding_path` (`ObjectRef` or `NativeOutputRef`, optional), `segmentation_result` (`LabelImageRef`, optional).
  - Outputs: `labels` (`LabelImageRef`) as the exported annotation result when changes exist.
- Update `MicrosamAdapter.resolve_io_pattern()` so sam_annotator entrypoints resolve to `SAM_ANNOTATOR` while existing `SAM_PROMPT` and `SAM_AMG` behavior is preserved.
- Keep parameter filtering behavior intact so artifact ports are removed from JSON params schemas.
  </action>
  <verify>
python -m compileall src/bioimage_mcp/registry/dynamic/models.py src/bioimage_mcp/registry/engine.py
python -c "from bioimage_mcp.registry.dynamic.models import IOPattern; assert 'SAM_ANNOTATOR' in IOPattern.__members__"
  </verify>
  <done>
- `describe micro_sam.sam_annotator.annotator_2d` no longer expects raw ndarray payloads in params.
- Annotator entrypoints advertise artifact inputs/outputs in discovery metadata.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update discovery tests for Phase 23 API surface</name>
  <files>
tests/unit/registry/test_microsam_adapter_discovery.py
tests/smoke/test_microsam_headless_live.py
  </files>
  <action>
- Update `tests/unit/registry/test_microsam_adapter_discovery.py` to assert inclusion of sam_annotator entrypoints and `IOPattern.SAM_ANNOTATOR` classification for interactive entry functions.
- Replace the old smoke assertion that sam_annotator must be excluded; assert expected interactive entrypoints are present in `list` output instead.
- Keep smoke test marker strategy unchanged (`requires_env("bioimage-mcp-microsam")`, `smoke_extended`) so it remains opt-in for the microsam environment.
  </action>
  <verify>
pytest tests/unit/registry/test_microsam_adapter_discovery.py -q
  </verify>
  <done>
- Unit discovery coverage reflects Phase 23 API-04 behavior.
- Existing smoke suite no longer encodes the obsolete Phase 22 exclusion rule.
  </done>
</task>

</tasks>

<verification>
- ruff check src/bioimage_mcp/registry/dynamic/adapters/microsam.py src/bioimage_mcp/registry/dynamic/models.py src/bioimage_mcp/registry/engine.py tests/unit/registry/test_microsam_adapter_discovery.py
- pytest tests/unit/registry/test_microsam_adapter_discovery.py -q
</verification>

<success_criteria>
- API-04 is met for discovery/describe: sam_annotator entrypoints are visible and artifact-typed.
- No regression to Phase 22 SAM_PROMPT/SAM_AMG discovery behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/23-microsam-interactive-bridge/23-01-SUMMARY.md`
</output>
