---
phase: 23-microsam-interactive-bridge
plan: 02
type: execute
wave: 2
depends_on: ["23-01"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  - tools/microsam/bioimage_mcp_microsam/entrypoint.py
  - tests/unit/registry/test_microsam_adapter_interactive.py
  - tests/unit/tools/test_microsam_entrypoint_interactive.py
autonomous: true

must_haves:
  truths:
    - "run micro_sam.sam_annotator.annotator_2d opens napari in the microsam worker process and blocks until viewer close"
    - "Closing a session with committed labels returns a LabelImageRef artifact"
    - "Launching interactive annotators without a display fails with a stable, actionable headless error"
    - "If no labels are committed, run completes successfully with no output artifact and machine-readable warning"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      provides: "Interactive annotator execution bridge (image load -> napari -> label export)"
    - path: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      provides: "Structured interactive error/warning propagation in execute_result"
    - path: tests/unit/registry/test_microsam_adapter_interactive.py
      provides: "Adapter-level coverage for interactive launch/export behavior"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      to: napari.run
      via: "interactive execution path for sam_annotator entrypoints"
      pattern: "napari\.run\(\)"
    - from: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      to: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      via: "execute() exception mapping to stable error.code"
      pattern: "HEADLESS_DISPLAY_REQUIRED"
---

<objective>
Implement the interactive execution bridge from MCP artifact inputs to napari annotator sessions and back to artifact outputs.

Purpose: This is the core Phase 23 runtime behavior for GUI-01/02/03/04 and INFRA-01/02.
Output: `micro_sam.sam_annotator.*` run() launches correctly in the isolated worker, exports committed labels, and emits stable headless errors/warnings.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/23-microsam-interactive-bridge/23-CONTEXT.md
@.planning/phases/23-microsam-interactive-bridge/23-RESEARCH.md
@.planning/phases/22-mu-sam-headless-api/22-03-SUMMARY.md
@src/bioimage_mcp/registry/dynamic/adapters/microsam.py
@tools/microsam/bioimage_mcp_microsam/entrypoint.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive sam_annotator execution path in MicrosamAdapter</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </files>
  <action>
- Replace the Phase 22 hard denylist branch for `sam_annotator` with an interactive execution path.
- Implement a dedicated helper for interactive entrypoints (`annotator_2d`, `annotator_3d`, `annotator_tracking`, `image_series_annotator` when present):
  1) load `image` artifact (`BioImageRef`) with native dims preserved,
  2) optionally load `embedding_path` (`ObjectRef` or path-like artifact) and `segmentation_result` (`LabelImageRef`),
  3) force `return_viewer=True`, call upstream annotator function, then `napari.run()`,
  4) on viewer close, extract `committed_objects` layer data and write it as `LabelImageRef`.
- If `committed_objects` is missing or unchanged, return success with no output artifacts.
- Keep non-entrypoint `micro_sam.sam_annotator.*` callables explicitly denylisted with a stable rationale code (API-05 compliance).
  </action>
  <verify>
python -m compileall src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </verify>
  <done>
- Interactive entrypoints no longer raise the old "denylisted for headless execution" runtime error.
- Successful interactive close with committed labels produces a single `LabelImageRef`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add headless guard and stable interactive error/warning responses</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
tools/microsam/bioimage_mcp_microsam/entrypoint.py
  </files>
  <action>
- Add a pre-launch GUI availability check in adapter interactive flow (Linux: require `DISPLAY` or `WAYLAND_DISPLAY`; optionally validate Qt availability through `qtpy`).
- Raise a structured adapter exception for non-GUI environments with code `HEADLESS_DISPLAY_REQUIRED` and remediation hints (for example: run on a desktop session or use remote desktop/Xvfb).
- Update microsam entrypoint exception mapping to preserve stable `error.code`, `message`, and optional `details` (do not collapse everything into generic `EXECUTION_ERROR`).
- For recoverable interactive conditions, return `warnings` as machine-readable strings (for example `MICROSAM_NO_CHANGES` and `MICROSAM_EMBEDDING_RECOMPUTE`) while keeping `ok=true`.
- Keep existing device-selection behavior (`tool_config.microsam.device`) unchanged.
  </action>
  <verify>
python -m compileall tools/microsam/bioimage_mcp_microsam/entrypoint.py
  </verify>
  <done>
- Headless interactive invocation returns deterministic `HEADLESS_DISPLAY_REQUIRED` instead of random Qt crashes/hangs.
- Successful interactive calls can include warnings without failing the run.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for interactive adapter and entrypoint error mapping</name>
  <files>
tests/unit/registry/test_microsam_adapter_interactive.py
tests/unit/tools/test_microsam_entrypoint_interactive.py
  </files>
  <action>
- Create `tests/unit/registry/test_microsam_adapter_interactive.py` with monkeypatched `micro_sam.sam_annotator` and `napari` to verify:
  - entrypoint dispatch for 2D/3D/tracking annotators,
  - `committed_objects` export to `LabelImageRef`,
  - no-change close returns no outputs.
- Create `tests/unit/tools/test_microsam_entrypoint_interactive.py` to verify entrypoint response shaping:
  - `HEADLESS_DISPLAY_REQUIRED` is surfaced as `ok=false` with stable error code,
  - interactive success can include warnings while staying `ok=true`.
- Keep tests independent from an actual GUI display and independent from the microsam conda env.
  </action>
  <verify>
pytest tests/unit/registry/test_microsam_adapter_interactive.py tests/unit/tools/test_microsam_entrypoint_interactive.py -q
  </verify>
  <done>
- Interactive bridge behavior is covered by deterministic unit tests.
- Error/warning protocol contract is locked by tests before live smoke checks.
  </done>
</task>

</tasks>

<verification>
- ruff check src/bioimage_mcp/registry/dynamic/adapters/microsam.py tools/microsam/bioimage_mcp_microsam/entrypoint.py tests/unit/registry/test_microsam_adapter_interactive.py tests/unit/tools/test_microsam_entrypoint_interactive.py
- pytest tests/unit/registry/test_microsam_adapter_interactive.py tests/unit/tools/test_microsam_entrypoint_interactive.py -q
</verification>

<success_criteria>
- GUI-01/GUI-02/GUI-03 runtime bridge implemented in isolated worker.
- GUI-04 export path implemented for committed labels.
- INFRA-02 satisfied via stable headless error contract.
</success_criteria>

<output>
After completion, create `.planning/phases/23-microsam-interactive-bridge/23-02-SUMMARY.md`
</output>
