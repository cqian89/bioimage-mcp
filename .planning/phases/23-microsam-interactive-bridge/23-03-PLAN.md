---
phase: 23-microsam-interactive-bridge
plan: 03
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - tests/smoke/test_microsam_interactive_bridge_live.py
autonomous: false

must_haves:
  truths:
    - "Live MCP smoke checks confirm sam_annotator discovery/describe contracts and deterministic headless failure behavior"
    - "Human verification confirms annotator_2d, annotator_3d, and annotator_tracking open with preloaded artifacts"
    - "Closing a GUI session after committing labels returns a LabelImageRef in run outputs"
  artifacts:
    - path: tests/smoke/test_microsam_interactive_bridge_live.py
      provides: "End-to-end smoke coverage for interactive bridge list/describe/run behavior"
  key_links:
    - from: tests/smoke/test_microsam_interactive_bridge_live.py
      to: micro_sam.sam_annotator.annotator_2d
      via: "live_server.call_tool('run', ...)"
      pattern: "micro_sam\.sam_annotator\.annotator_2d"
---

<objective>
Validate Phase 23 behavior end-to-end and gate completion on explicit human GUI verification.

Purpose: Interactive annotation requires both automated protocol checks and a real display-session confirmation before phase sign-off.
Output: Smoke tests for discovery/describe/headless contracts plus a human verification checkpoint for 2D/3D/tracking GUI launches and output export.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/23-microsam-interactive-bridge/23-CONTEXT.md
@.planning/phases/23-microsam-interactive-bridge/23-RESEARCH.md
@tests/smoke/test_microsam_headless_live.py
@tests/smoke/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add live smoke tests for interactive bridge contracts</name>
  <files>
tests/smoke/test_microsam_interactive_bridge_live.py
  </files>
  <action>
- Create `tests/smoke/test_microsam_interactive_bridge_live.py` with `smoke_extended`, `requires_env("bioimage-mcp-microsam")`, and `anyio` markers.
- Add automated checks for:
  1) `list` contains `micro_sam.sam_annotator.annotator_2d`, `annotator_3d`, and `annotator_tracking`.
  2) `describe` returns object schema for annotator entrypoints and does not require ndarray JSON payloads.
  3) `run` in forced headless mode returns `HEADLESS_DISPLAY_REQUIRED` with actionable hint text.
- Keep smoke tests deterministic in CI/headless runners by explicitly validating error contract (not by trying to open GUI windows).
  </action>
  <verify>
pytest tests/smoke/test_microsam_interactive_bridge_live.py -m smoke_extended -q
  </verify>
  <done>
- Live smoke suite proves API-04, INFRA-02, and discovery/describe/run wiring for interactive paths.
- Tests skip cleanly when microsam env is unavailable.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive napari bridge for micro_sam annotator_2d/3d/tracking with output export on close</what-built>
  <how-to-verify>
1. Start server in a desktop session with display access.
2. Run `micro_sam.sam_annotator.annotator_2d` with a small 2D image `BioImageRef`; verify napari opens with the image pre-loaded.
3. Add/edit labels and close the viewer; verify run result returns a `LabelImageRef` output.
4. Repeat for `micro_sam.sam_annotator.annotator_3d` with a volumetric image and `annotator_tracking` with a time-series image.
5. Run one session with no edits and close; verify run succeeds with no output artifact and a `MICROSAM_NO_CHANGES` warning.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or report exact failing step and observed output.</resume-signal>
</task>

</tasks>

<verification>
- pytest tests/smoke/test_microsam_interactive_bridge_live.py -m smoke_extended -q
- Human verification checkpoint approved
</verification>

<success_criteria>
- GUI-01/GUI-02/GUI-03/GUI-04 verified through automated + human checks.
- Phase 23 can be marked complete with a reproducible verification trail.
</success_criteria>

<output>
After completion, create `.planning/phases/23-microsam-interactive-bridge/23-03-SUMMARY.md`
</output>
