---
phase: 06-infrastructure-n-d-foundation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/base/bioimage_mcp_base/entrypoint.py
  - src/bioimage_mcp/registry/loader.py
  - tests/unit/registry/test_loader_subprocess_rich_metadata.py
autonomous: true

must_haves:
  truths:
    - "Registry can discover dynamic functions via tool subprocess when server cannot import the library"
    - "Discovered scipy.ndimage functions include usable params_schema in describe output (not empty stubs)"
  artifacts:
    - path: tools/base/bioimage_mcp_base/entrypoint.py
      provides: "meta.list handler that returns rich FunctionMetadata for dynamic_sources"
    - path: src/bioimage_mcp/registry/loader.py
      provides: "subprocess discovery parses rich metadata into FunctionMetadata.parameters"
    - path: tests/unit/registry/test_loader_subprocess_rich_metadata.py
      provides: "regression test for rich subprocess discovery parsing"
  key_links:
    - from: src/bioimage_mcp/registry/loader.py
      to: tools/base/bioimage_mcp_base/entrypoint.py
      via: "execute_tool(fn_id=meta.list)"
      pattern: "fn_id\": \"meta\\.list\""
---

<objective>
Enable dynamic function discovery to run inside the tool environment (subprocess) while preserving rich parameter metadata for describe().

Purpose: Keep heavy deps (scipy) out of the server process while still delivering high-fidelity schemas.
Output: Tool-side meta.list + loader-side parsing + unit test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-n-d-foundation/06-CONTEXT.md

@tools/base/bioimage_mcp_base/entrypoint.py
@src/bioimage_mcp/registry/loader.py
</context>

<tasks>

<task type="auto">
  <name>Tool-Side meta.list: return rich FunctionMetadata for dynamic_sources</name>
  <files>tools/base/bioimage_mcp_base/entrypoint.py</files>
  <action>
Add a `meta.list` handler (mirroring `tools/trackpy/bioimage_mcp_trackpy/entrypoint.py`) that:
- Loads `tools/base/manifest.yaml` and validates it with `bioimage_mcp.registry.manifest_schema.ToolManifest`.
- Runs `bioimage_mcp.registry.dynamic.discovery.discover_functions(manifest, ADAPTER_REGISTRY)` inside the base env.
- Returns `{"ok": true, "result": {"functions": [...]}}` where each item includes:
  - `fn_id`, `name`, `module`, `summary` (first line of description), `io_pattern`
  - `description`, `parameters` (full), `returns`, `source_adapter` (or `introspection_source`)
- Keeps fn_ids un-prefixed (e.g. `scipy.ndimage.gaussian_filter`), letting the server apply `base.`.

Use `docstring-parser` (already installed in base env) to parse the docstrings into the rich `parameters` dictionary structure.

Do NOT change the existing dynamic dispatch path; this is discovery-only.
  </action>
  <verify>python -m compileall tools/base/bioimage_mcp_base/entrypoint.py</verify>
  <done>`meta.list` executes successfully in the base env and returns a JSON payload containing function parameter metadata.</done>
</task>

<task type="auto">
  <name>Loader: parse rich subprocess discovery metadata into params_schema</name>
  <files>src/bioimage_mcp/registry/loader.py</files>
  <action>
Update `_discover_via_subprocess()` to support two payload shapes from `meta.list`:
- Legacy/minimal entries (current behavior): `{fn_id, name, module, summary, io_pattern}`.
- Rich entries: include `description`, `parameters` (dict of ParameterSchema-shaped dicts), and optional `returns`.

When rich fields are present:
- Build `FunctionMetadata` including `.parameters` and `.returns`.
- Preserve `io_pattern` if provided.
- Set `source_adapter` to something stable (e.g. `subprocess:numpydoc`) using the tool-provided `source_adapter` / `introspection_source` when present.

Keep the existing fallback behavior and error handling.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/loader.py</verify>
  <done>Subprocess discovery produces Functions with non-empty `params_schema.properties` when rich metadata is returned.</done>
</task>

<task type="auto">
  <name>Unit test: rich subprocess discovery populates params_schema</name>
  <files>tests/unit/registry/test_loader_subprocess_rich_metadata.py</files>
  <action>
Create a unit test that patches `bioimage_mcp.registry.loader.execute_tool` to return a successful `meta.list` response with ONE rich function entry, including a parameter like `sigma`.

Assertions:
- `load_manifest_file()` loads the manifest and includes the discovered function.
- The discovered function has `params_schema["properties"]["sigma"]` present.
- The discovered function's `introspection_source` reflects subprocess origin.

Keep the test hermetic: no conda, no scipy import, no filesystem beyond tmp_path.
  </action>
  <verify>pytest tests/unit/registry/test_loader_subprocess_rich_metadata.py -q</verify>
  <done>Test passes and fails if loader drops parameters from subprocess discovery.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/test_loader_subprocess_rich_metadata.py -q`
</verification>

<success_criteria>
- Server-side manifest loading can ingest rich dynamic discovery results from tool subprocesses.
- Dynamic functions discovered via subprocess have usable `params_schema` (not empty stubs).
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-n-d-foundation/06-01-SUMMARY.md`
</output>
