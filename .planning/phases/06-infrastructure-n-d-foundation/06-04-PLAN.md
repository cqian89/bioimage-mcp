---
phase: 06-infrastructure-n-d-foundation
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
  - tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "scipy.ndimage output OME-TIFF files retain physical pixel size metadata (microns/ms) from the input"
    - "Extracting metadata from the written output file yields non-empty OME-XML with PhysicalSizeX/PhysicalSizeY when provided on input"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      provides: "OME-TIFF writing that forwards physical_pixel_sizes/channel_names into OmeTiffWriter"
    - path: tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
      provides: "Regression test asserting output file metadata retains physical_pixel_sizes"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      to: bioio.writers.OmeTiffWriter.save
      via: "physical_pixel_sizes + channel_names arguments"
      pattern: "OmeTiffWriter\\.save\\(.*physical_pixel_sizes=.*channel_names="
---

<objective>
Close UAT gap: ensure scipy.ndimage output OME-TIFFs preserve physical pixel size metadata in the file’s OME-XML (not just in the artifact metadata dict).

Purpose: Downstream coordinate-aware operations (Phase 7+) require physical pixel sizes to survive round-trips through written artifacts.
Output: Updated scipy_ndimage OME-TIFF writer call + regression test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-n-d-foundation/06-UAT.md
@.planning/debug/scipy-metadata-loss.md

@src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
@src/bioimage_mcp/artifacts/metadata.py
@tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
</context>

<tasks>

<task type="auto">
  <name>Forward physical_pixel_sizes + channel_names into OmeTiffWriter.save</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</files>
  <action>
In `ScipyNdimageAdapter._save_image`, when writing via `bioio.writers.OmeTiffWriter.save`, pass through rich metadata into the *file*:

- If `metadata_override` contains `physical_pixel_sizes`, convert it to a `bioio_base.types.PhysicalPixelSizes` instance and pass it as `physical_pixel_sizes=...`.
  - Accept both shapes:
    - dict form: {"X": float, "Y": float, "Z": float?}
    - list form (legacy/test): [Y, X] or [Z, Y, X] — interpret as Y/X (and optional Z) in that order.
  - Only pass keys that are present and non-null.

- If `metadata_override` contains `channel_names` (list[str]), pass it as `channel_names=...`.

- Keep existing `dim_order=inferred_axes` behavior.

Constraints:
- Do not add new dependencies.
- Keep metadata values JSON-safe in the returned output dict metadata (still include `physical_pixel_sizes` and `channel_names` in the returned `metadata` map as today).
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</verify>
  <done>`OmeTiffWriter.save(...)` is called with `physical_pixel_sizes=` (when provided) and `channel_names=` (when provided).</done>
</task>

<task type="auto">
  <name>Add regression test: output file metadata round-trip preserves physical pixel sizes</name>
  <files>tests/unit/registry/dynamic/test_scipy_ndimage_execute.py</files>
  <action>
Add a new unit test that specifically validates *file metadata* preservation (UAT gap):

- Execute the adapter (using the existing mock module) with input metadata that includes:
  - `axes: "YX"`
  - `physical_pixel_sizes: {"Y": 1.0, "X": 2.0}` (use distinct values to avoid swapped-axis false positives)
  - `channel_names: ["Ch1"]`

- After execution, call `bioimage_mcp.artifacts.metadata.extract_image_metadata(Path(out["path"]))` and assert:
  - returned metadata contains `physical_pixel_sizes` with `Y == 1.0` and `X == 2.0`
  - returned metadata contains `channel_names` including "Ch1" (if present in writer output; if not reliably supported, keep this assertion best-effort but ensure physical_pixel_sizes is strict)

Notes:
- This test must fail on the pre-fix behavior (no physical sizes written into OME-XML) and pass after the fix.
  </action>
  <verify>pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -q</verify>
  <done>Test passes and proves physical pixel sizes survive in the written OME-TIFF file metadata.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -q`
</verification>

<success_criteria>
- UAT Truth #5 is now achievable: a scipy.ndimage output OME-TIFF retains physical pixel sizes when inspected via file metadata extraction.
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-n-d-foundation/06-04-SUMMARY.md`.
</output>
