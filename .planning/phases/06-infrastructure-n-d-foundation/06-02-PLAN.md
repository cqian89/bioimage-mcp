---
phase: 06-infrastructure-n-d-foundation
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/manifest_schema.py
  - src/bioimage_mcp/registry/dynamic/discovery.py
  - tools/base/manifest.yaml
  - tools/base/scipy_ndimage_blacklist.yaml
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
  - tests/unit/registry/dynamic/test_scipy_ndimage_discovery_config.py
autonomous: true

must_haves:
  truths:
    - "scipy.ndimage dynamic discovery respects an external blacklist config and ignores stale entries"
    - "Deprecated ndimage functions are excluded from discovery"
    - "Aliases remain exposed (no deduping), matching scipy module surface"
  artifacts:
    - path: tools/base/scipy_ndimage_blacklist.yaml
      provides: "External blacklist configuration for ndimage discovery"
    - path: tools/base/manifest.yaml
      provides: "References blacklist config via dynamic_sources"
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      provides: "Discovery filters (blacklist + deprecated)"
  key_links:
    - from: tools/base/manifest.yaml
      to: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      via: "dynamic_sources config passed into adapter.discover()"
      pattern: "adapter: scipy"
---

<objective>
Harden scipy.ndimage discovery with a config-driven blacklist and deprecated filtering, while passing manifest context into adapters for path resolution.

Purpose: Make discovery stable and configurable without code edits.
Output: Manifest schema support + config file + adapter filtering + unit test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-n-d-foundation/06-CONTEXT.md

@src/bioimage_mcp/registry/manifest_schema.py
@src/bioimage_mcp/registry/dynamic/discovery.py
@src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
@tools/base/manifest.yaml
</context>

<tasks>

<task type="auto">
  <name>Manifest schema: add blacklist_path to DynamicSource</name>
  <files>src/bioimage_mcp/registry/manifest_schema.py</files>
  <action>
Extend `DynamicSource` with an optional `blacklist_path: str | None` field.

Constraints:
- Keep backward compatibility: manifests without the field must still validate.
- No behavior changes outside allowing the field to exist and be dumped.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/manifest_schema.py</verify>
  <done>`ToolManifest.model_validate()` accepts a `dynamic_sources.*.blacklist_path` key.</done>
</task>

<task type="auto">
  <name>Discovery context: pass manifest_path into adapter.discover config</name>
  <files>src/bioimage_mcp/registry/dynamic/discovery.py</files>
  <action>
When building `source_config = source.model_dump()`, inject a stable manifest context key:
- `source_config["_manifest_path"] = str(manifest.manifest_path)`

Rationale: lets adapters resolve relative config files (blacklists) without global state.

Do NOT change cache keys or behavior besides passing additional config keys to adapters.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/dynamic/discovery.py</verify>
  <done>Adapters receive `_manifest_path` and can resolve config paths relative to it.</done>
</task>

<task type="auto">
  <name>Scipy ndimage discovery: blacklist + deprecated filtering + unit test</name>
  <files>tools/base/manifest.yaml, tools/base/scipy_ndimage_blacklist.yaml, src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py, tests/unit/registry/dynamic/test_scipy_ndimage_discovery_config.py</files>
  <action>
1) Add `tools/base/scipy_ndimage_blacklist.yaml` with a simple structure:
   - top-level `blacklist:` list of function names (strings)
   - keep it safe-by-default (may be empty), and include at least one illustrative entry + one intentionally-stale entry.

2) Update `tools/base/manifest.yaml` scipy dynamic source to set `blacklist_path: scipy_ndimage_blacklist.yaml`.

3) Update `ScipyNdimageAdapter.discover()` to:
   - If `_manifest_path` + `blacklist_path` are provided, resolve the blacklist file relative to the manifest directory.
   - Load YAML (use PyYAML already in stack) and treat missing/invalid files as "no blacklist".
   - Ignore blacklist entries that don't exist (no crash; log debug/warn).
   - Skip functions that appear deprecated (best-effort: check `inspect.getdoc(obj)` for a leading "Deprecated" line or `.. deprecated::`).
   - Keep existing alias behavior (dir-based iteration) and do NOT dedupe.

4) Add a hermetic unit test that creates a temporary importable module (written to tmp_path and added to sys.path) containing:
   - one normal function
   - one function whose docstring marks it deprecated
   - one function whose name is in the blacklist
   - verify adapter.discover filters blacklist+deprecated while keeping the normal function.

No SciPy import in the test.
  </action>
  <verify>pytest tests/unit/registry/dynamic/test_scipy_ndimage_discovery_config.py -q</verify>
  <done>Discovery supports an external blacklist file, skips deprecated functions, and ignores stale blacklist entries without crashing.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/dynamic/test_scipy_ndimage_discovery_config.py -q`
</verification>

<success_criteria>
- Base manifest can supply a blacklist file path to the scipy adapter.
- scipy.ndimage discovery can be tuned via config without code changes.
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-n-d-foundation/06-02-SUMMARY.md`
</output>
