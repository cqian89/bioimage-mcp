---
phase: 02-tool-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/cli.py
  - src/bioimage_mcp/bootstrap/list.py
  - tests/unit/bootstrap/test_list_output.py
autonomous: true

must_haves:
  truths:
    - "User can run `bioimage-mcp list` and see installed tools in table format"
    - "User can run `bioimage-mcp list --json` and get machine-readable output"
    - "Output shows tool name, status, and function count"
  artifacts:
    - path: "src/bioimage_mcp/bootstrap/list.py"
      provides: "CLI list command implementation"
      exports: ["list_tools"]
    - path: "tests/unit/bootstrap/test_list_output.py"
      provides: "Unit tests for list output formatting"
  key_links:
    - from: "src/bioimage_mcp/cli.py"
      to: "src/bioimage_mcp/bootstrap/list.py"
      via: "subparser handler"
      pattern: "_handle_list"
    - from: "src/bioimage_mcp/bootstrap/list.py"
      to: "src/bioimage_mcp/api/discovery.py"
      via: "DiscoveryService.list_tools"
      pattern: "DiscoveryService.*list_tools"
---

<objective>
Implement the `list` CLI command to expose installed tools and their status.

Purpose: Fulfills TOOL-02 requirement. Users need visibility into what tools are installed before using them.
Output: Working `bioimage-mcp list` command with table and JSON output modes.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-management/02-CONTEXT.md

Existing patterns:
@src/bioimage_mcp/cli.py (CLI structure with argparse subparsers)
@src/bioimage_mcp/bootstrap/doctor.py (module structure pattern)
@src/bioimage_mcp/api/discovery.py (DiscoveryService.list_tools already exists)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create list.py bootstrap module</name>
  <files>src/bioimage_mcp/bootstrap/list.py</files>
  <action>
Create new module following doctor.py pattern:

1. Import DiscoveryService from api/discovery.py
2. Create `list_tools(*, json_output: bool) -> int` function
3. For table output:
   - Print header row: Tool | Status | Functions
   - Query DiscoveryService.list_tools() for available tools
   - For each tool, show: tool_id, status (✓ installed / ⚠ partial / ✗ missing env), function count
   - Use simple column alignment (no external table library needed)
4. For JSON output:
   - Return structured JSON with tools array
   - Each tool: {id, status, function_count, env_id}

Status detection logic:
- Check if env_id exists via env_manager.detect_env_manager() + subprocess check
- "installed" if env exists and manifest valid
- "partial" if manifest exists but env missing
- "unavailable" if neither

Keep it simple - no spinners or colors in this first pass.
  </action>
  <verify>
`python -c "from bioimage_mcp.bootstrap.list import list_tools; print('import ok')"`
  </verify>
  <done>list.py module exists, exports list_tools function, follows doctor.py pattern</done>
</task>

<task type="auto">
  <name>Task 2: Wire list command to CLI and add tests</name>
  <files>src/bioimage_mcp/cli.py, tests/unit/bootstrap/test_list_output.py</files>
  <action>
In cli.py:
1. Add subparser for "list" command after "install"
2. Add --json flag (same pattern as doctor)
3. Create _handle_list(args) handler calling list.list_tools(json_output=args.json)

In tests/unit/bootstrap/test_list_output.py:
1. Create test file following test_doctor_output.py pattern
2. Test table output format (mock DiscoveryService)
3. Test JSON output structure
4. Test empty tools case
5. Test mixed status (installed, partial, unavailable)

Use pytest with mocking - don't require real conda envs for unit tests.
  </action>
  <verify>
`pytest tests/unit/bootstrap/test_list_output.py -v` passes
`python -m bioimage_mcp.cli list --help` shows usage
  </verify>
  <done>
- `bioimage-mcp list` shows table with tool status
- `bioimage-mcp list --json` returns valid JSON
- Unit tests pass
  </done>
</task>

</tasks>

<verification>
```bash
# Command works
bioimage-mcp list
bioimage-mcp list --json

# Tests pass
pytest tests/unit/bootstrap/test_list_output.py -v

# Linting passes
ruff check src/bioimage_mcp/bootstrap/list.py
```
</verification>

<success_criteria>
- [ ] `bioimage-mcp list` displays tools in readable table
- [ ] `bioimage-mcp list --json` returns machine-readable output
- [ ] Output includes tool ID, status, function count
- [ ] Unit tests cover table/JSON formats and edge cases
- [ ] Code follows existing conventions (doctor.py pattern)
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-management/02-01-SUMMARY.md`
</output>
