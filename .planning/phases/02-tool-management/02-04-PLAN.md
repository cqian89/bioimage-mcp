---
phase: 02-tool-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/bootstrap/list.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "List command shows installed tool packs when manifests exist on disk"
    - "List --json outputs tool information matching what doctor sees"
  artifacts:
    - path: "src/bioimage_mcp/bootstrap/list.py"
      provides: "Filesystem-based tool listing"
      contains: "load_manifests"
  key_links:
    - from: "src/bioimage_mcp/bootstrap/list.py"
      to: "bioimage_mcp.registry.loader"
      via: "import load_manifests"
      pattern: "from bioimage_mcp\\.registry\\.loader import load_manifests"
---

<objective>
Fix `list` command to use filesystem-based manifest discovery instead of SQLite database.

Purpose: The `list` command currently reads from a SQLite database that only gets populated when the server starts. This causes "No tools registered" output even when `doctor` correctly shows "3 tools, 820 functions" by reading manifests directly from disk. Both commands should use the same data source.

Output: A working `list` command that shows tools consistently with `doctor`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tool-management/02-UAT.md

# Reference implementation showing correct approach
@src/bioimage_mcp/bootstrap/doctor.py

# File to modify
@src/bioimage_mcp/bootstrap/list.py

# Provides load_manifests function
@src/bioimage_mcp/registry/loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor list.py to use filesystem manifest discovery</name>
  <files>src/bioimage_mcp/bootstrap/list.py</files>
  <action>
Modify `list_tools()` in `list.py` to use `load_manifests()` from `bioimage_mcp.registry.loader` instead of reading from SQLite database.

**Changes required:**

1. Add import at top:
   ```python
   from bioimage_mcp.registry.loader import load_manifests
   ```

2. Replace the SQLite-based logic (lines 39-119) with filesystem-based approach:
   - Call `load_manifests(config.tool_manifest_roots)` to get manifests
   - For each manifest, extract: tool_id, env_id, function count
   - Determine status by checking if conda env exists (keep existing `_get_installed_envs()` logic)

3. Keep the existing output format (table and JSON) but populate from manifests instead of database.

**Key reference:** See `doctor.py` lines 14-38 for how to correctly use `load_manifests()`.

**Do NOT:**
- Remove the `_get_installed_envs()` helper - it's still needed for status detection
- Change the output format - keep table columns and JSON structure identical
- Add any new dependencies beyond what's already in the codebase
  </action>
  <verify>
Run: `python -c "from bioimage_mcp.bootstrap.list import list_tools; print('import ok')"`
Verify no import errors occur.
  </verify>
  <done>
`list.py` imports and uses `load_manifests()` instead of SQLite database.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify list command matches doctor output</name>
  <files>src/bioimage_mcp/bootstrap/list.py</files>
  <action>
Run functional verification to confirm `list` and `doctor` now report consistent tool counts.

1. Run `bioimage-mcp doctor` and capture tool count
2. Run `bioimage-mcp list` and verify it shows the same tools
3. Run `bioimage-mcp list --json` and verify JSON output contains tools

If any discrepancies found, fix the implementation in `list.py`.
  </action>
  <verify>
Run: `bioimage-mcp doctor 2>/dev/null | grep -o "[0-9]* tools" | head -1`
Run: `bioimage-mcp list 2>/dev/null | grep -c "^[a-z]"` (count non-header lines)
Run: `bioimage-mcp list --json 2>/dev/null | python -c "import sys,json; d=json.load(sys.stdin); print(len(d.get('tools',[])))"`

All three commands should report the same tool count (3 based on UAT).
  </verify>
  <done>
- `bioimage-mcp list` shows tools with status and function counts
- `bioimage-mcp list --json` returns non-empty tools array
- Tool count matches `doctor` output
  </done>
</task>

</tasks>

<verification>
All UAT gaps addressed:
- Gap 1: "List command shows installed tool packs with status and function counts" - FIXED
- Gap 2: "List --json outputs tool information" - FIXED (same root cause)

Commands to verify:
```bash
bioimage-mcp doctor
bioimage-mcp list
bioimage-mcp list --json
```

Expected: All three show consistent tool information (3 tools per UAT context).
</verification>

<success_criteria>
1. `bioimage-mcp list` shows table with tools, status, and function counts
2. `bioimage-mcp list --json` returns `{"tools": [...]}` with tool entries
3. Tool count matches `doctor` output
4. No regression: existing functionality (status detection via conda env check) preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-tool-management/02-04-SUMMARY.md`
</output>
