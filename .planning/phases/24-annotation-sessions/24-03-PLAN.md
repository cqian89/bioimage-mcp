---
phase: 24-annotation-sessions
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  - tools/microsam/bioimage_mcp_microsam/entrypoint.py
  - tests/unit/tools/test_microsam_entrypoint_interactive.py
  - tests/smoke/test_microsam_headless_live.py
  - tests/smoke/test_microsam_interactive_bridge_live.py
autonomous: false

must_haves:
  truths:
    - "After closing an interactive annotator, reopening on the same image/model resumes from compatible cached state instead of cold-starting"
    - "Resume behavior survives viewer close/reopen within the same MCP session without losing previously committed labels"
    - "Users can observe progress stages for model loading and embedding computation in run logs/warnings"
    - "End-to-end checks prove both resume and progress behavior in headless-safe automation plus desktop verification"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      provides: "Interactive warm-start/resume wiring and stage-level progress emission"
    - path: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      provides: "Execute response propagation of cache/progress warnings metadata"
    - path: tests/smoke/test_microsam_headless_live.py
      provides: "Automated checks for cache status and progress signal visibility"
    - path: tests/smoke/test_microsam_interactive_bridge_live.py
      provides: "Interactive contract checks for resume behavior"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      to: micro_sam.sam_annotator.annotator_2d
      via: "interactive call injects compatible predictor/embedding for warm-start"
      pattern: "embedding_path|predictor"
    - from: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      to: tools/microsam/bioimage_mcp_microsam/entrypoint.py
      via: "adapter warnings/progress forwarded to execute_result"
      pattern: "warnings"
    - from: tests/smoke/test_microsam_headless_live.py
      to: micro_sam.compute_embedding
      via: "repeated run asserts hit/miss and progress markers"
      pattern: "compute_embedding"
---

<objective>
Complete session optimization UX by enabling interactive resume and visible progress signals.

Purpose: SESS-03 and SESS-04 are user-facing outcomes that depend on cache and lifecycle foundations from 24-01/24-02.
Output: Warm-start interactive behavior, stage-level progress visibility, and final smoke + human verification evidence for phase sign-off.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-annotation-sessions/24-CONTEXT.md
@.planning/phases/24-annotation-sessions/24-RESEARCH.md
@.planning/phases/24-annotation-sessions/24-01-SUMMARY.md
@.planning/phases/24-annotation-sessions/24-02-SUMMARY.md
@.planning/phases/23-microsam-interactive-bridge/23-03-SUMMARY.md
@src/bioimage_mcp/registry/dynamic/adapters/microsam.py
@tools/microsam/bioimage_mcp_microsam/entrypoint.py
@tests/smoke/test_microsam_headless_live.py
@tests/smoke/test_microsam_interactive_bridge_live.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interactive warm-start/resume behavior using compatible cached state</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
tools/microsam/bioimage_mcp_microsam/entrypoint.py
tests/unit/tools/test_microsam_entrypoint_interactive.py
  </files>
  <action>
- In interactive execution, resolve the same `image + model` cache key from 24-01 and inject compatible predictor/embedding inputs into annotator entrypoints when available.
- On incompatible cache, emit reset warning and continue with recompute; do not silently reuse mismatched state.
- Preserve resume semantics for previously committed labels by honoring `segmentation_result` as initial state and keeping export contract unchanged (`LabelImageRef` on close with changes).
- Update entrypoint-level tests so cache-hit resume and reset behavior is contract-tested in `execute_result` warnings.
  </action>
  <verify>
pytest tests/unit/tools/test_microsam_entrypoint_interactive.py -q
  </verify>
  <done>
- Reopening annotator sessions on compatible inputs can warm-start from cached state.
- Resume path remains artifact-safe and deterministic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit model/embedding progress indicators and verify via smoke tests</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
tools/microsam/bioimage_mcp_microsam/entrypoint.py
tests/smoke/test_microsam_headless_live.py
tests/smoke/test_microsam_interactive_bridge_live.py
  </files>
  <action>
- Add stage-level progress signaling for at least these phases: model load start/done and embedding compute start/done.
- Surface progress through existing response channels (warnings/log payload) rather than adding new IPC protocol messages in this phase.
- Extend smoke tests to assert cache status + progress markers are visible on repeated calls and interactive setup paths.
- Keep tests deterministic in CI/headless environments by checking response metadata/log content, not GUI timing.
  </action>
  <verify>
pytest tests/smoke/test_microsam_headless_live.py tests/smoke/test_microsam_interactive_bridge_live.py -m smoke_extended -q
  </verify>
  <done>
- SESS-04 is met with observable run-time progress indicators.
- Automated smoke checks validate both progress and cache visibility end to end.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Interactive resume workflow with warm-start cache reuse and progress/cache status feedback</what-built>
  <how-to-verify>
1. Start MCP server in a desktop session with microsam env available.
2. Run `micro_sam.sam_annotator.annotator_2d` on a test image, create labels, and close viewer; confirm one `LabelImageRef` output.
3. Run the same annotator on the same image/model again in the same session; verify warm-start behavior (faster launch and cache-hit/resume warning present).
4. Confirm previous labels can be loaded as `segmentation_result` and edited without losing prior work.
5. Trigger one forced fresh run (`force_fresh=true`) and verify reset status is reported.
6. Inspect run logs/warnings for stage markers (model load + embedding compute) and confirm they are present.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or report the failing step and observed warning/error payload.</resume-signal>
</task>

</tasks>

<verification>
- pytest tests/unit/tools/test_microsam_entrypoint_interactive.py -q
- pytest tests/smoke/test_microsam_headless_live.py tests/smoke/test_microsam_interactive_bridge_live.py -m smoke_extended -q
- Human verification checkpoint approved
</verification>

<success_criteria>
- SESS-03 is satisfied: interactive close/reopen resumes from compatible cached state without losing progress.
- SESS-04 is satisfied: progress indicators for model loading and embedding computation are visible in run feedback.
- Phase 24 has reproducible automated + human verification evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/24-annotation-sessions/24-03-SUMMARY.md`
</output>
