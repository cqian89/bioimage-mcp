---
phase: 24-annotation-sessions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/runtimes/persistent.py
  - tests/unit/runtimes/test_persistent_failure.py
  - tests/unit/runtimes/test_persistent_shutdown.py
autonomous: true

must_haves:
  truths:
    - "If the MCP server shuts down while a napari run is still open, the worker process is terminated automatically"
    - "Server shutdown does not hang indefinitely on BUSY interactive workers"
    - "Terminated workers are removed from manager state so stale processes do not leak into next runs"
  artifacts:
    - path: src/bioimage_mcp/runtimes/persistent.py
      provides: "Busy-worker shutdown fallback that force-kills blocked workers and marks them terminated"
    - path: tests/unit/runtimes/test_persistent_failure.py
      provides: "Failure-path coverage for forced termination when IPC response cannot be read"
    - path: tests/unit/runtimes/test_persistent_shutdown.py
      provides: "Direct shutdown coverage for server-termination and manager shutdown_all behavior"
  key_links:
    - from: src/bioimage_mcp/api/execution.py
      to: src/bioimage_mcp/runtimes/persistent.py
      via: "ExecutionService.close() -> PersistentWorkerManager.shutdown_all()"
      pattern: "shutdown_all\(\)"
    - from: src/bioimage_mcp/runtimes/persistent.py
      to: WorkerProcess
      via: "busy timeout path escalates to process kill"
      pattern: "_process\.kill\(\)"
---

<objective>
Guarantee cleanup of interactive microsam subprocesses when the MCP server terminates.

Purpose: SESS-02 is a lifecycle safety requirement; without deterministic teardown, interactive workers can leak and block future sessions.
Output: Robust worker shutdown behavior for BUSY napari sessions plus regression tests that prove forced termination paths.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-annotation-sessions/24-CONTEXT.md
@.planning/phases/24-annotation-sessions/24-RESEARCH.md
@.planning/phases/23-microsam-interactive-bridge/23-03-SUMMARY.md
@src/bioimage_mcp/bootstrap/serve.py
@src/bioimage_mcp/api/execution.py
@src/bioimage_mcp/runtimes/persistent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden worker shutdown path for blocked interactive sessions</name>
  <files>
src/bioimage_mcp/runtimes/persistent.py
  </files>
  <action>
- Update `WorkerProcess.shutdown()` to treat BUSY timeout as a terminal condition for interactive workers: if graceful shutdown cannot proceed, force-kill without waiting for additional IPC responses.
- Ensure shutdown always converges to `WorkerState.TERMINATED` and avoids deadlocks when stdout/stderr pipes are blocked by GUI runtime behavior.
- Preserve diagnostics by logging whether shutdown ended gracefully or via forced kill so operators can distinguish normal close from forced teardown.
- Keep behavior safe for non-interactive workers (graceful path first), but do not allow indefinite waiting in either path.
  </action>
  <verify>
python -m compileall src/bioimage_mcp/runtimes/persistent.py
  </verify>
  <done>
- Worker shutdown cannot hang forever on BUSY interactive runs.
- Forced termination path is explicit and deterministic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add runtime regression tests for termination and manager cleanup</name>
  <files>
tests/unit/runtimes/test_persistent_failure.py
tests/unit/runtimes/test_persistent_shutdown.py
  </files>
  <action>
- Extend runtime tests to simulate a worker that remains BUSY and cannot acknowledge shutdown, then assert `kill()` is called and state is terminal.
- Add manager-level test covering `PersistentWorkerManager.shutdown_all()` to confirm all registered workers are shut down and registry is cleared even when one worker forces kill.
- Keep tests mock-driven and deterministic (no real subprocess hangs) so they run in the default unit suite.
  </action>
  <verify>
pytest tests/unit/runtimes/test_persistent_failure.py tests/unit/runtimes/test_persistent_shutdown.py -q
  </verify>
  <done>
- Lifecycle cleanup behavior is enforced by unit tests instead of manual inspection.
- SESS-02 has direct automated evidence.
  </done>
</task>

</tasks>

<verification>
- ruff check src/bioimage_mcp/runtimes/persistent.py tests/unit/runtimes/test_persistent_failure.py tests/unit/runtimes/test_persistent_shutdown.py
- pytest tests/unit/runtimes/test_persistent_failure.py tests/unit/runtimes/test_persistent_shutdown.py -q
</verification>

<success_criteria>
- SESS-02 is satisfied: server termination path automatically cleans up interactive subprocesses.
- No regression to existing persistent worker behavior for normal (non-interactive) workloads.
</success_criteria>

<output>
After completion, create `.planning/phases/24-annotation-sessions/24-02-SUMMARY.md`
</output>
