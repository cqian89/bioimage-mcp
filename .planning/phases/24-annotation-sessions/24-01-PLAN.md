---
phase: 24-annotation-sessions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  - tools/microsam/manifest.yaml
  - tests/unit/registry/test_microsam_adapter_session_cache.py
autonomous: true

must_haves:
  truths:
    - "Repeated micro_sam calls on the same image/model reuse predictor or embedding state instead of recomputing every time"
    - "Cache hit/miss/reset status is visible to users in run warnings/logs"
    - "Per-run force-fresh bypass exists and returns miss/reset status even when a cached entry is available"
    - "Clearing session cache removes reusable predictor/embedding entries for subsequent calls"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      provides: "Session-scoped cache keying, compatibility checks, and cache status emission for predictor/embedding reuse"
    - path: tools/microsam/manifest.yaml
      provides: "Public schema for cache controls (force_fresh and explicit cache clear)"
    - path: tests/unit/registry/test_microsam_adapter_session_cache.py
      provides: "Deterministic coverage for cache hit/miss/reset and clear behavior"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      to: src/bioimage_mcp/registry/dynamic/object_cache.py
      via: "image+model cache key lookup and object reuse"
      pattern: "OBJECT_CACHE\.(get|set|clear)"
    - from: src/bioimage_mcp/registry/dynamic/adapters/microsam.py
      to: tools/microsam/manifest.yaml
      via: "runtime params expose force_fresh control"
      pattern: "force_fresh"
---

<objective>
Implement the phase-24 cache contract for heavy micro_sam predictors and embeddings.

Purpose: SESS-01 depends on deterministic, session-scoped cache behavior before resume/progress work can be layered on top.
Output: `image + model` keyed predictor/embedding reuse, force-fresh bypass, and explicit cache-clear behavior with visible status signals.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-annotation-sessions/24-CONTEXT.md
@.planning/phases/24-annotation-sessions/24-RESEARCH.md
@.planning/phases/22-mu-sam-headless-api/22-03-SUMMARY.md
@.planning/phases/23-microsam-interactive-bridge/23-02-SUMMARY.md
@src/bioimage_mcp/registry/dynamic/adapters/microsam.py
@tools/microsam/manifest.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic session cache keying and invalidation rules</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </files>
  <action>
- Add explicit helpers in `MicrosamAdapter` to build and validate cache keys using normalized image identity + model (`image + model` contract from phase context).
- Record cache entries as session-scoped metadata (including model, image identity, and freshness marker) so later calls can detect compatibility without guesswork.
- Implement invalidation behavior from context: model/image mismatch invalidates, incompatible/corrupt entries are purged immediately, and corruption triggers structured warning metadata before recompute.
- Keep "most recent wins" for duplicate compatible entries; avoid introducing cross-session persistence by default to prevent unexpected disk growth.
  </action>
  <verify>
python -m compileall src/bioimage_mcp/registry/dynamic/adapters/microsam.py
  </verify>
  <done>
- Adapter can deterministically decide hit/miss/reset for the same image/model across repeated calls in one MCP session.
- Incompatible or unreadable cache entries are not reused silently.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire cache controls into compute and segmentation execution paths</name>
  <files>
src/bioimage_mcp/registry/dynamic/adapters/microsam.py
tools/microsam/manifest.yaml
  </files>
  <action>
- Update `micro_sam.compute_embedding` and compatible segmentation execution paths to consult the new cache key before loading models or recomputing embeddings.
- Add per-run `force_fresh` handling that bypasses reuse, recomputes state, and emits reset/miss status explicitly.
- Keep `micro_sam.cache.clear` as the explicit operator action to wipe session cache state; ensure it clears both object entries and any adapter-owned cache index metadata.
- Expose cache-control params in `tools/microsam/manifest.yaml` so describe/run contracts are explicit and not hidden implementation knobs.
- Emit minimal, machine-readable cache status warnings (`MICROSAM_CACHE_HIT`, `MICROSAM_CACHE_MISS`, `MICROSAM_CACHE_RESET`) for every relevant call.
  </action>
  <verify>
python -m compileall src/bioimage_mcp/registry/dynamic/adapters/microsam.py
python -c "import pathlib, yaml; yaml.safe_load(pathlib.Path('tools/microsam/manifest.yaml').read_text())"
  </verify>
  <done>
- Repeated same-image same-model calls can return hit instead of recompute.
- `force_fresh=true` bypasses reuse and still leaves cache in a valid post-run state.
- `micro_sam.cache.clear` guarantees the next run is a miss.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add unit coverage for hit/miss/reset and clear semantics</name>
  <files>
tests/unit/registry/test_microsam_adapter_session_cache.py
  </files>
  <action>
- Add a dedicated unit test module that mocks micro_sam model loading and validates cache behavior without requiring GPU or napari.
- Cover at least: first-call miss, second-call hit, force-fresh reset, explicit clear, and corrupted cache entry fallback with warning.
- Assert warning/status emissions are stable and machine-readable so later smoke checks can rely on them.
  </action>
  <verify>
pytest tests/unit/registry/test_microsam_adapter_session_cache.py -q
  </verify>
  <done>
- Cache contract behavior is regression-protected by deterministic unit tests.
- SESS-01 has direct, executable evidence before interactive verification work.
  </done>
</task>

</tasks>

<verification>
- ruff check src/bioimage_mcp/registry/dynamic/adapters/microsam.py tests/unit/registry/test_microsam_adapter_session_cache.py
- pytest tests/unit/registry/test_microsam_adapter_session_cache.py -q
</verification>

<success_criteria>
- SESS-01 is satisfied at the adapter layer: cache hit/miss/reset is deterministic and user-visible.
- Cache controls are surfaced through manifest/describe contracts, not hidden behind undocumented params.
</success_criteria>

<output>
After completion, create `.planning/phases/24-annotation-sessions/24-01-SUMMARY.md`
</output>
