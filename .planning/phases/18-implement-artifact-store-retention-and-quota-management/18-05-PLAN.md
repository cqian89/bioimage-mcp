---
phase: 18-implement-artifact-store-retention-and-quota-management
plan: 5
type: execute
wave: 5
depends_on:
  - 18-04
files_modified:
  - tests/unit/storage/test_retention_quota.py
  - tests/unit/storage/test_cleanup_engine.py
  - tests/integration/test_cli_storage_cleanup.py
autonomous: true

must_haves:
  truths:
    - "Retention and quota cleanup behavior is covered by automated tests"
    - "Dry-run never deletes artifacts or DB rows"
    - "CLI commands expose storage status and cleanup summaries with stable JSON"
  artifacts:
    - path: tests/unit/storage/test_retention_quota.py
      provides: "Unit coverage for StorageManager candidate selection + invariants"
    - path: tests/unit/storage/test_cleanup_engine.py
      provides: "Unit coverage for cleanup run behavior (dry-run, quota->target, cooldown)"
    - path: tests/integration/test_cli_storage_cleanup.py
      provides: "Integration coverage for CLI status/cleanup/pin JSON outputs"
  key_links:
    - from: tests/integration/test_cli_storage_cleanup.py
      to: src/bioimage_mcp/cli.py
      via: "subprocess invocation of CLI"
      pattern: "python -m bioimage_mcp\.cli"
---

<objective>
Add automated tests to verify retention/quota behavior end-to-end (DB schema + policy + cleanup engine + CLI), preventing regressions and ensuring safety constraints hold.

Purpose: Retention/quota features are high-risk; tests provide confidence against accidental deletion or blocking behavior.
Output: Unit + integration tests validating key invariants and JSON contracts.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-implement-artifact-store-retention-and-quota-management/18-CONTEXT.md
@.planning/phases/18-implement-artifact-store-retention-and-quota-management/18-RESEARCH.md

@src/bioimage_mcp/storage/sqlite.py
@src/bioimage_mcp/storage/manager.py
@src/bioimage_mcp/storage/cleanup.py
@src/bioimage_mcp/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit tests for StorageManager candidate selection and usage tracking triggers</name>
  <files>tests/unit/storage/test_retention_quota.py</files>
  <action>
Add unit tests that construct an in-memory sqlite DB (using existing init_schema) and assert retention/quota invariants.

Coverage requirements:
- Trigger-based total bytes:
  - Insert artifacts (non-memory) and assert registry_state.total_artifact_bytes increments.
  - Delete artifacts and assert it decrements.
  - Ensure memory artifacts are excluded from totals.
- Candidate selection ordering + exclusions:
  - Create artifacts with varying created_at and sizes.
  - Mark some pinned; verify they are excluded.
  - Create sessions with statuses active/inactive; verify active-session artifacts are excluded.
  - Ensure artifacts from the most recent session are excluded (protected session).
  - Ensure ordering is oldest-first.

Keep tests deterministic: use fixed ISO timestamps.
  </action>
  <verify>pytest -q tests/unit/storage/test_retention_quota.py</verify>
  <done>
Unit tests prove the DB accounting and candidate selection logic respects pinned/active/protected rules and FIFO deletion priority.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit + integration tests for cleanup engine and CLI JSON contracts</name>
  <files>
tests/unit/storage/test_cleanup_engine.py
tests/integration/test_cli_storage_cleanup.py
  </files>
  <action>
Add coverage for cleanup execution and the CLI contract.

Unit tests (`tests/unit/storage/test_cleanup_engine.py`):
- Dry-run:
  - Create fake file artifacts on disk + DB rows.
  - Run cleanup dry-run and assert:
    - files still exist
    - artifact rows still exist
    - cleanup_events row recorded with dry_run=1
- Quota cleanup down to target:
  - Set quota_bytes small and target_fraction=0.8.
  - Insert enough artifacts to exceed trigger; run cleanup real mode.
  - Assert after_bytes <= quota_bytes * 0.8 and oldest eligible artifacts were removed first.
- Cooldown enforcement:
  - Run cleanup twice in quick succession; second should skip unless force=True.

Integration tests (`tests/integration/test_cli_storage_cleanup.py`):
- Run `python -m bioimage_mcp.cli status --json` against a temp artifact_store_root.
- Run `python -m bioimage_mcp.cli cleanup --dry-run --json` and assert stable keys.
- Run pin/unpin commands and assert pinned state affects cleanup dry-run candidate counts.

Note: In integration tests, set config via temp HOME or write a temp `.bioimage-mcp/config.yaml` pointing at the temp artifact_store_root.
  </action>
  <verify>pytest -q tests/unit/storage/test_cleanup_engine.py tests/integration/test_cli_storage_cleanup.py</verify>
  <done>
Cleanup behavior (dry-run, quota->target, cooldown) and CLI JSON outputs are validated by automated tests.
  </done>
</task>

</tasks>

<verification>
- `ruff check .` and `ruff format .`
- `pytest -q`
</verification>

<success_criteria>
- Tests cover the safety-critical behaviors: skip pinned/active/protected, FIFO, dry-run safety, quota cleanup down to target, cooldown.
- CLI `status` and `cleanup` JSON outputs are stable and asserted.
</success_criteria>

<output>
After completion, create `.planning/phases/18-implement-artifact-store-retention-and-quota-management/18-05-SUMMARY.md`
</output>
