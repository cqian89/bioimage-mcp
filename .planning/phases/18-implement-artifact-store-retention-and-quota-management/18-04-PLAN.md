---
phase: 18-implement-artifact-store-retention-and-quota-management
plan: 4
type: execute
wave: 4
depends_on:
  - 18-03
files_modified:
  - src/bioimage_mcp/cli.py
  - src/bioimage_mcp/bootstrap/status.py
  - src/bioimage_mcp/bootstrap/cleanup.py
  - src/bioimage_mcp/bootstrap/pin.py
autonomous: true

must_haves:
  truths:
    - "User can inspect current storage usage, quotas, and recent cleanup summary via CLI"
    - "User can trigger manual cleanup and preview deletions via --dry-run"
    - "User can pin/unpin artifacts so they are exempt from cleanup"
  artifacts:
    - path: src/bioimage_mcp/bootstrap/status.py
      provides: "CLI storage status output (human + JSON)"
    - path: src/bioimage_mcp/bootstrap/cleanup.py
      provides: "CLI manual cleanup trigger (real + dry-run)"
    - path: src/bioimage_mcp/bootstrap/pin.py
      provides: "CLI pin/unpin implementation updating artifacts.pinned"
  key_links:
    - from: src/bioimage_mcp/cli.py
      to: src/bioimage_mcp/bootstrap/status.py
      via: "argparse handler wiring"
      pattern: "set_defaults\(_handler=_handle_"
    - from: src/bioimage_mcp/bootstrap/cleanup.py
      to: src/bioimage_mcp/storage/cleanup.py
      via: "run_cleanup/maybe_cleanup"
      pattern: "cleanup"
---

<objective>
Add CLI commands for storage visibility and control: `bioimage-mcp status`, `bioimage-mcp cleanup` (with dry-run), and pin/unpin support.

Purpose: Provide operational tooling to manage storage growth and verify retention/quota behavior without reading internal DB state.
Output: New CLI subcommands + JSON output for automation.
</objective>

<execution_context>
@/home/qianchen/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/qianchen/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-implement-artifact-store-retention-and-quota-management/18-CONTEXT.md
@src/bioimage_mcp/cli.py
@src/bioimage_mcp/bootstrap/list.py
@src/bioimage_mcp/bootstrap/serve.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement `bioimage-mcp status` for storage usage + recent cleanup events</name>
  <files>
src/bioimage_mcp/bootstrap/status.py
src/bioimage_mcp/cli.py
  </files>
  <action>
Add a status command that surfaces storage state and cleanup summaries.

Create `src/bioimage_mcp/bootstrap/status.py`:
- Implement `status(*, json_output: bool) -> int` following existing bootstrap patterns.
- Output MUST include:
  - total_bytes (from registry_state)
  - quota_bytes (from config.storage)
  - usage_fraction + usage_percent
  - warn_threshold (0.8) + whether warning is active
  - retention_days
  - last_cleanup event summary (most recent cleanup_events row) if available
  - last_cleanup_at and cooldown remaining (if tracked)
- For human output: concise multi-line summary.
- For JSON output: stable keys with a single top-level object.

Wire into `src/bioimage_mcp/cli.py`:
- Add `status` subcommand with `--json`.
- Add handler `_handle_status` similar to existing patterns.
  </action>
  <verify>python -m bioimage_mcp.cli status --json</verify>
  <done>
`bioimage-mcp status` prints storage usage and last cleanup summary; `--json` returns machine-readable output with stable keys.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement `bioimage-mcp cleanup` (manual trigger + dry-run) and pin/unpin commands</name>
  <files>
src/bioimage_mcp/bootstrap/cleanup.py
src/bioimage_mcp/bootstrap/pin.py
src/bioimage_mcp/cli.py
  </files>
  <action>
Add manual cleanup and pin control via CLI.

Create `src/bioimage_mcp/bootstrap/cleanup.py`:
- Implement `cleanup(*, dry_run: bool, json_output: bool, force: bool) -> int`.
- Call cleanup engine with reason="manual".
- Print summary-only output (counts + bytes freed + before/after + whether skipped due to cooldown).
- Support `--dry-run` (no disk/DB changes), `--force` (override cooldown), and `--json`.

Create `src/bioimage_mcp/bootstrap/pin.py`:
- Implement `pin(ref_id: str, *, unpin: bool, json_output: bool) -> int`.
- Update artifacts.pinned for the given ref_id; return NOT_FOUND if missing.

Wire into `src/bioimage_mcp/cli.py`:
- Add `cleanup` subcommand with flags: `--dry-run`, `--force`, `--json`.
- Add `pin` and `unpin` subcommands (or a single `pin` with `--unset`) with `--json`.

Constraints:
- Keep outputs summary-only; no per-artifact deletion listing.
- Do not require the server to be running; CLI works directly on the SQLite DB under artifact_store_root.
  </action>
  <verify>
python -m bioimage_mcp.cli cleanup --dry-run --json
python -m bioimage_mcp.cli pin deadbeefdeadbeefdeadbeefdeadbeef --json || true
  </verify>
  <done>
Manual cleanup runs (and dry-run previews) via CLI, and pin/unpin toggles the pinned flag so cleanup skips pinned artifacts.
  </done>
</task>

</tasks>

<verification>
- `python -m bioimage_mcp.cli status`
- `python -m bioimage_mcp.cli cleanup --dry-run`
- `pytest -q`
</verification>

<success_criteria>
- CLI provides storage visibility and control required by the phase boundary.
- Dry-run is available for preview and does not modify disk/DB.
- Pinned artifacts can be exempted from cleanup via CLI.
</success_criteria>

<output>
After completion, create `.planning/phases/18-implement-artifact-store-retention-and-quota-management/18-04-SUMMARY.md`
</output>
