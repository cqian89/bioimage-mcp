---
phase: 20-strategize-and-execute-test-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/contract/test_discovery_contract.py
  - tests/contract/test_discovery_hierarchy.py
  - tests/contract/test_meta_describe_contract.py
  - tests/unit/registry/test_manifest_discovery.py
autonomous: true

must_haves:
  truths:
    - "Discovery contract tests allow additive evolution for list/search/describe surfaces."
    - "meta.describe contract treats params_schema property descriptions as best-effort (missing descriptions do not fail)."
    - "Manifest discovery logic (and tests) only targets manifest.y*ml, not arbitrary YAML files."
  artifacts:
    - path: "tests/contract/test_discovery_contract.py"
      provides: "Additive-compatible contract checks for discovery surfaces."
    - path: "tests/contract/test_meta_describe_contract.py"
      provides: "meta.describe contract without mandatory per-property descriptions."
    - path: "tests/unit/registry/test_manifest_discovery.py"
      provides: "Regression coverage: only manifest.y*ml files are treated as manifests."
  key_links:
    - from: "tests/contract/test_meta_describe_contract.py"
      to: "params_schema"
      via: "properties validation"
      pattern: "params_schema"

---

<objective>
Relax overly strict contract assertions to match the Phase 20 contract policy.

Purpose: Keep strictness at true protocol boundaries while allowing discovery surfaces to evolve additively and tool-pack schemas to be best-effort.
Output: Updated contract tests (discovery + meta.describe) and a regression test for manifest-only YAML discovery.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-strategize-and-execute-test-consolidation/20-CONTEXT.md
@.planning/phases/20-strategize-and-execute-test-consolidation/20-RESEARCH.md

@tests/contract/test_discovery_contract.py
@tests/contract/test_discovery_hierarchy.py
@tests/contract/test_meta_describe_contract.py
@src/bioimage_mcp/registry/loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make discovery contract assertions additive-compatible</name>
  <files>
tests/contract/test_discovery_contract.py
tests/contract/test_discovery_hierarchy.py
  </files>
  <action>
Update discovery-related contract tests so they validate required fields without forbidding extra keys.

Specific changes:
- Replace exact-key assertions like `assert set(page.keys()) == {...}` with subset assertions:
  - `assert {required...}.issubset(page.keys())`

- In `tests/contract/test_discovery_contract.py::test_discovery_describe_function_returns_schema`:
  - Remove the allowlist-style restriction on top-level keys.
  - Keep strong requirements that MUST remain true:
    - `id` present and matches
    - `params_schema` present
    - `inputs`/`outputs` present for function nodes
    - `error` shape for NOT_FOUND remains validated elsewhere

Rationale: list/search/describe are user-facing discovery surfaces and must allow additive evolution.
  </action>
  <verify>pytest tests/contract/test_discovery_contract.py -q && pytest tests/contract/test_discovery_hierarchy.py -q</verify>
  <done>Discovery contract tests pass while allowing additional response fields.</done>
</task>

<task type="auto">
  <name>Task 2: Relax params_schema property description requirements in meta.describe contract</name>
  <files>tests/contract/test_meta_describe_contract.py</files>
  <action>
Update `tests/contract/test_meta_describe_contract.py` so it no longer requires every params_schema property to have a description.

Replace `test_params_schema_property_has_description` with a best-effort contract:
- If a property has `description`, assert it is a non-empty string.
- Do NOT fail if `description` is absent.

Keep strict protocol checks:
- `ok` / `error` success vs error shape
- Required presence of `result.params_schema`, `result.tool_version`, and `result.introspection_source`
  </action>
  <verify>pytest tests/contract/test_meta_describe_contract.py -q</verify>
  <done>Contract suite no longer fails when tool schemas lack descriptions for some parameters (e.g., trackpy).</done>
</task>

<task type="auto">
  <name>Task 3: Add regression test that manifest discovery ignores non-manifest YAML files</name>
  <files>tests/unit/registry/test_manifest_discovery.py</files>
  <action>
Create `tests/unit/registry/test_manifest_discovery.py` to lock in the behavior that only `manifest.yaml` / `manifest.yml` are treated as tool manifests.

Test design:
- Create a temp directory with multiple YAML files, including:
  - `tools/foo/manifest.yaml`
  - `tools/foo/not_a_manifest.yaml`
  - `tools/bar/manifest.yml`
  - `tools/bar/other.yml`
- Call `bioimage_mcp.registry.loader.discover_manifest_paths([tmp_tools_root])`.
- Assert it returns ONLY the manifest files (both yaml/yml), and never returns the other YAMLs.

This directly addresses the carried-forward contract concern about skipping non-manifest YAMLs.
  </action>
  <verify>pytest tests/unit/registry/test_manifest_discovery.py -q</verify>
  <done>Regression coverage prevents future manifest loaders/tests from accidentally validating arbitrary YAML files.</done>
</task>

</tasks>

<verification>
- `pytest tests/contract -q`
- `pytest tests/unit/registry/test_manifest_discovery.py -q`
</verification>

<success_criteria>
- Contract tests enforce real boundaries (protocol shape) without blocking additive discovery evolution.
- meta.describe contract is compatible with best-effort documentation quality.
- Only manifest.y*ml files are treated as tool manifests.
</success_criteria>

<output>
After completion, create `.planning/phases/20-strategize-and-execute-test-consolidation/20-02-SUMMARY.md`
</output>
