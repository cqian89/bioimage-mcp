---
phase: 20-strategize-and-execute-test-consolidation
plan: 04
type: execute
wave: 3
depends_on:
  - "20-03"
files_modified:
  - AGENTS.md
  - .planning/codebase/TESTING.md
autonomous: true

must_haves:
  truths:
    - "Developers can run the intended PR gate locally using documented pytest commands."
    - "Marker semantics (smoke_minimal/smoke_pr/smoke_extended, requires_env) are documented in one place."
  artifacts:
    - path: "AGENTS.md"
      provides: "Contributor-facing marker and command guidance stays accurate."
    - path: ".planning/codebase/TESTING.md"
      provides: "Project test organization and recommended run commands reflect Phase 20 consolidation."
  key_links:
    - from: ".planning/codebase/TESTING.md"
      to: "pytest.ini"
      via: "documented markers and commands"
      pattern: "smoke_pr|smoke_extended|requires_env"

---

<objective>
Update developer-facing docs to reflect the consolidated test tiers and env gating.

Purpose: Make the consolidated suite usable without tribal knowledge: clear commands for local runs and CI-like PR gates.
Output: Updated marker documentation and recommended pytest commands.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-strategize-and-execute-test-consolidation/20-CONTEXT.md
@pytest.ini
@AGENTS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh marker documentation and examples for the new smoke tiers</name>
  <files>AGENTS.md</files>
  <action>
Update `AGENTS.md` so examples and marker lists match Phase 20:

- Replace references to `smoke_full` with the new tiers:
  - `smoke_minimal` (fast)
  - `smoke_pr` (PR gate)
  - `smoke_extended` (extended/nightly)

- Update env gating guidance:
  - Prefer `@pytest.mark.requires_env("bioimage-mcp-...")`.
  - Mention legacy convenience markers only as deprecated aliases.

Include a short, copy-pastable PR-gate command set:
- `pytest tests/unit -q`
- `pytest tests/contract -q`
- `pytest tests/smoke -m smoke_minimal -q`
- `pytest tests/smoke -m smoke_pr -q`
  </action>
  <verify>python -c "import pathlib; t=pathlib.Path('AGENTS.md').read_text(encoding='utf-8'); assert 'smoke_pr' in t and 'smoke_extended' in t and 'requires_env' in t"</verify>
  <done>Docs match the new marker scheme and provide clear local run commands.</done>
</task>

<task type="auto">
  <name>Task 2: Update internal test runbook to reflect directory + marker alignment</name>
  <files>.planning/codebase/TESTING.md</files>
  <action>
Update `.planning/codebase/TESTING.md` to match current reality:

- Replace any outdated async guidance (`pytest.mark.asyncio`) with the actual smoke-suite convention (AnyIO/async fixtures).
- Document the test directories and how they map to markers:
  - `tests/unit/` (no envs)
  - `tests/contract/` (schema/protocol)
  - `tests/integration/` (server flows)
  - `tests/smoke/` (E2E) with smoke_minimal/smoke_pr/smoke_extended

- Add a concise “CI-like PR gate” section listing the recommended commands.

Keep this file short; it is a runbook, not a spec.
  </action>
  <verify>python -c "import pathlib; t=pathlib.Path('.planning/codebase/TESTING.md').read_text(encoding='utf-8'); assert 'smoke_pr' in t and 'smoke_extended' in t and 'requires_env' in t"</verify>
  <done>Runbook reflects the consolidated tiers and the intended PR gate commands.</done>
</task>

</tasks>

<verification>
- `pytest -q` (expected to pass; optional env-gated tests may skip with warnings)
</verification>

<success_criteria>
- Developer-facing and internal docs match how the consolidated suite is selected and run.
- New contributors can run the PR gate locally without guessing marker names.
</success_criteria>

<output>
After completion, create `.planning/phases/20-strategize-and-execute-test-consolidation/20-04-SUMMARY.md`
</output>
