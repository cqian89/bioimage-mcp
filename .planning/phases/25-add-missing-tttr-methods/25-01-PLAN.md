---
phase: 25-add-missing-tttr-methods
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/tttrlib/schema/tttrlib_coverage.json
  - tools/tttrlib/schema/tttrlib_api.json
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/contract/test_tttrlib_parity_inventory.py
  - tests/unit/test_tttrlib_entrypoint_unsupported.py
autonomous: true
requirements: [TTTR-01, TTTR-04]

must_haves:
  truths:
    - "Phase 25 has a machine-readable runtime parity inventory where each uncovered method is classified as supported, supported_subset, deferred, or denied"
    - "Denied or deferred tttrlib callables return stable unsupported error codes with remediation, not silent omission"
    - "Parity inventory drift is automatically detected by contract tests"
  artifacts:
    - path: tools/tttrlib/schema/tttrlib_coverage.json
      provides: "Runtime-derived callable coverage registry with per-method status and rationale"
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "Central unsupported-callable handling with stable error responses"
    - path: tests/contract/test_tttrlib_parity_inventory.py
      provides: "Automated drift guard between installed runtime and coverage registry"
  key_links:
    - from: tests/contract/test_tttrlib_parity_inventory.py
      to: tools/tttrlib/schema/tttrlib_coverage.json
      via: "coverage status validation against runtime callable enumeration"
      pattern: "supported|supported_subset|deferred|denied"
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tools/tttrlib/schema/tttrlib_coverage.json
      via: "unsupported-callable routing and stable remediation"
      pattern: "TTTRLIB_UNSUPPORTED_METHOD"
---

<objective>
Establish the parity-control foundation before adding large method coverage.

Purpose: Near-full runtime parity is only safe if every callable is explicitly tracked and unsupported behavior is deterministic.
Output: Runtime parity inventory, explicit unsupported policy wiring, and contract tests that fail on drift.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-VALIDATION.md
@tools/tttrlib/manifest.yaml
@tools/tttrlib/schema/tttrlib_api.json
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py

<interfaces>
From tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py:
```python
FUNCTION_HANDLERS: dict[str, Callable[[dict[str, Any], dict[str, Any], Path], dict[str, Any]]]
```

From tools/tttrlib/manifest.yaml:
```yaml
functions:
  - id: tttrlib.Class.method
    inputs: [...]
    outputs: [...]
    params_schema: {...}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runtime parity inventory and contract drift guard</name>
  <files>
tools/tttrlib/schema/tttrlib_coverage.json
tools/tttrlib/schema/tttrlib_api.json
tests/contract/test_tttrlib_parity_inventory.py
  </files>
  <action>
- Add `tools/tttrlib/schema/tttrlib_coverage.json` containing callable coverage records keyed by strict upstream IDs (`tttrlib.Class.method`) with fields: `status`, `owner`, `rationale`, and `revisit_trigger`.
- Seed coverage from installed runtime inventory for at least `TTTR`, `CLSMImage`, and `Correlator` class surfaces; classify each callable as `supported`, `supported_subset`, `deferred`, or `denied`.
- Update `tttrlib_api.json` metadata to reference the coverage file as the parity source and preserve `upstream_version` tracking.
- Add `tests/contract/test_tttrlib_parity_inventory.py` that fails when runtime methods in target classes are missing coverage classification or contain invalid status values.
  </action>
  <verify>
    <automated>pytest tests/contract/test_tttrlib_parity_inventory.py -q</automated>
  </verify>
  <done>
Coverage drift is automatically detectable and every target callable has an explicit status.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stable unsupported-callable routing in tttr entrypoint</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_unsupported.py
  </files>
  <behavior>
    - Test 1: Calling a denied/deferred function ID returns `ok=false` with stable code `TTTRLIB_UNSUPPORTED_METHOD` and non-empty remediation guidance.
    - Test 2: Unknown IDs still return the existing unknown-function path (no regression of baseline dispatch behavior).
  </behavior>
  <action>
- Add a centralized unsupported-method policy reader in entrypoint that checks `tttrlib_coverage.json` before handler dispatch.
- Return stable structured errors for `denied`/`deferred` methods including method ID and concise remediation guidance.
- Keep strict separation between `unsupported` and truly `unknown` IDs to preserve discoverability and debugging quality.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_unsupported.py -q</automated>
  </verify>
  <done>
Unsupported methods fail fast with stable diagnostics and no silent omissions.
  </done>
</task>

</tasks>

<verification>
- pytest tests/contract/test_tttrlib_parity_inventory.py -q
- pytest tests/unit/test_tttrlib_entrypoint_unsupported.py -q
</verification>

<success_criteria>
- TTTR-01 satisfied: runtime parity inventory exists and is enforced by tests.
- TTTR-04 baseline satisfied: unsupported methods have explicit, stable runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-01-SUMMARY.md`
</output>
