---
phase: 25-add-missing-tttr-methods
plan: 10
type: execute
wave: 2
depends_on: ["25-09"]
files_modified:
  - src/bioimage_mcp/api/execution.py
  - tools/tttrlib/manifest.yaml
  - tools/tttrlib/schema/tttrlib_api.json
  - tools/tttrlib/schema/tttrlib_coverage.json
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/contract/test_tttrlib_manifest.py
  - tests/contract/test_tttrlib_schema_alignment.py
  - tests/unit/api/test_execution.py
  - tests/unit/test_tttrlib_entrypoint_tttr_methods.py
  - tests/smoke/test_tttrlib_live.py
autonomous: true
requirements: [TTTR-02, TTTR-03, TTTR-04, TTTR-05]
gap_closure: true

must_haves:
  truths:
    - "Removed specialized TTTR export methods are not discoverable as supported MCP callables and fail with TTTRLIB_UNSUPPORTED_METHOD rather than NOT_FOUND."
    - "Generic tttrlib.TTTR.write only succeeds when it creates a real file under work_dir and the returned TTTRRef uses a canonical format value."
    - "Wrong extensions and unsupported export combinations fail with stable export guardrail or subset errors instead of TTTRRef schema validation."
  artifacts:
    - path: src/bioimage_mcp/api/execution.py
      provides: "Run-only fallback for known deferred or denied tttrlib export IDs."
    - path: tools/tttrlib/manifest.yaml
      provides: "Reduced runtime-safe TTTR export discovery surface."
    - path: tools/tttrlib/schema/tttrlib_coverage.json
      provides: "Deferred or denied status for removed specialized export IDs."
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "Canonical TTTR format mapping plus post-write file existence verification."
    - path: tests/unit/test_tttrlib_entrypoint_tttr_methods.py
      provides: "Regression coverage for wrong-extension, missing-file, and canonical-format TTTR write behavior."
  key_links:
    - from: tools/tttrlib/schema/tttrlib_coverage.json
      to: src/bioimage_mcp/api/execution.py
      via: "run-only fallback for deferred or denied tttrlib IDs"
      pattern: "TTTRLIB_UNSUPPORTED_METHOD|write_header|write_hht3v2_events|write_spc132_events"
    - from: tools/tttrlib/manifest.yaml
      to: tools/tttrlib/schema/tttrlib_api.json
      via: "contract-triad alignment for the reduced export surface"
      pattern: "tttrlib.TTTR.write|write_header|write_hht3v2_events|write_spc132_events"
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: src/bioimage_mcp/artifacts/models.py
      via: "TTTR write outputs must map filename suffixes to canonical TTTRRef.format values"
      pattern: "SPC-130|PHOTON-HDF5|TTTRRef"
---

<objective>
Close the Phase 25 TTTR export-surface regression without widening the public runtime contract.

Purpose: The current working tree drifted back to pre-fix behavior: removed export IDs fall out at core lookup, generic writes build invalid `TTTRRef.format` values from raw suffixes, and successful responses are returned before a real export file is verified.
Output: Restored run-only unsupported fallback for removed export IDs, manifest/schema/coverage re-aligned to the reduced safe export surface, and entrypoint regressions that keep generic write guardrails deterministic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-UAT.md
@.planning/phases/25-add-missing-tttr-methods/25-06-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-07-SUMMARY.md
@.planning/debug/tttr-export-guardrail-diagnose.md
@src/bioimage_mcp/api/execution.py
@src/bioimage_mcp/artifacts/models.py
@tools/tttrlib/manifest.yaml
@tools/tttrlib/schema/tttrlib_api.json
@tools/tttrlib/schema/tttrlib_coverage.json
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
@tests/contract/test_tttrlib_manifest.py
@tests/contract/test_tttrlib_schema_alignment.py
@tests/unit/api/test_execution.py
@tests/unit/test_tttrlib_entrypoint_tttr_methods.py
@tests/smoke/test_tttrlib_live.py

<interfaces>
From `src/bioimage_mcp/api/execution.py`:
```python
manifest, fn_def = _get_function_metadata(config, fn_id)
if not manifest:
    raise KeyError(fn_id)
```

From `src/bioimage_mcp/artifacts/models.py`:
```python
class TTTRRef(ArtifactRef):
    format: Literal[
        "PTU", "HT3", "SPC-130", "SPC-630_256", "SPC-630_4096",
        "PHOTON-HDF5", "HDF", "CZ-RAW", "SM"
    ] | None = None
```

From `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`:
```python
tttr.write(str(filepath))
fmt = filepath.suffix[1:].upper() if filepath.suffix else "auto"
output = _build_tttr_file_output(filepath, fmt)

tttr.write_header(str(filepath))
tttr.write_hht3v2_events(str(filepath), tttr)
tttr.write_spc132_events(str(filepath), tttr)
```

Honor the locked decision from `25-CONTEXT.md`: unsupported or unsafe methods stay hidden from discovery and return stable remediation when invoked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore reduced export discovery and run-only unsupported routing</name>
  <files>
src/bioimage_mcp/api/execution.py
tools/tttrlib/manifest.yaml
tools/tttrlib/schema/tttrlib_api.json
tools/tttrlib/schema/tttrlib_coverage.json
tests/contract/test_tttrlib_manifest.py
tests/contract/test_tttrlib_schema_alignment.py
tests/unit/api/test_execution.py
  </files>
  <behavior>
    - Test 1: `tttrlib.TTTR.write_header`, `tttrlib.TTTR.write_hht3v2_events`, and `tttrlib.TTTR.write_spc132_events` are not published as supported discovery entries.
    - Test 2: Direct `run()` for those removed IDs still reaches the tttrlib worker and returns `TTTRLIB_UNSUPPORTED_METHOD`, while a truly unknown ID still returns core `NOT_FOUND`.
    - Test 3: The manifest, published schema, and coverage registry agree on the reduced export surface and on the deferred or denied rationale for removed export methods.
  </behavior>
  <action>
- Restore the `25-06`/`25-07` coverage-driven fallback in `src/bioimage_mcp/api/execution.py` so strict `tttrlib.*` IDs that are known in `tools/tttrlib/schema/tttrlib_coverage.json` with `deferred` or `denied` status resolve to the tttrlib worker for `run()` only.
- Re-align `tools/tttrlib/manifest.yaml`, `tools/tttrlib/schema/tttrlib_api.json`, and `tools/tttrlib/schema/tttrlib_coverage.json` to the reduced runtime-safe export surface described in `25-07-SUMMARY.md`: keep only the supported generic `tttrlib.TTTR.write` discovery entry, and keep removed specialized exports out of discovery with explicit deferred or denied coverage entries.
- Reinstate contract and execution regressions in `tests/contract/test_tttrlib_manifest.py`, `tests/contract/test_tttrlib_schema_alignment.py`, and `tests/unit/api/test_execution.py` so future drift back to `NOT_FOUND` or re-published specialized export methods is caught immediately.
- Do not implement a new Python-side shim for specialized exports in this plan. The user decision is to preserve the reduced runtime-safe surface, not to broaden it again.
  </action>
  <verify>
    <automated>pytest tests/contract/test_tttrlib_manifest.py tests/contract/test_tttrlib_schema_alignment.py tests/unit/api/test_execution.py -q</automated>
  </verify>
  <done>
Removed specialized exports stay out of discovery, and invoking them now returns the stable unsupported-method contract instead of `NOT_FOUND`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-harden generic TTTR.write output validation and canonical format mapping</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_tttr_methods.py
tests/smoke/test_tttrlib_live.py
  </files>
  <behavior>
    - Test 1: Generic `.spc` and `.h5` writes return `TTTRRef` outputs with canonical format values accepted by `TTTRRef`, not raw suffix strings like `SPC` or `TXT`.
    - Test 2: Wrong extensions and unsupported write combinations fail with stable guardrail or `TTTRLIB_UNSUPPORTED_ARGUMENT_PATTERN` errors before any invalid `TTTRRef` reaches core import.
    - Test 3: If `tttr.write()` returns false or no file appears on disk, MCP returns a deterministic error and does not emit a ghost export artifact.
  </behavior>
  <action>
- Replace the raw `filepath.suffix[1:].upper()` mapping in `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py` with canonical TTTR format selection that matches `src/bioimage_mcp/artifacts/models.py` (for example `.spc` -> `SPC-130`, `.h5`/`.hdf5` -> `PHOTON-HDF5`). Reuse or restore the existing format helper pattern instead of hand-rolling per-branch strings.
- After `tttr.write(...)`, verify both the upstream return semantics and on-disk file existence before building `tttr_out`. If the file is missing or the call clearly indicates failure, return `TTTRLIB_UNSUPPORTED_ARGUMENT_PATTERN` with stable guidance rather than a bogus success payload.
- Keep the existing path-boundary and extension guardrails intact. This task restores the post-write safety contract; it must not relax sandbox enforcement to make tests pass.
- Reinstate unit regressions in `tests/unit/test_tttrlib_entrypoint_tttr_methods.py` and live smoke assertions in `tests/smoke/test_tttrlib_live.py` that prove wrong-extension, supported-format, and no-file cases stay deterministic.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py -q && pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q -k "tttr_write or export"</automated>
  </verify>
  <done>
Generic TTTR export only succeeds when a real file exists and the returned `TTTRRef` is schema-valid, while removed specialized export methods remain stable unsupported paths.
  </done>
</task>

</tasks>

<verification>
- pytest tests/contract/test_tttrlib_manifest.py tests/contract/test_tttrlib_schema_alignment.py tests/unit/api/test_execution.py tests/unit/test_tttrlib_entrypoint_tttr_methods.py -q
- pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q
</verification>

<success_criteria>
- UAT gap 3 closes with no `TTTRRef format='TXT'` or `format='SPC'` validation failures in the supported export path.
- Removed specialized export methods reject with `TTTRLIB_UNSUPPORTED_METHOD`, and generic `TTTR.write` never returns a success artifact without a real file.
- TTTR-02 through TTTR-05 remain satisfied through synchronized contract, worker, and smoke regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-10-SUMMARY.md`
</output>
