---
phase: 25-add-missing-tttr-methods
plan: 08
type: execute
wave: 3
depends_on: ["25-07"]
files_modified:
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/unit/test_tttrlib_entrypoint_clsm_methods.py
  - tests/smoke/test_tttrlib_live.py
autonomous: true
requirements: [TTTR-03, TTTR-05]
gap_closure: true

must_haves:
  truths:
    - "CLSMImage.get_settings returns clean JSON metadata without SWIG transport fields like this or thisown."
    - "The cleaned get_settings payload still preserves real CLSM domain fields and nested JSON-safe values."
    - "Live smoke explicitly guards the absence of SWIG proxy fields instead of only checking that the JSON artifact is non-empty."
  artifacts:
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "CLSM settings serializer that strips SWIG transport attrs while keeping domain data."
    - path: tests/unit/test_tttrlib_entrypoint_clsm_methods.py
      provides: "Unit coverage for SWIG-style fields leaking through get_settings serialization."
    - path: tests/smoke/test_tttrlib_live.py
      provides: "Live assertion that get_settings artifacts exclude this/thisown."
  key_links:
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/unit/test_tttrlib_entrypoint_clsm_methods.py
      via: "serializer blocklist behavior for SWIG transport attrs"
      pattern: "_serialize_clsm_settings|thisown|this"
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/smoke/test_tttrlib_live.py
      via: "artifact-content assertions against live get_settings output"
      pattern: "CLSMImage.get_settings|thisown|this"
---

<objective>
Close the remaining CLSM metadata cleanliness gap from Phase 25 UAT.

Purpose: `get_settings` already returns a JSON artifact, but it still leaks SWIG transport fields that make the payload look like an internal proxy instead of usable metadata.
Output: A cleaned CLSM settings serializer plus unit and live regressions that explicitly reject `this` and `thisown` in the returned JSON artifact.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-UAT.md
@.planning/phases/25-add-missing-tttr-methods/25-05-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-07-SUMMARY.md
@.planning/debug/phase25-gap4-tttrlib-settings-diagnosis.md
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
@tests/unit/test_tttrlib_entrypoint_clsm_methods.py
@tests/smoke/test_tttrlib_live.py

<interfaces>
From `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`:
```python
def _normalize_json_safe_value(value: Any, *, _seen: set[int] | None = None) -> Any:
    for attr_name in sorted(name for name in dir(value) if not name.startswith("_")):
        attr_value = getattr(value, attr_name)
        if callable(attr_value):
            continue
        public_attrs[attr_name] = _normalize_json_safe_value(attr_value, _seen=_seen | {obj_id})

def _serialize_clsm_settings(settings: Any) -> dict[str, Any]:
    payload = _normalize_json_safe_value(settings)
```

From `.planning/debug/phase25-gap4-tttrlib-settings-diagnosis.md`:
```text
Live CLSMSettings proxies expose transport attrs `this` and `thisown`.
Current unit tests omit those attrs, and smoke only checks for a non-empty dict.
```

Keep the existing JSON-safe conversion for nested vectors and scalar values; this gap is about filtering transport fields, not reverting to stringified proxies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip SWIG transport attrs from CLSM settings serialization</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_clsm_methods.py
  </files>
  <behavior>
    - Test 1: A fake `CLSMSettings` object that includes `this`, `thisown`, and real domain fields serializes into a JSON object that keeps the domain fields and drops the transport attrs.
    - Test 2: Nested SWIG-like vectors or scalar wrappers still normalize into JSON-safe Python values after the filter is applied.
  </behavior>
  <action>
- Update `_normalize_json_safe_value()` or `_serialize_clsm_settings()` so SWIG transport attrs `this` and `thisown` are always excluded from the emitted payload. Use a targeted blocklist for transport attrs rather than a tiny hardcoded whitelist, because real `CLSMSettings` objects carry more valid domain fields than the current fake does.
- Keep the existing JSON-safe normalization behavior from `25-05-SUMMARY.md` for nested vectors and scalar wrappers; the goal is clean metadata, not less metadata.
- Extend `tests/unit/test_tttrlib_entrypoint_clsm_methods.py` with a fake settings object that explicitly includes `this` and `thisown` plus real fields, then assert the output JSON omits only the transport attrs.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q</automated>
  </verify>
  <done>
`CLSMImage.get_settings` returns inspectable JSON metadata without leaking SWIG transport fields, while real settings content remains available.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard the cleaned CLSM settings payload in live smoke coverage</name>
  <files>
tests/smoke/test_tttrlib_live.py
  </files>
  <action>
- Strengthen the existing live `tttrlib.CLSMImage.get_settings` smoke scenario so it opens the returned `NativeOutputRef` JSON artifact, asserts the payload is a dict, and explicitly rejects keys `this` and `thisown`.
- Keep one positive assertion on known domain fields from the same artifact so the smoke test proves the serializer still returns useful metadata rather than an empty dict.
- Leave the neighboring `get_image_info` smoke path intact as the clean baseline for metadata methods.
  </action>
  <verify>
    <automated>pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q -k "clsm_metadata_methods"</automated>
  </verify>
  <done>
Live smoke now fails if CLSM settings regress back to exposing SWIG proxy transport fields.
  </done>
</task>

</tasks>

<verification>
- pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q
- pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q
</verification>

<success_criteria>
- UAT gap 4 is closed with both unit and live assertions that `this` and `thisown` are absent from `get_settings` artifacts.
- TTTR-03 stays accurate for CLSM metadata payloads, and TTTR-05 now guards the exact regression the user observed.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-08-SUMMARY.md`
</output>
