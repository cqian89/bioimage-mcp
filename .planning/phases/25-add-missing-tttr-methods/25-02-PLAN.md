---
phase: 25-add-missing-tttr-methods
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - tools/tttrlib/manifest.yaml
  - tools/tttrlib/schema/tttrlib_api.json
  - tools/tttrlib/schema/tttrlib_coverage.json
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/unit/test_tttrlib_entrypoint_tttr_methods.py
  - tests/contract/test_tttrlib_manifest.py
  - tests/contract/test_tttrlib_schema_alignment.py
autonomous: true
requirements: [TTTR-02, TTTR-03, TTTR-04]

must_haves:
  truths:
    - "Core TTTR method families beyond open/header/write are available through MCP with strict upstream IDs"
    - "New TTTR method outputs follow artifact policy (TableRef for tabular outputs, NativeOutputRef for irregular payloads, TTTRRef/ObjectRef where reusable)"
    - "Write/export methods are guarded by explicit path and safety checks"
  artifacts:
    - path: tools/tttrlib/manifest.yaml
      provides: "Expanded TTTR callable surface with explicit input/output contracts"
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "Executable handlers for newly exposed TTTR methods and guarded export behavior"
    - path: tests/unit/test_tttrlib_entrypoint_tttr_methods.py
      provides: "Deterministic behavior coverage for TTTR method mappings and guardrails"
  key_links:
    - from: tools/tttrlib/manifest.yaml
      to: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      via: "function id to handler registration consistency"
      pattern: "FUNCTION_HANDLERS"
    - from: tools/tttrlib/schema/tttrlib_api.json
      to: tests/contract/test_tttrlib_schema_alignment.py
      via: "schema-to-manifest parity checks"
      pattern: "functions"
---

<objective>
Expand TTTR class coverage with contract-safe method mappings.

Purpose: TTTR methods are the highest-priority parity gap from user decisions and unlock most non-imaging workflows.
Output: New TTTR method surface in manifest/schema/handlers with guarded export behavior and regression tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-VALIDATION.md
@.planning/phases/25-add-missing-tttr-methods/25-01-SUMMARY.md
@tools/tttrlib/manifest.yaml
@tools/tttrlib/schema/tttrlib_api.json
@tools/tttrlib/schema/tttrlib_coverage.json
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TTTR getter/selection/statistics method family mappings</name>
  <files>
tools/tttrlib/manifest.yaml
tools/tttrlib/schema/tttrlib_api.json
tools/tttrlib/schema/tttrlib_coverage.json
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_tttr_methods.py
tests/contract/test_tttrlib_manifest.py
tests/contract/test_tttrlib_schema_alignment.py
  </files>
  <behavior>
    - Test 1: Newly exposed TTTR getter/selection calls return mapped artifact outputs with expected shape metadata or column contracts.
    - Test 2: Methods classified as `supported_subset` reject unsupported argument patterns with stable remediation errors.
  </behavior>
  <action>
- Add TTTR family methods prioritized for analysis workflows (e.g., selection, intensity trace, histogram/statistics getters) using strict IDs and explicit output mapping.
- For each added method, update manifest and `tttrlib_api.json` in lockstep, and mark status transitions in `tttrlib_coverage.json` from deferred/denied to supported/supported_subset.
- Implement entrypoint handlers that preserve artifact boundary and avoid raw array returns in protocol payloads.
- Extend contract tests so the expanded method set is asserted and schema alignment remains strict.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py tests/contract/test_tttrlib_manifest.py tests/contract/test_tttrlib_schema_alignment.py -q</automated>
  </verify>
  <done>
Core TTTR non-imaging parity is materially expanded with contract-checked mappings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden TTTR write/export variants with explicit path guardrails</name>
  <files>
tools/tttrlib/manifest.yaml
tools/tttrlib/schema/tttrlib_api.json
tools/tttrlib/schema/tttrlib_coverage.json
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_tttr_methods.py
  </files>
  <behavior>
    - Test 1: Relative output paths resolve under `work_dir` and succeed for supported export methods.
    - Test 2: Unsafe or unsupported export combinations fail with stable error code and actionable message.
  </behavior>
  <action>
- Add/expand TTTR export methods (including missing write-family callables) only where explicit path parameters can be safely enforced.
- Apply a shared path validator that prevents unsafe destinations and returns deterministic errors.
- Keep unsupported write combinations classified as `denied` or `supported_subset` in coverage metadata with rationale and revisit trigger.
- Ensure all exported TTTR outputs return proper `TTTRRef` metadata and consistent format labeling.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py -q</automated>
  </verify>
  <done>
Write/export parity increases without violating safety constraints or artifact contracts.
  </done>
</task>

</tasks>

<verification>
- pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py -q
- pytest tests/contract/test_tttrlib_manifest.py tests/contract/test_tttrlib_schema_alignment.py -q
</verification>

<success_criteria>
- TTTR-02 and TTTR-03 are satisfied for the prioritized TTTR method families.
- TTTR-04 remains enforced through explicit subset/deny handling for unsafe writes.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-02-SUMMARY.md`
</output>
