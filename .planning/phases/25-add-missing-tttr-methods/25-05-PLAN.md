---
phase: 25-add-missing-tttr-methods
plan: 05
type: execute
wave: 4
depends_on: ["25-03", "25-04"]
files_modified:
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/unit/test_tttrlib_entrypoint_clsm_methods.py
  - tests/smoke/test_tttrlib_live.py
autonomous: true
requirements: [TTTR-02, TTTR-03, TTTR-05]
gap_closure: true

must_haves:
  truths:
    - "CLSMImage.get_settings returns JSON metadata instead of a stringified SWIG proxy"
    - "Correlator.get_curve, get_x_axis, and get_corr all succeed for supported constructor args and emit consistent tabular artifacts"
    - "Live smoke opens returned artifacts and verifies payload content, not just ref shape"
  artifacts:
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "CLSM metadata serialization and normalized Correlator getter output helpers"
    - path: tests/unit/test_tttrlib_entrypoint_clsm_methods.py
      provides: "Unit coverage for JSON-safe settings payloads and real-shape Correlator getter behavior"
    - path: tests/smoke/test_tttrlib_live.py
      provides: "Live artifact-content checks for CLSM settings and Correlator getter consistency"
  key_links:
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/unit/test_tttrlib_entrypoint_clsm_methods.py
      via: "serializer and shared table-builder behavior"
      pattern: "get_settings|get_curve|get_x_axis|get_corr"
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/smoke/test_tttrlib_live.py
      via: "artifact-content assertions against live outputs"
      pattern: "CLSMImage.get_settings|Correlator.get_corr"
---

<objective>
Close the diagnosed CLSM settings and Correlator output gaps from Phase 25 UAT.

Purpose: These payload-level regressions mean the current parity surface claims success while still returning incorrect metadata and brittle Correlator artifacts.
Output: JSON-safe CLSM settings export, normalized Correlator getter outputs, and stronger live validation of payload content.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-UAT.md
@.planning/phases/25-add-missing-tttr-methods/25-03-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-04-SUMMARY.md
@.planning/debug/phase25-gap4-tttrlib-settings.md
@.planning/debug/phase-25-uat-gap-5.md
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
@tests/unit/test_tttrlib_entrypoint_clsm_methods.py
@tests/smoke/test_tttrlib_live.py

<interfaces>
From `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`:
```python
def _write_native_output(payload: Any, stem: str, work_dir: Path) -> dict[str, Any]:
    with open(output_path, "w", encoding="utf-8") as f:
        json.dump(payload, f, default=str)

def handle_clsm_get_settings(...):
    output = _write_native_output(clsm.get_settings(), "clsm_settings", work_dir)

def handle_correlator_get_curve(...):
    curve = correlator.get_curve()
    tau, corr = curve
```

From live-debug evidence in `.planning/debug/phase25-gap4-tttrlib-settings.md` and `.planning/debug/phase-25-uat-gap-5.md`:
```text
CLSMImage.get_settings() -> CLSMSettings SWIG proxy, not dict
Correlator.get_curve() -> CorrelatorCurve object with .x/.y and no __iter__
```

Use these runtime facts directly; do not preserve the current proxy-string or tuple-unpack behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize CLSM settings into JSON-safe metadata artifacts</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_clsm_methods.py
tests/smoke/test_tttrlib_live.py
  </files>
  <behavior>
    - Test 1: `tttrlib.CLSMImage.get_settings` writes a JSON object with scalar fields and nested SWIG vector values converted to native Python data.
    - Test 2: Unit and smoke coverage reject the old stringified proxy behavior by opening the artifact and asserting object content.
  </behavior>
  <action>
- Add an explicit `CLSMSettings` serializer/normalizer that walks the returned object's public attributes and converts nested SWIG containers into JSON-safe Python scalars/lists/dicts; do not rely on `json.dump(..., default=str)` for this path.
- Route `handle_clsm_get_settings` through that serializer before calling `_write_native_output`; leave `get_image_info` untouched except for any helper reuse that makes tests clearer.
- Strengthen `tests/unit/test_tttrlib_entrypoint_clsm_methods.py` and `tests/smoke/test_tttrlib_live.py` so they open the returned `NativeOutputRef` artifact and assert JSON object content rather than only checking the ref type.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q</automated>
  </verify>
  <done>
`CLSMImage.get_settings` produces JSON metadata that a downstream agent can inspect without parsing a SWIG proxy string.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize Correlator getter outputs around the live CorrelatorCurve shape</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_clsm_methods.py
tests/smoke/test_tttrlib_live.py
  </files>
  <behavior>
    - Test 1: `tttrlib.Correlator.get_curve` accepts a non-iterable CorrelatorCurve-like object and returns the `tau,correlation` table via `.x/.y`.
    - Test 2: `get_x_axis` and `get_corr` emit consistent table metadata/header contracts with the constructor and `get_curve` paths, while unsupported subset params still fail stably.
    - Test 3: Live smoke covers `get_corr` and verifies headers/content for `get_curve`, `get_x_axis`, and `get_corr` artifacts.
  </behavior>
  <action>
- Refactor Correlator table emission through one shared helper used by the constructor and all getter handlers so column naming, row counts, and metadata stay aligned.
- Update `handle_correlator_get_curve` to read `.x` and `.y` (or the getter methods on the returned object) instead of tuple-unpacking; preserve the current supported-subset constructor validation and do not broaden the public params.
- Expand unit tests to use a non-iterable CorrelatorCurve-like fake plus explicit assertions for `get_x_axis` and `get_corr`, then extend the live smoke test to exercise `tttrlib.Correlator.get_corr` and inspect the returned CSV/table headers.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q</automated>
  </verify>
  <done>
All exposed Correlator getters succeed against the real runtime shape and produce consistent tabular artifacts that match their declared subset contract.
  </done>
</task>

</tasks>

<verification>
- pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q
- pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q
</verification>

<success_criteria>
- The diagnosed CLSM settings payload gap and Correlator getter/runtime-shape gap are closed with content-level evidence.
- TTTR-02 and TTTR-03 stay accurate for CLSM/Correlator behavior, and TTTR-05 now verifies payload content instead of only artifact ref shape.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-05-SUMMARY.md`
</output>
