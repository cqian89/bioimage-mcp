---
phase: 25-add-missing-tttr-methods
plan: 07
type: execute
wave: 2
depends_on: ["25-06"]
files_modified:
  - tools/tttrlib/manifest.yaml
  - tools/tttrlib/schema/tttrlib_api.json
  - tools/tttrlib/schema/tttrlib_coverage.json
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/contract/test_tttrlib_manifest.py
  - tests/contract/test_tttrlib_schema_alignment.py
  - tests/unit/api/test_execution.py
  - tests/unit/test_tttrlib_entrypoint_tttr_methods.py
  - tests/smoke/test_tttrlib_live.py
autonomous: true
requirements: [TTTR-02, TTTR-03, TTTR-04, TTTR-05]
gap_closure: true

must_haves:
  truths:
    - "The public tttrlib export surface includes only Python-safe write methods that match the live runtime, and removed methods fail with TTTRLIB_UNSUPPORTED_METHOD instead of runtime crashes."
    - "Generic TTTR.write never returns a success artifact when the upstream writer produced no file."
    - "Existing path-boundary and extension guardrails remain intact while export subset errors become deterministic."
  artifacts:
    - path: tools/tttrlib/manifest.yaml
      provides: "Export method surface narrowed to the live Python-safe subset."
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "Deterministic export failure handling for TTTR.write and any remaining supported write variants."
    - path: tests/unit/test_tttrlib_entrypoint_tttr_methods.py
      provides: "Regression coverage for missing-file and false-return write failures."
    - path: tests/unit/api/test_execution.py
      provides: "Run-path proof that removed export IDs now fail with TTTRLIB_UNSUPPORTED_METHOD instead of NOT_FOUND or runtime errors."
  key_links:
    - from: tools/tttrlib/manifest.yaml
      to: tools/tttrlib/schema/tttrlib_api.json
      via: "contract-triad updates for the reduced export subset"
      pattern: "write|write_header|write_spc132_events|write_hht3v2_events"
    - from: tools/tttrlib/schema/tttrlib_coverage.json
      to: src/bioimage_mcp/api/execution.py
      via: "plan-06 unsupported routing fallback for removed export IDs"
      pattern: "TTTRLIB_UNSUPPORTED_METHOD|write_header|write_spc132_events"
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/unit/test_tttrlib_entrypoint_tttr_methods.py
      via: "post-write file existence and stable subset-error assertions"
      pattern: "handle_tttr_write|TTTRLIB_UNSUPPORTED_ARGUMENT_PATTERN"
---

<objective>
Close the remaining TTTR export-runtime UAT blocker by narrowing the exported surface to what the live Python binding can actually support.

Purpose: The current MCP contract overstates tttrlib write support, which causes valid sandboxed calls to crash or falsely succeed after guardrails pass.
Output: Manifest/schema/coverage alignment for the real export subset, deterministic generic write failure handling, and regressions proving removed methods now fail cleanly.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-UAT.md
@.planning/phases/25-add-missing-tttr-methods/25-02-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-04-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-06-SUMMARY.md
@.planning/debug/phase-25-uat-gap-3-export-runtime.md
@tools/tttrlib/manifest.yaml
@tools/tttrlib/schema/tttrlib_api.json
@tools/tttrlib/schema/tttrlib_coverage.json
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
@tests/contract/test_tttrlib_manifest.py
@tests/contract/test_tttrlib_schema_alignment.py
@tests/unit/api/test_execution.py
@tests/unit/test_tttrlib_entrypoint_tttr_methods.py
@tests/smoke/test_tttrlib_live.py

<interfaces>
From `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`:
```python
def handle_tttr_write(...):
    tttr.write(str(filepath))
    output = _build_tttr_file_output(filepath, fmt)
    return {"ok": True, "outputs": {"tttr_out": output}}

def handle_tttr_write_header(...):
    tttr.write_header(str(filepath))

def handle_tttr_write_spc132_events(...):
    tttr.write_spc132_events(str(filepath), tttr)
```

From `.planning/debug/phase-25-uat-gap-3-export-runtime.md`:
```text
TTTR.write_spc132_events -> documented as (fp, tttr) with fp: FILE *
TTTR.write_header -> rejects Python strings at runtime
TTTR.write -> can leave no output file for unsupported source/container combinations
```

Treat the debug evidence as the contract source of truth. Do not keep a method surfaced just because an earlier plan assumed it worked.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reclassify unsupported TTTR export methods out of the public MCP surface</name>
  <files>
tools/tttrlib/manifest.yaml
tools/tttrlib/schema/tttrlib_api.json
tools/tttrlib/schema/tttrlib_coverage.json
tests/contract/test_tttrlib_manifest.py
tests/contract/test_tttrlib_schema_alignment.py
tests/unit/api/test_execution.py
  </files>
  <behavior>
    - Test 1: `tttrlib.TTTR.write_header` is no longer discoverable as a supported callable and direct `run()` now returns `TTTRLIB_UNSUPPORTED_METHOD` via the plan-06 fallback.
    - Test 2: `tttrlib.TTTR.write_spc132_events` is also removed or reclassified unless a real Python-safe wrapper is implemented in this plan; the default acceptable closure is stable unsupported-method rejection, not another runtime-only crash.
    - Test 3: If `write_hht3v2_events` has the same non-path-safe live binding shape, it is reclassified in the same patch instead of leaving a sibling contract drift behind.
  </behavior>
  <action>
- Update `manifest.yaml`, `tttrlib_api.json`, and `tttrlib_coverage.json` together so the public export surface only lists write methods that are Python-safe in the installed runtime. Keep strict upstream IDs and preserve the explicit deferred or denied rationale in coverage metadata.
- Reclassify `tttrlib.TTTR.write_header` based on the diagnosed string-binding failure, and reclassify `tttrlib.TTTR.write_spc132_events` unless you also build and test a real FILE*-safe bridge inside this plan. The default for this gap closure is removal from the supported surface, not another shim that still depends on undefined SWIG behavior.
- Audit `tttrlib.TTTR.write_hht3v2_events` against the same live-signature rule before leaving it supported. If its runtime contract is not a normal filename-safe call, reclassify it in the same patch for consistency.
- Add contract assertions that the reduced export subset is what discovery exposes, then extend `tests/unit/api/test_execution.py` so direct execution of the removed IDs now returns `TTTRLIB_UNSUPPORTED_METHOD` rather than `NOT_FOUND` or a transport-level crash.
  </action>
  <verify>
    <automated>pytest tests/contract/test_tttrlib_manifest.py tests/contract/test_tttrlib_schema_alignment.py tests/unit/api/test_execution.py -q</automated>
  </verify>
  <done>
Unsupported export methods are no longer advertised as safe MCP callables, and calling them now produces the stable unsupported-method contract instead of a runtime failure.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make generic TTTR.write fail deterministically when no export file is produced</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_tttr_methods.py
tests/smoke/test_tttrlib_live.py
  </files>
  <behavior>
    - Test 1: If upstream `tttr.write()` returns false or leaves no file on disk, MCP returns a stable `TTTRLIB_UNSUPPORTED_ARGUMENT_PATTERN` error and does not emit a bogus `TTTRRef`.
    - Test 2: Existing unsafe-path and wrong-extension guardrail behavior stays unchanged for any remaining supported export variants.
    - Test 3: Live smoke no longer assumes a positive specialized SPC export; it instead verifies the new stable behavior for the reduced export subset.
  </behavior>
  <action>
- Keep `tttrlib.TTTR.write` surfaced only as a supported subset. After the upstream call, verify the export path now exists before building a `TTTRRef`; if upstream returns an explicit false value or no file appears, convert that result into `TTTRLIB_UNSUPPORTED_ARGUMENT_PATTERN` with guidance that the source TTTR/container combination is unsupported for file export.
- Do not weaken the existing sandbox/path guardrails from `25-02-SUMMARY.md`. Unsafe paths and wrong extensions must continue to fail before any runtime writer call.
- Update `tests/unit/test_tttrlib_entrypoint_tttr_methods.py` so the fake generic writer covers both false-return and no-file cases, and replace the old positive specialized-export assumption in `tests/smoke/test_tttrlib_live.py` with assertions that the remaining export subset either produces a real file or returns the stable subset error.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py -q && pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q -k "specialized_spc_export"</automated>
  </verify>
  <done>
Generic TTTR export never reports success without a real file, and the remaining write-family behavior matches the live runtime instead of the previously overstated contract.
  </done>
</task>

</tasks>

<verification>
- pytest tests/contract/test_tttrlib_manifest.py tests/contract/test_tttrlib_schema_alignment.py tests/unit/api/test_execution.py tests/unit/test_tttrlib_entrypoint_tttr_methods.py -q
- pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q
</verification>

<success_criteria>
- UAT gap 3 is closed by aligning discovery, execution, and runtime behavior around the real Python-safe TTTR export subset.
- Removed export methods reject cleanly with `TTTRLIB_UNSUPPORTED_METHOD`, and `TTTR.write` never returns a ghost artifact for missing files.
- TTTR-02 through TTTR-05 remain satisfied with contract and regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-07-SUMMARY.md`
</output>
