---
phase: 25-add-missing-tttr-methods
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/api/execution.py
  - src/bioimage_mcp/artifacts/metadata.py
  - src/bioimage_mcp/artifacts/preview.py
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/unit/api/test_execution.py
  - tests/unit/test_tttrlib_entrypoint_tttr_methods.py
  - tests/unit/artifacts/test_native_metadata.py
  - tests/unit/artifacts/test_preview.py
autonomous: true
requirements: [TTTR-03, TTTR-05]
gap_closure: true

must_haves:
  truths:
    - "TTTR intensity-trace and selection outputs report numeric table dtypes instead of string fallback labels."
    - "One-column and empty CSV-backed TTTR tables remain usable and retain explicit row_count plus numeric column metadata after core import."
    - "artifact_info table previews only fall back to inferred dtypes that match the CSV contents instead of hardcoded string values."
  artifacts:
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "TTTR table outputs with nested metadata.columns and metadata.row_count for intensity traces and selections."
    - path: src/bioimage_mcp/api/execution.py
      provides: "Core TableRef import that preserves worker-supplied schema during file import."
    - path: src/bioimage_mcp/artifacts/metadata.py
      provides: "Deterministic CSV metadata extraction for one-column and header-only TTTR tables."
    - path: src/bioimage_mcp/artifacts/preview.py
      provides: "Preview dtype inference that reflects numeric CSV content when metadata is absent."
    - path: tests/unit/test_tttrlib_entrypoint_tttr_methods.py
      provides: "Regression coverage for TTTR table metadata emission and imported TableRef dtypes."
  key_links:
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: src/bioimage_mcp/api/execution.py
      via: "worker TableRef outputs must carry nested metadata that survives import_file()"
      pattern: "metadata.*columns|metadata.*row_count|get_intensity_trace|_write_selection_table"
    - from: src/bioimage_mcp/api/execution.py
      to: src/bioimage_mcp/artifacts/metadata.py
      via: "TableRef import merges worker metadata before CSV fallback extraction"
      pattern: "metadata_override|extract_table_metadata|columns|row_count"
    - from: src/bioimage_mcp/artifacts/preview.py
      to: tests/unit/artifacts/test_preview.py
      via: "preview dtype fallback proves numeric columns stay numeric"
      pattern: "generate_table_preview|dtypes"
---

<objective>
Close the remaining TTTR table-metadata regression from Phase 25 UAT.

Purpose: The TTTR selection and intensity-trace flows already return usable artifacts, but the current worker and core import path drop high-fidelity table schema and let preview fallback relabel numeric columns as `string`.
Output: Restored nested TableRef metadata for affected TTTR handlers, deterministic one-column CSV metadata extraction, and regression tests that prove numeric dtypes survive both import and preview paths.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-UAT.md
@.planning/phases/25-add-missing-tttr-methods/25-04-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-06-SUMMARY.md
@.planning/debug/phase25-gap2-table-dtypes.md
@src/bioimage_mcp/api/execution.py
@src/bioimage_mcp/artifacts/metadata.py
@src/bioimage_mcp/artifacts/preview.py
@src/bioimage_mcp/api/artifacts.py
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
@tests/unit/api/test_execution.py
@tests/unit/test_tttrlib_entrypoint_tttr_methods.py
@tests/unit/artifacts/test_native_metadata.py
@tests/unit/artifacts/test_preview.py

<interfaces>
From `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`:
```python
def _write_table_output(...):
    return {
        "type": "TableRef",
        "columns": columns,
        "row_count": int(table.shape[0]),
        "metadata": {
            "columns": [{"name": name, "dtype": "float64"} for name in columns],
            "row_count": int(table.shape[0]),
        },
    }

def _write_selection_table(...):
    return {
        "type": "TableRef",
        "columns": ["index"],
        "row_count": int(selection_indices.size),
    }
```

From `src/bioimage_mcp/api/execution.py`:
```python
tool_metadata = out.get("metadata") or {}
ref = self._artifact_store.import_file(
    p,
    artifact_type=out_type,
    format=fmt,
    metadata_override=tool_metadata,
    ...,
)
```

From `src/bioimage_mcp/artifacts/metadata.py` and `src/bioimage_mcp/artifacts/preview.py`:
```python
def extract_table_metadata(path: Path) -> dict | None:
    dialect = csv.Sniffer().sniff(sample) if sample else None
    reader = csv.DictReader(f, dialect=dialect) if dialect else csv.DictReader(f)

return {
    "table_preview": md,
    "dtypes": {col: "string" for col in header},
}
```

Use `_write_table_output()` as the contract baseline for affected TTTR tables: restore nested metadata instead of inventing a TTTR-only preview path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore authoritative TTTR table schema at the worker and import boundary</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
src/bioimage_mcp/api/execution.py
tests/unit/test_tttrlib_entrypoint_tttr_methods.py
tests/unit/api/test_execution.py
  </files>
  <behavior>
    - Test 1: `tttrlib.TTTR.get_intensity_trace` returns a `TableRef` whose `metadata.columns` carry numeric dtypes and whose imported artifact preserves that schema.
    - Test 2: `_write_selection_table()` emits `metadata.columns` plus `metadata.row_count` for both populated and empty selections, and imported `TableRef` metadata keeps `index` numeric instead of falling back to `string`.
    - Test 3: This fix is additive; existing correlator-style tables that already use `_write_table_output()` remain unchanged.
  </behavior>
  <action>
- Restore the `25-04`/`25-06` table contract in `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`: `handle_get_intensity_trace()` and `_write_selection_table()` must emit nested `metadata.columns` and `metadata.row_count`, not only top-level `columns`/`row_count`.
- Update `src/bioimage_mcp/api/execution.py` so TableRef imports merge top-level `columns` and `row_count` into `tool_metadata` before `ArtifactStore.import_file()`; preserve worker-supplied metadata as authoritative instead of discarding it at the core boundary.
- Add regression coverage in `tests/unit/test_tttrlib_entrypoint_tttr_methods.py` for raw handler outputs and in `tests/unit/api/test_execution.py` for imported artifacts, including the empty-selection case called out in `25-UAT.md`.
- Do not broaden discovery or change TTTR method support. This plan only restores metadata fidelity for already supported TTTR outputs.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py tests/unit/api/test_execution.py -q</automated>
  </verify>
  <done>
Affected TTTR tables now carry explicit numeric schema from the worker through core import, and UAT no longer depends on lossy fallback metadata for intensity traces or selections.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make CSV metadata and preview fallbacks numeric-safe for TTTR tables</name>
  <files>
src/bioimage_mcp/artifacts/metadata.py
src/bioimage_mcp/artifacts/preview.py
tests/unit/artifacts/test_native_metadata.py
tests/unit/artifacts/test_preview.py
  </files>
  <behavior>
    - Test 1: `extract_table_metadata()` returns deterministic `columns` and `row_count` for one-column and header-only selection CSVs instead of `None` or malformed string-only columns.
    - Test 2: `generate_table_preview()` reports numeric dtypes for numeric CSV columns when preview fallback is used, including a one-column numeric table.
    - Test 3: Existing multi-column preview behavior stays intact aside from the dtype labels becoming accurate.
  </behavior>
  <action>
- Harden `src/bioimage_mcp/artifacts/metadata.py` so single-column and header-only CSVs do not depend entirely on `csv.Sniffer()`. Use a deterministic header-first parsing path that still infers `int64`/`float64` from the first data row when present.
- Replace the hardcoded `string` preview dtypes in `src/bioimage_mcp/artifacts/preview.py` with lightweight inference aligned to the metadata helper, so preview fallback matches real CSV contents instead of masking numeric tables as strings.
- Extend `tests/unit/artifacts/test_native_metadata.py` and `tests/unit/artifacts/test_preview.py` with one-column numeric and header-only TTTR-style CSV fixtures that lock in the diagnosed regression.
- Keep this generic helper fix additive. Do not add TTTR-only special cases in preview or metadata extraction.
  </action>
  <verify>
    <automated>pytest tests/unit/artifacts/test_native_metadata.py tests/unit/artifacts/test_preview.py -q</automated>
  </verify>
  <done>
Numeric CSV-backed TTTR tables still advertise numeric dtypes even when preview fallback or generic metadata extraction is exercised.
  </done>
</task>

</tasks>

<verification>
- pytest tests/unit/test_tttrlib_entrypoint_tttr_methods.py tests/unit/api/test_execution.py tests/unit/artifacts/test_native_metadata.py tests/unit/artifacts/test_preview.py -q
</verification>

<success_criteria>
- UAT gap 2 no longer reports `string` dtypes for numeric TTTR intensity-trace or selection artifacts.
- Worker-supplied TTTR table schema survives file import, and fallback CSV helpers remain correct for one-column and empty tables.
- TTTR-03 and TTTR-05 stay satisfied through focused regression coverage rather than broad behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-09-SUMMARY.md`
</output>
