---
phase: 25-add-missing-tttr-methods
plan: 11
type: execute
wave: 3
depends_on: ["25-10"]
files_modified:
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tests/unit/test_tttrlib_entrypoint_clsm_methods.py
  - tests/smoke/test_tttrlib_live.py
autonomous: true
requirements: [TTTR-03, TTTR-05]
gap_closure: true

must_haves:
  truths:
    - "tttrlib.CLSMImage.get_settings returns JSON metadata without SWIG transport fields `this` or `thisown`."
    - "The cleaned CLSM settings artifact still keeps useful domain fields and nested JSON-safe values."
    - "Unit and live smoke coverage both fail if SWIG transport keys leak back into the payload."
  artifacts:
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "Recursive CLSM settings serializer with transport-field filtering."
    - path: tests/unit/test_tttrlib_entrypoint_clsm_methods.py
      provides: "Regression coverage for fake CLSM settings objects that include `this` and `thisown`."
    - path: tests/smoke/test_tttrlib_live.py
      provides: "Live artifact-content assertions rejecting SWIG transport keys."
  key_links:
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/unit/test_tttrlib_entrypoint_clsm_methods.py
      via: "serializer blocklist proves transport attrs are removed while nested values remain"
      pattern: "_normalize_json_safe_value|this|thisown"
    - from: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      to: tests/smoke/test_tttrlib_live.py
      via: "live get_settings artifact assertions reject transport keys"
      pattern: "CLSMImage.get_settings|this|thisown"
---

<objective>
Close the remaining CLSM settings payload-cleanliness regression from Phase 25 UAT.

Purpose: The worker still serializes every public SWIG proxy attribute, so `get_settings` leaks transport-only keys even though the intended Phase 25 behavior was to keep domain metadata and strip wrapper internals.
Output: Restored transport-field filtering in the CLSM serializer plus unit and smoke regressions that explicitly reject `this` and `thisown`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-add-missing-tttr-methods/25-CONTEXT.md
@.planning/phases/25-add-missing-tttr-methods/25-RESEARCH.md
@.planning/phases/25-add-missing-tttr-methods/25-UAT.md
@.planning/phases/25-add-missing-tttr-methods/25-05-SUMMARY.md
@.planning/phases/25-add-missing-tttr-methods/25-08-SUMMARY.md
@.planning/debug/phase25-gap4-settings-swig.md
@tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
@tests/unit/test_tttrlib_entrypoint_clsm_methods.py
@tests/smoke/test_tttrlib_live.py

<interfaces>
From `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`:
```python
def _normalize_json_safe_value(value: Any, *, _seen: set[int] | None = None) -> Any:
    for attr_name in sorted(name for name in dir(value) if not name.startswith("_")):
        attr_value = getattr(value, attr_name)
        if callable(attr_value):
            continue
        public_attrs[attr_name] = _normalize_json_safe_value(attr_value, _seen=_seen | {obj_id})

def _serialize_clsm_settings(settings: Any) -> dict[str, Any]:
    payload = _normalize_json_safe_value(settings)
```

From `25-08-SUMMARY.md`:
```text
Exclude only SWIG transport attrs this and thisown during CLSM settings normalization so real domain metadata continues to pass through recursively.
```

Restore that exact cleanup contract; do not replace rich CLSM settings output with a narrow whitelist or stringified proxy repr.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore transport-field filtering in CLSM settings serialization</name>
  <files>
tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
tests/unit/test_tttrlib_entrypoint_clsm_methods.py
  </files>
  <behavior>
    - Test 1: A fake CLSM settings object that contains domain fields plus `this` and `thisown` serializes into a dict that keeps the domain fields and drops only the transport attrs.
    - Test 2: Nested vector-like values and scalar wrappers still normalize into JSON-safe Python data after the filter is applied.
  </behavior>
  <action>
- Restore the narrow SWIG transport-attribute blocklist from the diagnosed `25-08` cleanup in `tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py`. `this` and `thisown` must always be skipped during recursive attribute traversal.
- Keep the broader recursive normalization behavior from `25-05-SUMMARY.md` intact so live CLSM settings payloads remain rich and JSON-safe instead of collapsing to strings or a tiny whitelist.
- Reinstate unit regressions in `tests/unit/test_tttrlib_entrypoint_clsm_methods.py` that explicitly model fake settings objects with `this`/`thisown` and verify those keys are absent from the serialized result.
  </action>
  <verify>
    <automated>pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q</automated>
  </verify>
  <done>
`CLSMImage.get_settings` once again emits clean JSON metadata with real domain content preserved and SWIG transport fields removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reinstate live smoke checks for CLSM payload cleanliness</name>
  <files>
tests/smoke/test_tttrlib_live.py
  </files>
  <action>
- Strengthen the live `tttrlib.CLSMImage.get_settings` smoke path so it opens the returned JSON artifact, asserts the payload is a dict, explicitly rejects keys `this` and `thisown`, and still checks for at least one expected domain field.
- Keep the neighboring `get_image_info` assertions unchanged as the clean baseline for CLSM metadata methods.
- Mirror the exact user-observed failure in the smoke name or assertion message so future regressions are obvious in CI output.
  </action>
  <verify>
    <automated>pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q -k "clsm_metadata_methods"</automated>
  </verify>
  <done>
Live smoke coverage now blocks the specific regression where SWIG proxy transport keys leak into CLSM settings artifacts.
  </done>
</task>

</tasks>

<verification>
- pytest tests/unit/test_tttrlib_entrypoint_clsm_methods.py -q
- pytest tests/smoke/test_tttrlib_live.py -m smoke_extended -q
</verification>

<success_criteria>
- UAT gap 4 closes with both unit and live assertions that `this` and `thisown` are absent from `get_settings` payloads.
- TTTR-03 and TTTR-05 remain satisfied while preserving the richer CLSM metadata contract established earlier in Phase 25.
</success_criteria>

<output>
After completion, create `.planning/phases/25-add-missing-tttr-methods/25-11-SUMMARY.md`
</output>
