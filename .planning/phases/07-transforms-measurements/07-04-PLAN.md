---
phase: 07-transforms-measurements
plan: 4
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - tools/base/manifest.yaml
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
  - tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
autonomous: true

must_haves:
  truths:
    - "FFT and Fourier-domain filters can operate on complex images represented as OME-TIFF artifacts"
    - "IFFT can return a real-valued image artifact to complete complex->real workflows"
  artifacts:
    - path: tools/base/manifest.yaml
      provides: "Dynamic discovery includes scipy.fft functions under the scipy prefix"
      contains: "scipy.fft"
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      provides: "Complex artifact encoding/decoding and Fourier workflow support"
      contains: "fourier"
  key_links:
    - from: tools/base/manifest.yaml
      to: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      via: "dynamic_sources(adapter=scipy)"
      pattern: "modules:"
---

<objective>
Enable Phase 7 Fourier workflows by exposing `scipy.fft` functions and supporting complex-valued images as artifacts (real/imag channels), including a practical complex->real IFFT behavior.

Purpose: NDIMG-05 requires Fourier-domain filters and reliable complex handling across artifact boundaries.
Output: `scipy.fft` discovery + complex encoding/decoding in adapter + unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-transforms-measurements/07-CONTEXT.md
@.planning/phases/07-transforms-measurements/07-RESEARCH.md

@tools/base/manifest.yaml
@src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
@tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
</context>

<tasks>

<task type="auto">
  <name>Expose scipy.fft via dynamic discovery and add ifftn real_output overlay</name>
  <files>tools/base/manifest.yaml</files>
  <action>
Update the scipy dynamic source in `tools/base/manifest.yaml`:

- Add `scipy.fft` to the `modules:` list for the `adapter: scipy` / `prefix: scipy` dynamic source.

Add a function overlay for `base.scipy.fft.ifftn` to support a practical complex->real transition:

- In `function_overlays`, add `base.scipy.fft.ifftn` with a `params_override` that introduces:
  - `real_output` (boolean, default true): "If true, return only the real component after inverse FFT."

Do not change any other manifest behavior.
  </action>
  <verify>python -c "import yaml; from pathlib import Path; d=yaml.safe_load(Path('tools/base/manifest.yaml').read_text()); assert any(ds.get('adapter')=='scipy' and 'scipy.fft' in (ds.get('modules') or []) for ds in (d.get('dynamic_sources') or [])); assert 'base.scipy.fft.ifftn' in (d.get('function_overlays') or {}); print('ok')"</verify>
  <done>Dynamic discovery can include scipy.fft functions and ifftn has a documented `real_output` param override.</done>
</task>

<task type="auto">
  <name>Support complex image artifacts for fft/fourier filters/ifft</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</files>
  <action>
Extend `ScipyNdimageAdapter.execute()` to support complex-valued workflows:

- Complex artifact encoding (output):
  - If the scipy result is a complex ndarray, write it as a 2-channel real-valued image:
    - Channel 0: real
    - Channel 1: imag
  - Ensure `metadata_override.channel_names` is set to `["real", "imag"]`.
  - Ensure the output axes include a `C` dimension (e.g., YX -> CYX; ZYX -> CZYX; TCZYX stays TCZYX but with C==2).

- Complex artifact decoding (input):
  - If the primary input has `channel_names` indicating complex components (case-insensitive match for real/imag) and C==2, recombine into a complex array before calling scipy.

- Apply this behavior for:
  - `scipy.fft.*` functions (fftn/ifftn and friends)
  - `scipy.ndimage.fourier_*` filters

- Implement `real_output` support for `scipy.fft.ifftn`:
  - Pop `real_output` from params (default true).
  - If true, return `result.real` as a standard real-valued BioImageRef.
  - If false, return complex-encoded 2-channel output.

Do not introduce new external deps; keep imports lazy (bioio only inside methods).
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</verify>
  <done>Complex arrays round-trip across artifact boundaries and ifftn can optionally return a real-valued image.</done>
</task>

<task type="auto">
  <name>Unit tests: complex encoding/decoding and ifftn real_output behavior</name>
  <files>tests/unit/registry/dynamic/test_scipy_ndimage_execute.py</files>
  <action>
Add hermetic tests (use the existing `mock_module` side_effect) covering:

- Complex output encoding:
  - Mock `scipy.fft.fftn` to return a complex array.
  - Assert adapter returns a BioImageRef whose metadata includes `channel_names == ["real", "imag"]` and whose axes include `C`.
  - File round-trip (checker requirement): read written OME-TIFF metadata from disk and assert it matches.
    Use the same helper as Phase 06-04:
    - `from bioimage_mcp.artifacts.metadata import extract_image_metadata`
    - `file_meta = extract_image_metadata(Path(out["path"]))`
    - Assert `"C" in file_meta["axes"]` and `[c.lower() for c in file_meta["channel_names"]] == ["real", "imag"]`.

- Complex input decoding:
  - Provide an input artifact whose data is 2-channel (real/imag) and whose `channel_names` indicate complex.
  - Mock a Fourier-domain function and assert it receives a complex dtype array.

- ifftn real_output:
  - Mock `ifftn` to return a complex array with non-zero imaginary part.
  - With `params={"real_output": true}` assert output is a real-valued BioImageRef (no complex channel_names).
  - With `params={"real_output": false}` assert output is 2-channel real/imag encoding.
  </action>
  <verify>pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -k "fft or fourier or ifft" -q</verify>
  <done>Tests pass and validate complex handling and complex->real transition for ifftn.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -k "fft or fourier or ifft" -q`
</verification>

<success_criteria>
- Agents can compose FFT -> Fourier filter -> IFFT workflows using only artifacts.
- Complex images are represented as channelized OME-TIFF with real/imag channels, and IFFT can return a real image.
</success_criteria>

<output>
After completion, create `.planning/phases/07-transforms-measurements/07-04-SUMMARY.md`
</output>
