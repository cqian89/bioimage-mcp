---
phase: 07-transforms-measurements
plan: 3
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
  - tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
autonomous: true

must_haves:
  truths:
    - "scipy.ndimage.label returns a LabelImageRef plus a counts JSON artifact"
    - "Measurement functions return JSON keyed by label id, with missing labels represented as null"
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      provides: "Multi-output label handling and measurement JSON serialization"
      contains: "_save_json"
    - path: tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
      provides: "Regression tests for label + measurement JSON outputs"
  key_links:
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      to: "NativeOutputRef(format=json)"
      via: "measurement normalization -> _save_json"
      pattern: "NativeOutputRef"
    - from: src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
      to: "inputs named labels"
      via: "named input artifact -> params['labels']"
      pattern: "params\[name\]"
---

<objective>
Implement Phase 7 measurement outputs for scipy.ndimage by producing stable JSON artifacts and multi-output labeling.

Purpose: Enable downstream logic to consume label counts and per-label measurements directly from JSON artifacts.
Output: Label -> (labels image + counts json) and measurement functions -> JSON keyed by label id.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-transforms-measurements/07-CONTEXT.md
@.planning/phases/07-transforms-measurements/07-RESEARCH.md

@src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
@tests/unit/registry/dynamic/test_scipy_ndimage_execute.py
</context>

<tasks>

<task type="auto">
  <name>Add JSON serialization helper for measurement payloads</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</files>
  <action>
Add a dedicated helper for JSON artifact output (do NOT reuse `_save_scalar`, which enforces `{value: ...}`):

- Implement `_save_json(payload: Any, work_dir: Path | None = None, filename: str = "output.json") -> dict`
  - Writes `payload` as-is to JSON
  - Returns a `NativeOutputRef` dict with `format: json`
  - Include minimal metadata such as `{"schema": "scipy.ndimage.measurement.v1"}` if helpful

This helper will be used for `label` counts and all NDIMG-04 measurement JSON outputs.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</verify>
  <done>Adapter can write arbitrary JSON payloads as `NativeOutputRef(format=json)` without wrapping them in `{value: ...}`.</done>
</task>

<task type="auto">
  <name>Implement label multi-output and per-label measurement JSON normalization</name>
  <files>src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py</files>
  <action>
Update `ScipyNdimageAdapter.execute()` for Phase 7 measurement behavior:

1) `scipy.ndimage.label`:
   - When the scipy return is a 2-tuple `(label_image, num_features)`, return TWO artifacts:
     - `labels`: OME-TIFF image with `type="LabelImageRef"`
     - `counts`: JSON artifact with at least `{ "num_features": <int> }`
   - Prevent output overwrites when `work_dir` is provided:
     - Add `filename: str = "output.ome.tiff"` to `_save_image(...)` and use it when computing the `path` under `work_dir`.
     - Use distinct filenames for the two outputs: `labels.ome.tiff` and `counts.json`.

2) NDIMG-04 measurement functions (minimum set):
   - `center_of_mass`, `sum`, `mean`, `extrema`
   - Normalize outputs into a dict keyed by label id string:
     - Detect present labels from the provided `labels` array:
       - `present = set(np.unique(labels_array).tolist()) - {0}`
     - Normalize requested label ids:
       - If `params` includes `index` (scalar or sequence): `requested = [int(...)]` in the same order as provided.
       - Else (no `index`): treat `requested` as the sorted present labels (so output is stable and never includes "missing" by default).
     - Missing-label behavior (BLOCKER requirement):
       - For any `requested` label id not in `present`, set output value to `None` (serializes as JSON `null`).
       - Do this explicitly regardless of scipy return shape (do not rely on scipy to emit NaN/0 for missing labels).
     - Map scipy returns to `requested` labels:
       - If `len(requested) == 1`: always emit `{ "<id>": value }`.
       - If scipy returns a sequence with the same length as `requested`: zip in order.
       - If scipy returns a scalar/tuple for a single requested label, treat it as that label's value.
     - JSON safety:
       - Coerce numpy scalars/arrays into JSON-native types.
       - Coerce any NaN values (including inside tuples/lists) to `None` so JSON is valid and "missing" is represented consistently.

   - Input wiring: these measurements must accept a `labels` input artifact (LabelImageRef) in `inputs`.
     The adapter already supports auxiliary artifacts by name; rely on that to pass `labels=<ndarray>` into scipy.

Always emit measurement results using `_save_json()` (not `_save_scalar()`).

Keep existing generic behavior for all other scipy functions.
  </action>
  <verify>pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -k "label or center_of_mass or extrema" -q</verify>
  <done>
- `label` returns `[LabelImageRef, NativeOutputRef]`.
- Measurement functions return JSON dicts keyed by label id, with missing labels represented as null.
  </done>
</task>

<task type="auto">
  <name>Unit tests: label outputs + measurement JSON shape (including missing labels)</name>
  <files>tests/unit/registry/dynamic/test_scipy_ndimage_execute.py</files>
  <action>
Extend `tests/unit/registry/dynamic/test_scipy_ndimage_execute.py` with hermetic tests using the `mock_module` fixture:

- `test_execute_label_returns_labels_and_counts_json`:
  - Mock `label` to return `(labels_array, 2)`.
  - Assert output includes one `LabelImageRef` and one `NativeOutputRef(format=json)`.
  - Assert returned output paths are distinct and both files exist under `tmp_path`.
  - Assert the filenames are stable: `labels.ome.tiff` and `counts.json`.
  - Read counts JSON and assert `num_features == 2`.

- `test_execute_center_of_mass_json_index_and_missing_labels`:
  - Mock `center_of_mass` to return values consistent with the requested `index`.
  - Call with `params={"index": [1, 3]}` and provide BOTH:
    - `image` artifact input (intensity image)
    - `labels` artifact input
  - Assert JSON payload keys include "1" and "3" and that missing labels are `null`.
  - IMPORTANT (BLOCKER wiring proof): assert the mocked `center_of_mass` was called with `labels=<ndarray>` from the `labels` input artifact (fail if not).

Prefer validating JSON payload structure over exact floating values.
  </action>
  <verify>pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -k "label or center_of_mass" -q</verify>
  <done>Tests pass and fail if output typing or JSON schema regresses.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/dynamic/test_scipy_ndimage_execute.py -k "label or center_of_mass or extrema" -q`
</verification>

<success_criteria>
- Labeling and measurement functions produce JSON artifacts designed for downstream composition.
- Missing labels do not error; they serialize to null.
</success_criteria>

<output>
After completion, create `.planning/phases/07-transforms-measurements/07-03-SUMMARY.md`
</output>
