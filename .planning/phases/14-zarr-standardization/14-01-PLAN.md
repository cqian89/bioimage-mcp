---
phase: 14-zarr-standardization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/dynamic/io_bridge.py
  - src/bioimage_mcp/api/execution.py
  - src/bioimage_mcp/registry/dynamic/adapters/xarray.py
  - src/bioimage_mcp/registry/dynamic/adapters/skimage.py
  - src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
  - src/bioimage_mcp/registry/dynamic/adapters/phasorpy.py
  - tools/base/bioimage_mcp_base/entrypoint.py
  - tools/base/bioimage_mcp_base/ops/io.py
  - tools/base/manifest.yaml
  - specs/014-native-artifact-types/contracts/artifact-metadata-schema.json
  - tests/contract/test_artifact_metadata_schema_allows_custom_dims.py
  - tests/unit/api/test_execution_storage_materialization.py
  - tests/integration/test_cross_env_handoff.py
  - tests/integration/test_storage_type_materialization.py
  - tests/smoke/test_equivalence_xarray.py
autonomous: true

must_haves:
  truths:
    - "IOBridge defaults to OME-Zarr for interchange when no format is specified."
    - "Core can materialize directory-backed OME-Zarr artifacts by using import_directory."
    - "Xarray adapter and base.export prefer OME-Zarr over OME-TIFF by default."
    - "Skimage/Scipy/Phasorpy adapters default to OME-Zarr outputs unless explicitly overridden."
    - "Metadata.dims preserves native axis names (no character-split TCZYX padding)."
    - "Artifact metadata schema allows multi-character dims and >5D for OME-Zarr."
  artifacts:
    - path: src/bioimage_mcp/registry/dynamic/io_bridge.py
      provides: "DEFAULT_INTERCHANGE_FORMAT = 'OME-Zarr' and .zarr extension logic"
    - path: src/bioimage_mcp/api/execution.py
      provides: "_materialize_zarr_to_file handles directories via import_directory"
    - path: tools/base/bioimage_mcp_base/entrypoint.py
      provides: "handle_materialize supports OME-Zarr as default and correctly writes it"
  key_links:
    - from: src/bioimage_mcp/api/execution.py
      to: src/bioimage_mcp/artifacts/store.py
      via: "import_directory for directory-backed materialization"
---

<objective>
Standardize OME-Zarr as the primary interchange format across the core server and base tools, and fix materialization issues for directory-backed artifacts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/026-zarr-artifact/proposal.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update IOBridge to default to OME-Zarr</name>
  <files>src/bioimage_mcp/registry/dynamic/io_bridge.py</files>
  <action>
- Change `DEFAULT_INTERCHANGE_FORMAT` from `"OME-TIFF"` to `"OME-Zarr"`.
- Update `create_materialization_path` extension logic to prefer `.ome.zarr` (or `.zarr`) for OME-Zarr.
  </action>
  <verify>grep "DEFAULT_INTERCHANGE_FORMAT = \"OME-Zarr\"" src/bioimage_mcp/registry/dynamic/io_bridge.py</verify>
  <done>
IOBridge now negotiates OME-Zarr by default.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Core materialization for directory-backed artifacts</name>
  <files>src/bioimage_mcp/api/execution.py</files>
  <action>
- Update `_materialize_zarr_to_file` to support directories.
- Instead of raising `ValueError` when `src_path.is_dir()`, use `self._artifact_store.import_directory` if `src_path` is a directory.
- Update `_materialize_memory_artifact_via_worker` or the cross-env handoff logic to ensure that if a worker materializes to a directory, core correctly imports it.
- In `ExecutionService.run_workflow`, ensure `_process_handoff` correctly handles the imported directory artifact.
  </action>
  <verify>python -m compileall src/bioimage_mcp/api/execution.py</verify>
  <done>
Core can now import directory-backed artifacts during materialization.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Worker Materialization to handle OME-Zarr by default</name>
  <files>tools/base/bioimage_mcp_base/entrypoint.py</files>
  <action>
- Update `handle_materialize` to default to OME-Zarr if `target_format` is not specified or set to the new default.
- Ensure `OMEZarrWriter` is used correctly with native axes.
- Persist native axes/dims metadata alongside `_MEMORY_ARTIFACTS` (or pass it through the request) so materialization uses correct axes, not just shape inference.
- Update `_convert_memory_inputs_to_files` if it also needs to prefer OME-Zarr for temporary handoffs.
  </action>
  <verify>python -m compileall tools/base/bioimage_mcp_base/entrypoint.py</verify>
  <done>
Worker materialization now supports and prefers OME-Zarr.
  </done>
</task>

<task type="auto">
  <name>Task 4: Standardize Xarray Adapter and Base Export to OME-Zarr</name>
  <files>
    src/bioimage_mcp/registry/dynamic/adapters/xarray.py
    tools/base/bioimage_mcp_base/ops/io.py
  </files>
  <action>
- Update `XarrayAdapterForRegistry._save_output` in `xarray.py` to prefer OME-Zarr over OME-TIFF.
- Update `XarrayAdapterForRegistry._save_output` in `xarray.py` to prefer OME-Zarr over OME-TIFF.
- Update `export` in `tools/base/bioimage_mcp_base/ops/io.py` so the default format is `"OME-Zarr"` when not explicitly provided.
- Ensure OME-TIFF is only used when requested or required by a tool manifest.
  </action>
  <verify>grep "fmt = \"OME-Zarr\"" src/bioimage_mcp/registry/dynamic/adapters/xarray.py</verify>
  <done>
Xarray operations and base export now default to OME-Zarr.
  </done>
</task>

<task type="auto">
  <name>Task 5: Standardize Dynamic Adapters to OME-Zarr Outputs</name>
  <files>
    src/bioimage_mcp/registry/dynamic/adapters/skimage.py
    src/bioimage_mcp/registry/dynamic/adapters/scipy_ndimage.py
    src/bioimage_mcp/registry/dynamic/adapters/phasorpy.py
  </files>
  <action>
- Update adapter output writers to default to OME-Zarr when writing BioImageRef/LabelImageRef artifacts.
- Ensure any OME-TIFF output is only used when the tool explicitly requests it.
- Preserve native axes in metadata and avoid TCZYX padding assumptions.
  </action>
  <verify>grep "OME-Zarr" src/bioimage_mcp/registry/dynamic/adapters/skimage.py</verify>
  <done>
Core dynamic adapters default to OME-Zarr outputs.
  </done>
</task>

<task type="auto">
  <name>Task 6: Update Base Manifest Supported Storage Types</name>
  <files>tools/base/manifest.yaml</files>
  <action>
- Add `supported_storage_types: ["file", "zarr-temp"]` to key functions in `tools/base/manifest.yaml`.
- Ensure `base.io.bioimage.export` and other I/O functions explicitly support `zarr-temp`.
  </action>
  <verify>grep "zarr-temp" tools/base/manifest.yaml</verify>
  <done>
Tool manifests now explicitly allow OME-Zarr (zarr-temp) inputs.
  </done>
</task>

<task type="auto">
  <name>Task 7: Update Artifact Metadata Schema for Native Axes</name>
  <files>
    specs/014-native-artifact-types/contracts/artifact-metadata-schema.json
    tests/contract/test_artifact_metadata_schema_allows_custom_dims.py
  </files>
  <action>
- Relax `dims` constraints to allow multi-character axis names (e.g., "bins").
- Allow `shape`/`ndim` lengths greater than 5 for OME-Zarr artifacts.
- Keep OME-TIFF constraints intact where appropriate to avoid regression.
- Extend contract tests to validate multi-character axis names for OME-Zarr.
  </action>
  <verify>jq '.definitions.ImageMetadata.properties.dims' specs/014-native-artifact-types/contracts/artifact-metadata-schema.json</verify>
  <done>
Artifact metadata schema supports native axes for OME-Zarr.
  </done>
</task>

<task type="auto">
  <name>Task 8: Update Tests for OME-Zarr Defaults and Handoff</name>
  <files>
    tests/unit/api/test_execution_storage_materialization.py
    tests/integration/test_cross_env_handoff.py
    tests/integration/test_storage_type_materialization.py
    tests/smoke/test_equivalence_xarray.py
  </files>
  <action>
- Update expectations to allow OME-Zarr directory outputs by default.
- Add regression coverage for directory-backed materialization and cross-env handoff.
- Remove TCZYX padding assumptions from equivalence assertions.
  </action>
  <verify>pytest tests/integration/test_cross_env_handoff.py -k "zarr" -v</verify>
  <done>
Tests are updated to support the new OME-Zarr standard.
  </done>
</task>

</tasks>

<verification>
- ruff check .
- pytest tests/unit/api/test_execution_storage_materialization.py
- pytest tests/integration/test_cross_env_handoff.py
- pytest tests/integration/test_storage_type_materialization.py
- pytest tests/smoke/test_equivalence_xarray.py
- pytest tests/contract/test_artifact_metadata_schema_allows_custom_dims.py
- bioimage-mcp doctor
</verification>

<success_criteria>
- Multi-dimensional workflows (e.g., xarray transpose) produce OME-Zarr artifacts by default.
- Cross-environment handoff of OME-Zarr artifacts works without "Core cannot materialize directory-backed artifacts" errors.
- Native axis names are preserved in artifact metadata.
</success_criteria>

<output>
After completion, create `.planning/phases/14-zarr-standardization/14-01-SUMMARY.md`
</output>
