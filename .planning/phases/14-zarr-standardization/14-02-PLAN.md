---
phase: 14-zarr-standardization
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - envs/bioimage-mcp-base.yaml
  - envs/bioimage-mcp-cellpose.yaml
  - envs/bioimage-mcp-tttrlib.yaml
  - envs/bioimage-mcp-base.lock.yml
  - envs/bioimage-mcp-cellpose.lock.yml
  - tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
  - tools/tttrlib/manifest.yaml
  - tools/cellpose/manifest.yaml
  - tools/cellpose/bioimage_mcp_cellpose/entrypoint.py
  - tools/cellpose/bioimage_mcp_cellpose/ops/segment.py
  - tools/cellpose/bioimage_mcp_cellpose/ops/denoise.py
  - tools/cellpose/bioimage_mcp_cellpose/ops/utils.py
  - tests/smoke/test_tttrlib_live.py
  - tests/smoke/test_cellpose_pipeline_live.py
autonomous: true

must_haves:
  truths:
    - "tttrlib produces OME-Zarr for fluorescence decay with explicit 'bins' axis."
    - "Cellpose inputs accept OME-Zarr and are converted to OME-TIFF internally when required."
    - "Cellpose outputs default to OME-Zarr unless explicitly overridden."
    - "Smoke tests validate OME-Zarr + custom axis names."
  artifacts:
    - path: tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py
      provides: "get_fluorescence_decay writes OME-Zarr with bins axis"
---

<objective>
Update tttrlib and Cellpose tool packs to align with the OME-Zarr standardization, specifically using OME-Zarr for multi-dimensional data like FLIM decay.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@specs/026-zarr-artifact/proposal.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure tool environments include bioio-ome-zarr by default</name>
  <files>
    envs/bioimage-mcp-base.yaml
    envs/bioimage-mcp-cellpose.yaml
    envs/bioimage-mcp-tttrlib.yaml
    envs/bioimage-mcp-base.lock.yml
    envs/bioimage-mcp-cellpose.lock.yml
  </files>
  <action>
- Add `bioio-ome-zarr` to base, cellpose, and tttrlib env definitions.
- Regenerate lockfiles to keep environments reproducible.
  </action>
  <verify>grep "bioio-ome-zarr" envs/bioimage-mcp-base.yaml</verify>
  <done>
All tool envs ship with the OME-Zarr reader/writer plugin.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tttrlib to use OME-Zarr for decay outputs</name>
  <files>tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py</files>
  <action>
- Replace `OmeTiffWriter` usage in `get_fluorescence_decay` with `OMEZarrWriter`.
- Use explicit axis name `"bins"` (or `"microtime"`) for the decay dimension instead of hijacking `"T"`.
- Remove `microtime_axis: "T"` hint from metadata as it is no longer needed with OME-Zarr.
- Ensure pixel sizes and units are preserved if possible.
  </action>
  <verify>python -m compileall tools/tttrlib/bioimage_mcp_tttrlib/entrypoint.py</verify>
  <done>
tttrlib now produces semantically correct OME-Zarr decay artifacts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update Cellpose outputs and conversion utilities</name>
  <files>
    tools/cellpose/bioimage_mcp_cellpose/entrypoint.py
    tools/cellpose/bioimage_mcp_cellpose/ops/segment.py
    tools/cellpose/bioimage_mcp_cellpose/ops/denoise.py
    tools/cellpose/bioimage_mcp_cellpose/ops/utils.py
  </files>
  <action>
- Default Cellpose outputs to OME-Zarr for labels and denoised images.
- Keep internal conversion from OME-Zarr input to OME-TIFF when required by Cellpose runtime.
- Preserve native axes and avoid TCZYX padding assumptions in outputs.
  </action>
  <verify>grep "OME-Zarr" tools/cellpose/bioimage_mcp_cellpose/ops/segment.py</verify>
  <done>
Cellpose outputs follow the OME-Zarr standard while preserving compatibility.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update tttrlib and Cellpose manifests</name>
  <files>
    tools/tttrlib/manifest.yaml
    tools/cellpose/manifest.yaml
  </files>
  <action>
- Update `tttrlib` manifest to specify `format: OME-Zarr` for decay outputs.
- Update `cellpose` manifest inputs to accept `supported_storage_types: ["file", "zarr-temp"]`.
- Update `cellpose` output formats to `OME-Zarr` by default where applicable.
  </action>
  <verify>grep "OME-Zarr" tools/cellpose/manifest.yaml</verify>
  <done>
Tool manifests are updated to handle the new OME-Zarr default.
  </done>
</task>

<task type="auto">
  <name>Task 5: Update smoke tests for OME-Zarr outputs</name>
  <files>
    tests/smoke/test_tttrlib_live.py
    tests/smoke/test_cellpose_pipeline_live.py
  </files>
  <action>
- Update assertions to expect `format="OME-Zarr"`.
- Validate that the output has the expected axis names (e.g., `"bins"`).
- Remove checks for `microtime_axis` hint.
  </action>
  <verify>pytest tests/smoke/test_tttrlib_live.py -v</verify>
  <done>
Smoke tests verify the OME-Zarr standardization for TTTR and Cellpose.
  </done>
</task>

</tasks>

<verification>
- pytest tests/smoke/test_tttrlib_live.py
- pytest tests/smoke/test_cellpose_pipeline_live.py
- bioimage-mcp doctor
</verification>

<success_criteria>
- tttrlib workflows produce OME-Zarr with `bins` axis.
- Cellpose workflows accept OME-Zarr inputs and produce OME-Zarr outputs by default.
</success_criteria>

<output>
After completion, create `.planning/phases/14-zarr-standardization/14-02-SUMMARY.md`
</output>
