---
phase: 12-core-engine-ast-first
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - envs/bioimage-mcp-base.yaml
  - envs/bioimage-mcp-base.lock.yml
  - src/bioimage_mcp/runtimes/introspect.py
  - tests/unit/runtimes/test_introspect.py
autonomous: true

must_haves:
  truths:
    - "meta.describe-derived params_schema reflects type hints + docstrings, with correct required fields."
    - "Artifact ports are excluded from params_schema emission."
    - "Schema output is deterministic across runs."
  artifacts:
    - path: "src/bioimage_mcp/runtimes/introspect.py"
      provides: "Pydantic v2 TypeAdapter-based schema emission with artifact omission + docstring parsing."
    - path: "tests/unit/runtimes/test_introspect.py"
      provides: "Unit coverage for required/default/type/docstring behaviors."
  key_links:
    - from: "src/bioimage_mcp/runtimes/introspect.py"
      to: "pydantic.TypeAdapter"
      via: "json_schema generation"
      pattern: "TypeAdapter\("

---

<objective>
Modernize runtime introspection schema generation so runtime fallback produces high-fidelity, stable JSON Schema.

Purpose: Make the fallback path reliable for dynamic libraries and keep schemas stable for caching and contract tests.
Output: Updated `introspect_python_api()` implementation + updated unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-core-engine-ast-first/12-CONTEXT.md
@.planning/phases/12-core-engine-ast-first/12-RESEARCH.md

@src/bioimage_mcp/runtimes/meta_protocol.py
@tools/base/bioimage_mcp_base/entrypoint.py
@src/bioimage_mcp/runtimes/introspect.py
@tests/unit/runtimes/test_introspect.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update base tool env spec for docstring-parser</name>
  <files>
envs/bioimage-mcp-base.yaml
envs/bioimage-mcp-base.lock.yml
  </files>
  <action>
Plan 12-02 adds runtime docstring parsing in `src/bioimage_mcp/runtimes/introspect.py`, which is executed inside tool-pack environments (e.g., `tools/base/.../entrypoint.py` imports it during `meta.describe`).

Update the base tool environment spec so `docstring-parser` is present in that runtime:

- Add `docstring-parser` to `envs/bioimage-mcp-base.yaml` (prefer the existing `pip:` section so it is included in lock generation).
- Regenerate `envs/bioimage-mcp-base.lock.yml` so it includes the new pip dependency.

Notes:
- Keep this change lightweight (pure-Python dependency; no scientific stacks added).
- If `conda-lock` is not available in the current dev environment, still update `envs/bioimage-mcp-base.yaml` and document the missing lock regeneration as an execution deviation.
  </action>
  <verify>python -c "import pathlib; t=pathlib.Path('envs/bioimage-mcp-base.lock.yml').read_text(); assert 'docstring-parser' in t; print('ok')"</verify>
  <done>`docstring-parser` is present in the base tool env spec and reflected in the lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: Replace introspect_python_api with TypeAdapter + docstring parsing</name>
  <files>src/bioimage_mcp/runtimes/introspect.py</files>
  <action>
Update `introspect_python_api()` to generate JSON Schema via Pydantic v2, instead of manual `TYPE_MAP`.

Implementation constraints:
- Keep function signature: `introspect_python_api(func, descriptions, exclude_params=None) -> dict`.
- Do not introduce any imports that pull heavy scientific deps into the core server.

Required behavior:
- Use `inspect.signature(func)` to determine parameter names and required-vs-optional (no default => required).
- Parse docstrings with `docstring-parser` to enrich descriptions when `descriptions` is missing entries.
- Generate JSON Schema via `TypeAdapter`/Pydantic v2 for parameters where runtime type objects are available.
- Explicitly omit artifact-like parameters from schema (anything that is an ArtifactRef type, or name matches common ports like `image`, `labels`, `table`, `signal`, etc.). This MUST be conservative: better to omit than to leak artifact ports into params_schema.
- Make output deterministic:
  - Stable key ordering of `properties`.
  - Stable `required` ordering.

Keep `introspect_argparse()` behavior intact (adjust only if required to maintain determinism).
  </action>
  <verify>python -c "from bioimage_mcp.runtimes.introspect import introspect_python_api; print('ok')"</verify>
  <done>`introspect_python_api()` uses Pydantic v2 schema generation and produces deterministic output.</done>
</task>

<task type="auto">
  <name>Task 3: Update unit tests to match new schema semantics</name>
  <files>tests/unit/runtimes/test_introspect.py</files>
  <action>
Update existing tests to assert on stable, meaningful properties:
- Required detection.
- Default inclusion.
- Primitive type mapping via Pydantic (int/float/str/bool).
- Docstring-derived descriptions when curated descriptions are absent.
- Deterministic output: repeated calls produce identical dicts.

Avoid assertions tied to exact Pydantic schema dialect beyond what the repo contracts require.
  </action>
  <verify>pytest tests/unit/runtimes/test_introspect.py</verify>
  <done>All unit tests pass with the new schema generator.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/runtimes/test_introspect.py`
- `ruff check .` and `ruff format --check .`
</verification>

<success_criteria>
- Runtime fallback schemas are higher fidelity and deterministic.
- Artifact ports do not appear in generated params_schema.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-engine-ast-first/12-02-SUMMARY.md`
</output>
