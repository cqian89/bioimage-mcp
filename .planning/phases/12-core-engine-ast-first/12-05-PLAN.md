---
phase: 12-core-engine-ast-first
plan: 05
type: execute
wave: 4
depends_on:
  - "12-03"
  - "12-04"
files_modified:
  - src/bioimage_mcp/api/discovery.py
  - src/bioimage_mcp/bootstrap/serve.py
  - src/bioimage_mcp/registry/engine.py
  - src/bioimage_mcp/runtimes/meta_protocol.py
  - tests/contract/test_cellpose_introspection_types.py
autonomous: true

must_haves:
  truths:
    - "tools/list and tools/describe reflect a single unified introspection source (consistent schema + metadata)."
    - "meta.describe responses include tool_version + introspection_source alongside params_schema (not embedded inside params_schema)."
    - "describe excludes artifact ports from params_schema."
  artifacts:
    - path: "src/bioimage_mcp/api/discovery.py"
      provides: "Describe path uses DiscoveryEngine + persistent schema_cache, returns adjacent metadata block."
    - path: "src/bioimage_mcp/bootstrap/serve.py"
      provides: "Server boot uses unified loader/engine output for indexing." 
  key_links:
    - from: "src/bioimage_mcp/api/discovery.py"
      to: "src/bioimage_mcp/registry/engine.py"
      via: "engine-based schema retrieval"
      pattern: "DiscoveryEngine\(|engine\."

---

<objective>
Wire the API layer to the unified engine + persistent cache, and align describe payload to the Phase 12 metadata contract.

Purpose: Ensure consistent schemas/metadata across list/describe and eliminate the separate file-backed schema cache path.
Output: API uses the unified engine/cache and contract tests validate payload shape.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-core-engine-ast-first/12-CONTEXT.md
@src/bioimage_mcp/api/discovery.py
@src/bioimage_mcp/bootstrap/serve.py
@src/bioimage_mcp/runtimes/meta_protocol.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align meta.describe protocol parsing and server describe payload contract</name>
  <files>
src/bioimage_mcp/runtimes/meta_protocol.py
src/bioimage_mcp/api/discovery.py
  </files>
  <action>
Update describe payload so tool_version/introspection_source are returned in an adjacent metadata block.

Concrete contract for `describe` tool response (function nodes):
- Keep `params_schema` as a pure JSON Schema object.
- Add `meta` (or similarly named) object containing:
  - `tool_version`
  - `introspection_source`
  - `callable_fingerprint`
  - `module` (full module path)

Do NOT embed those fields inside `params_schema`.

If the meta.describe worker protocol currently returns tool_version/introspection_source, keep parsing it, but adapt API output into the new shape.
  </action>
  <verify>pytest -k "describe" tests/unit/api</verify>
  <done>`describe` returns params_schema + adjacent metadata fields, matching Phase 12 decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Replace file-backed SchemaCache usage with persistent registry schema_cache</name>
  <files>
src/bioimage_mcp/api/discovery.py
src/bioimage_mcp/registry/engine.py
  </files>
  <action>
Remove the `SchemaCache` JSON file path from the `describe_function()` enriched schema path.

Clarify and enforce the single-source-of-truth split:

- `functions` table is the authoritative source for function metadata returned by list/describe (fn_id, module, io, base params_schema, introspection_source).
- `schema_cache` table is only a persistent cache for *enriched* `params_schema` obtained via isolated `meta.describe` (it may override the base `functions.params_schema_json` when valid).

Concrete describe flow (no "parallel" sources):
1. Load function row from `functions` (+ tool row from `tools`) as the baseline payload.
2. Attempt a DB cache hit via `RegistryIndex.get_cached_schema(...)`.
3. If cache miss/invalid: call isolated `meta.describe`, normalize/filter `params_schema`, then persist via `RegistryIndex.upsert_schema_cache(...)` and return the enriched schema.

Implementation requirements:
- Use `RegistryIndex.get_cached_schema()` / `upsert_schema_cache()` (DB-backed).
- Compute/propagate invalidation keys using Phase 12 rules (tool_version + env_lock_hash + source_hash/callable_fingerprint).

Also ensure `params_schema` filtering for artifact ports remains enforced at the API boundary.
  </action>
  <verify>pytest tests/unit/api/test_discovery.py</verify>
  <done>Describe caching is DB-backed and no longer depends on schema_cache.json.</done>
</task>

<task type="auto">
  <name>Task 3: Update contract tests for new metadata block and stable output</name>
  <files>tests/contract/test_cellpose_introspection_types.py</files>
  <action>
Update contract assertions to reflect:
- `params_schema` contains only JSON Schema.
- `meta.tool_version` and `meta.introspection_source` exist.
- `meta.callable_fingerprint` exists.
- Ordering is deterministic (where applicable in tests).

Avoid relaxing tests to the point that regressions could slip through.
  </action>
  <verify>pytest tests/contract/test_cellpose_introspection_types.py</verify>
  <done>Contract tests pass and validate the new describe payload shape.</done>
</task>

</tasks>

<verification>
- `pytest -m "not slow"`
</verification>

<success_criteria>
- list/describe rely on a single schema source and return consistent metadata.
- describe payload matches Phase 12 contract.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-engine-ast-first/12-05-SUMMARY.md`
</output>
