---
phase: 12-core-engine-ast-first
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/runtimes/introspect.py
  - tests/unit/runtimes/test_introspect.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "meta.describe-derived params_schema reflects type hints + docstrings, with correct required fields."
  artifacts:
    - path: "src/bioimage_mcp/runtimes/introspect.py"
      provides: "Runtime schema emission preserves required fields while using docstring-derived descriptions."
    - path: "tests/unit/runtimes/test_introspect.py"
      provides: "Unit tests validating required/docstring/typeadapter interactions."
  key_links:
    - from: "src/bioimage_mcp/runtimes/introspect.py"
      to: "pydantic.TypeAdapter"
      via: "json_schema()"
      pattern: "TypeAdapter\(.*\)\.json_schema\(\)"

---

<objective>
Harden runtime params_schema emission so docstring descriptions and required-field logic cannot conflict or regress.

Purpose: Meet the schema fidelity requirement by explicitly validating required/properties consistency and by testing docstring + TypeAdapter interactions.
Output: Post-processing guards in introspect_python_api + unit tests that lock required/description behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-core-engine-ast-first/12-core-engine-ast-first-VERIFICATION.md

@src/bioimage_mcp/runtimes/introspect.py
@tests/unit/runtimes/test_introspect.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit required/properties validation and stabilize description merging in introspect_python_api</name>
  <files>src/bioimage_mcp/runtimes/introspect.py</files>
  <action>
Update `introspect_python_api()` post-processing to make required/docstring behavior explicit and verifiable.

Implementation requirements:

1) Required/properties consistency:
- After building `schema["properties"]`, ensure `schema.get("required", [])` contains only keys present in `properties`.
- If the required list becomes empty, remove the `required` key entirely (omit it instead of `required: []`).

2) Description merging:
- Keep the current precedence: curated descriptions > docstring descriptions > fallback.
- When applying TypeAdapter schema (`param_json_schema`), do NOT allow it to delete or override required handling (required is managed at the object level only).
- Keep the current behavior of dropping TypeAdapter's `description` only when a curated/docstring description exists.

3) schema_from_descriptions:
- If returning a schema from descriptions only, do not include an empty `required` key.

Keep deterministic output ordering (sorted properties, stable required ordering).
  </action>
  <verify>python -m compileall src/bioimage_mcp/runtimes/introspect.py</verify>
  <done>introspect_python_api() guarantees required keys always align to properties and required is omitted when empty.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for required/docstring/TypeAdapter interactions</name>
  <files>tests/unit/runtimes/test_introspect.py</files>
  <action>
Extend `tests/unit/runtimes/test_introspect.py` with focused tests:

- Required + docstring description:
  - Define a function where a parameter has NO default and has an Args docstring description.
  - Assert the param is in `schema["required"]` and the description matches the docstring.

- Artifact omission does not leak required:
  - Define a function with a required `image` parameter (no default) and another required non-artifact param.
  - Assert `image` is not present in properties and is not present in required.

- TypeAdapter schema does not affect required semantics:
  - Use an Optional type annotation with no default (e.g., `x: int | None`) and ensure it is still required.
  - Ensure description is preserved (from docstring or curated).

- required omitted when empty:
  - Define a function where all parameters have defaults.
  - Assert `"required" not in schema`.

Keep tests self-contained; do not rely on external packages beyond what's already in core deps.
  </action>
  <verify>pytest tests/unit/runtimes/test_introspect.py</verify>
  <done>New tests fail on the pre-fix behavior and pass with the required/description post-processing.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/runtimes/test_introspect.py`
- `ruff check .` and `ruff format --check .`
</verification>

<success_criteria>
- Runtime schema emission keeps required-field correctness independent of docstring/TypeAdapter description handling.
- Unit tests explicitly cover the failure mode described in Phase 12 verification.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-engine-ast-first/12-09-SUMMARY.md`
</output>
