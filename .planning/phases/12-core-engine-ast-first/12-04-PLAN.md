---
phase: 12-core-engine-ast-first
plan: 04
type: execute
wave: 3
depends_on:
  - "12-03"
files_modified:
  - src/bioimage_mcp/storage/sqlite.py
  - src/bioimage_mcp/registry/index.py
  - src/bioimage_mcp/config/schema.py
  - src/bioimage_mcp/config/loader.py
autonomous: true

must_haves:
  truths:
    - "Schema cache persists across server restarts and is invalidated on tool_version/env lock/source hash changes."
    - "callable_fingerprint is recorded and available for cache/diagnostics."
  artifacts:
    - path: "src/bioimage_mcp/storage/sqlite.py"
      provides: "DB schema + migrations for expanded schema_cache metadata."
    - path: "src/bioimage_mcp/registry/index.py"
      provides: "RegistryIndex schema_cache read/write using extended cache keys."
  key_links:
    - from: "src/bioimage_mcp/registry/index.py"
      to: "schema_cache"
      via: "get_cached_schema/upsert_schema_cache"
      pattern: "schema_cache"

---

<objective>
Upgrade persistent schema caching to support invalidation on tool_version, env lock hash, and callable/source hash.

Purpose: Make caching safe and reproducible for unified introspection.
Output: Extended schema_cache storage + RegistryIndex methods updated accordingly.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-core-engine-ast-first/12-CONTEXT.md
@.planning/phases/12-core-engine-ast-first/12-RESEARCH.md

@src/bioimage_mcp/storage/sqlite.py
@src/bioimage_mcp/registry/index.py
@src/bioimage_mcp/registry/dynamic/discovery.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schema_cache table to record fingerprints and invalidation keys</name>
  <files>src/bioimage_mcp/storage/sqlite.py</files>
  <action>
Extend the SQLite schema and migrations so `schema_cache` records:
- `env_lock_hash` (string)
- `callable_fingerprint` (string)
- `source_hash` (string; can be same as callable_fingerprint if that is the chosen definition)

Migration approach:
- Add new columns via `ALTER TABLE` if missing (similar to existing `functions` table migrations).
- Keep existing rows valid (NULL/empty values allowed).

Do not break existing tests or startup paths.
  </action>
  <verify>python -c "from bioimage_mcp.config.loader import load_config; from bioimage_mcp.storage.sqlite import connect; connect(load_config()).close(); print('ok')"</verify>
  <done>DB starts successfully and schema_cache contains the new columns.</done>
</task>

<task type="auto">
  <name>Task 2: Update RegistryIndex schema cache methods to use invalidation keys</name>
  <files>src/bioimage_mcp/registry/index.py</files>
  <action>
Update `RegistryIndex.get_cached_schema()` and `RegistryIndex.upsert_schema_cache()` so that:
- Cache lookup validates tool_version and compares the stored invalidation keys.
- Cache write records `introspection_source`, `introspected_at`, plus the new invalidation keys.

Adopt this cache key contract:
- Hit only if tool_version matches AND env_lock_hash matches AND source_hash (or callable_fingerprint) matches.

Keep method signatures stable if possible; if signature changes are required, update all callers in a follow-up plan (Plan 12-05).
  </action>
  <verify>pytest -k "schema_cache" tests/unit</verify>
  <done>Schema cache logic enforces invalidation keys and tests remain green.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure config exposes schema cache location and remains backward compatible</name>
  <files>
src/bioimage_mcp/config/schema.py
src/bioimage_mcp/config/loader.py
  </files>
  <action>
If config needs new fields for cache behavior (e.g., enabling/disabling, or selecting DB-only vs file cache), add them conservatively:
- Default behavior should keep the server working without requiring user config changes.
- Do not introduce a separate schema_version; per Phase 12 decisions, schema changes are signaled by tool_version.
  </action>
  <verify>python -c "from bioimage_mcp.config.loader import load_config; load_config(); print('ok')"</verify>
  <done>Config loads successfully and cache behavior remains backward compatible.</done>
</task>

</tasks>

<verification>
- `pytest -m "not slow"`
</verification>

<success_criteria>
- Cache invalidation keys are stored and enforced.
- callable_fingerprint is persistable and usable by later engine/API wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-engine-ast-first/12-04-SUMMARY.md`
</output>
