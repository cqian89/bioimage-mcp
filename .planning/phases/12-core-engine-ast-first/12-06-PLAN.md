---
phase: 12-core-engine-ast-first
plan: 06
type: execute
wave: 5
depends_on:
  - "12-05"
files_modified:
  - src/bioimage_mcp/registry/diagnostics.py
  - src/bioimage_mcp/bootstrap/doctor.py
  - src/bioimage_mcp/bootstrap/checks.py
  - tests/integration/test_doctor.py
autonomous: true

must_haves:
  truths:
    - "Operator can see actionable readiness diagnostics for missing tool envs/packages."
    - "Diagnostics record runtime fallback usage, missing docs, and overlay application (log-only by default)."
  artifacts:
    - path: "src/bioimage_mcp/bootstrap/doctor.py"
      provides: "Doctor output includes environment readiness diagnostics with remediation."
    - path: "src/bioimage_mcp/registry/diagnostics.py"
      provides: "Engine diagnostics events (fallback, overlays, missing docs)."
  key_links:
    - from: "src/bioimage_mcp/bootstrap/doctor.py"
      to: "src/bioimage_mcp/bootstrap/checks.py"
      via: "run_all_checks"
      pattern: "run_all_checks\(\)"

---

<objective>
Add diagnostics and readiness reporting around the unified introspection pipeline.

Purpose: Make failures actionable for operators while keeping diagnostics log-only for clients.
Output: Enhanced doctor checks + diagnostics capture for fallback/overlays/missing docs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/12-core-engine-ast-first/12-CONTEXT.md

@src/bioimage_mcp/bootstrap/doctor.py
@src/bioimage_mcp/bootstrap/checks.py
@src/bioimage_mcp/registry/diagnostics.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend registry diagnostics to cover engine events</name>
  <files>src/bioimage_mcp/registry/diagnostics.py</files>
  <action>
Replace/extend the current minimal `ManifestDiagnostic` model usage with diagnostics capable of recording:
- runtime fallback usage per function
- overlay applied (and conflict warnings)
- missing/empty docs for parameters
- skipped callable errors (reason + fn_id)

Diagnostics requirements:
- Log-only by default (no client-visible MCP payload changes).
- Configurable detail level (default minimal).
- Deterministic ordering when diagnostics are enumerated.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry</verify>
  <done>Diagnostics structures support Phase 12 events and can be emitted by the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Add doctor readiness checks for tool env availability and remediation</name>
  <files>
src/bioimage_mcp/bootstrap/checks.py
src/bioimage_mcp/bootstrap/doctor.py
  </files>
  <action>
Add/extend checks to satisfy ENV-01:
- Detect missing tool environments referenced by manifests.
- Provide actionable remediation steps (commands) in doctor output.

Keep checks fast and offline.
Do not attempt to import tool code.
  </action>
  <verify>bioimage-mcp doctor --json</verify>
  <done>Doctor JSON includes readiness info for missing tool envs/packages with remediation steps.</done>
</task>

<task type="auto">
  <name>Task 3: Add integration coverage for doctor output stability</name>
  <files>tests/integration/test_doctor.py</files>
  <action>
Add an integration test that:
- Runs `bioimage-mcp doctor --json` (or calls `doctor(json_output=True)` directly) and asserts the payload includes registry summary + checks + remediation entries.
- Avoids depending on local conda state by mocking env detection or running against a minimal fixture.
  </action>
  <verify>pytest tests/integration/test_doctor.py</verify>
  <done>Doctor integration test passes and validates the new readiness diagnostics shape.</done>
</task>

</tasks>

<verification>
- `pytest -m "not slow"`
</verification>

<success_criteria>
- Operator-facing diagnostics exist for missing environments and introspection fallback usage.
- Client-facing APIs remain stable; diagnostics remain log-only.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-engine-ast-first/12-06-SUMMARY.md`
</output>
