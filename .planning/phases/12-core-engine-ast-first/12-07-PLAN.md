---
phase: 12-core-engine-ast-first
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/engine.py
  - tests/unit/registry/test_registry_engine.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Dynamic function discovery is AST-first and does not import tool code in the core server."
    - "If AST inspection is incomplete, the engine falls back to isolated runtime meta.describe."
  artifacts:
    - path: "src/bioimage_mcp/registry/engine.py"
      provides: "DiscoveryEngine only triggers runtime fallback when AST is incomplete (no unconditional execute_tool)."
    - path: "tests/unit/registry/test_registry_engine.py"
      provides: "Unit coverage that runtime fallback is conditional."
  key_links:
    - from: "src/bioimage_mcp/registry/engine.py"
      to: "bioimage_mcp.runtimes.executor.execute_tool"
      via: "conditional runtime fallback"
      pattern: "execute_tool\("

---

<objective>
Stop DiscoveryEngine from invoking runtime meta.describe unconditionally during dynamic discovery.

Purpose: Preserve AST-first isolation by avoiding tool entrypoint execution unless AST inspection is incomplete.
Output: Conditional fallback logic + tests that prove execute_tool is not called on the AST-complete path.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-core-engine-ast-first/12-core-engine-ast-first-VERIFICATION.md

@src/bioimage_mcp/registry/engine.py
@tests/unit/registry/test_registry_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make runtime fallback conditional in DiscoveryEngine</name>
  <files>src/bioimage_mcp/registry/engine.py</files>
  <action>
Update `DiscoveryEngine._process_callable()` so it does NOT call `_runtime_describe()` unless AST-derived params_schema is incomplete.

Concrete behavior:
- Compute AST-derived `params_schema` via `_generate_static_params_schema(sc)`.
- Define "AST incomplete" as: `params_schema` missing `properties` OR `properties` is empty after artifact-port filtering.
- Only when AST is incomplete:
  - Call `_runtime_describe()`.
  - If runtime succeeds: override params_schema and set `introspection_source` to `runtime:{...}` as today.
  - If runtime fails: skip callable (existing behavior).
- When AST is complete:
  - Do NOT call `_runtime_describe()`.
  - Keep `introspection_source="ast"`.

Also align the runtime request shape with the API path:
- In `_runtime_describe()` request, send `params: {"target_fn": fn_id}` (not `{"fn_id": fn_id}`), matching `src/bioimage_mcp/api/discovery.py`.

Keep diagnostics behavior:
- Only emit `EngineEventType.RUNTIME_FALLBACK` when fallback actually runs.
  </action>
  <verify>python -m compileall src/bioimage_mcp/registry/engine.py</verify>
  <done>DiscoveryEngine no longer calls execute_tool for AST-complete callables; fallback still works for AST-incomplete callables.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit assertions that runtime fallback is not executed for AST-complete callables</name>
  <files>tests/unit/registry/test_registry_engine.py</files>
  <action>
Update/add unit tests to explicitly prove the gating:

- In `test_discovery_engine_ast_only`:
  - Keep the existing AST report with at least one parameter.
  - Assert `execute_tool` is NOT called (e.g., `mock_execute.assert_not_called()`).

- In `test_discovery_engine_runtime_fallback`:
  - Keep the incomplete AST report (no parameters).
  - Assert `execute_tool` is called exactly once.

Do not require any real tool environments; keep using mocks.
  </action>
  <verify>pytest tests/unit/registry/test_registry_engine.py</verify>
  <done>Tests fail on the old unconditional behavior and pass with the new conditional fallback.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/test_registry_engine.py`
- `ruff check .` and `ruff format --check .`
</verification>

<success_criteria>
- Dynamic discovery no longer executes tool entrypoints unless AST is incomplete.
- Engine unit tests enforce the new behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/12-core-engine-ast-first/12-07-SUMMARY.md`
</output>
