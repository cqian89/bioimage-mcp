---
phase: 01-core-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/bootstrap/checks.py
  - tests/unit/bootstrap/test_checks.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "bioimage-mcp doctor detects NVIDIA GPU availability via nvidia-smi"
    - "bioimage-mcp doctor detects Apple Silicon MPS availability via system commands"
    - "GPU section in doctor shows unified output (CUDA and/or MPS)"
    - "GPU hardware details surfaced when detectable (model, memory)"
  artifacts:
    - path: "src/bioimage_mcp/bootstrap/checks.py"
      provides: "Unified GPU detection (CUDA + MPS)"
      contains: "check_gpu"
    - path: "tests/unit/bootstrap/test_checks.py"
      provides: "Tests for MPS detection"
      contains: "test_check_gpu_mps"
  key_links:
    - from: "src/bioimage_mcp/bootstrap/doctor.py"
      to: "src/bioimage_mcp/bootstrap/checks.py"
      via: "check_gpu() in run_all_checks()"
      pattern: "check_gpu\\(\\)"
---

<objective>
Complete CORE-02: Add MPS (Apple Silicon) GPU detection alongside existing NVIDIA detection.

Purpose: Enable bioimage-mcp to detect and report Apple Silicon GPU availability for MPS-accelerated tool execution, completing the GPU detection requirement.

Output: Enhanced `check_gpu()` function that detects both CUDA (nvidia-smi) and MPS (Apple Silicon) GPUs with unified output.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-runtime/01-CONTEXT.md
@src/bioimage_mcp/bootstrap/checks.py
@src/bioimage_mcp/bootstrap/doctor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance check_gpu() with MPS detection</name>
  <files>src/bioimage_mcp/bootstrap/checks.py</files>
  <action>
Extend the `check_gpu()` function to detect Apple Silicon MPS alongside NVIDIA CUDA:

1. Keep existing nvidia-smi detection for CUDA
2. Add MPS detection using system commands (avoid PyTorch dependency):
   - macOS detection: `platform.system() == "Darwin"`
   - Apple Silicon detection: `subprocess.run(["sysctl", "-n", "hw.optional.arm64"])` returns "1"
   - Alternative check: `sysctl -n machdep.cpu.brand_string` contains "Apple"

3. Return unified GPU info in details dict:
   ```python
   details = {
       "gpu_available": cuda_available or mps_available,
       "cuda": {
           "available": cuda_available,
           "detector": "nvidia-smi",
           "nvidia_smi": nvidia_smi_path,  # if available
       },
       "mps": {
           "available": mps_available,
           "detector": "sysctl",
           "apple_silicon": True,  # if detected
       },
   }
   ```

4. For NVIDIA, try to get additional info via nvidia-smi query:
   - `nvidia-smi --query-gpu=name,memory.total --format=csv,noheader`
   - Parse and add to cuda details (fail gracefully if unavailable)

5. Always return ok=True (GPU is optional, not a failure condition)

Per CONTEXT.md: "Use system commands (e.g., `system_profiler`, `sysctl`) to detect Apple Silicon — avoid PyTorch dependency in core server"
  </action>
  <verify>
Run: `python -c "from bioimage_mcp.bootstrap.checks import check_gpu; r = check_gpu(); print(r.details)"` - should show cuda and mps keys
  </verify>
  <done>
check_gpu() returns details with both cuda and mps detection results; mps.available is True on Apple Silicon, cuda.available is True if nvidia-smi present
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests for MPS detection</name>
  <files>tests/unit/bootstrap/test_checks.py</files>
  <action>
Add tests for the enhanced GPU detection:

1. `test_check_gpu_mps_detection_on_apple_silicon`:
   - Mock `platform.system()` to return "Darwin"
   - Mock `subprocess.run` for sysctl to return "1" for hw.optional.arm64
   - Assert mps.available is True in details

2. `test_check_gpu_mps_not_available_on_linux`:
   - Mock `platform.system()` to return "Linux"
   - Assert mps.available is False

3. `test_check_gpu_cuda_and_mps_both_detected`:
   - Mock both nvidia-smi present AND Apple Silicon detected
   - Assert both cuda.available and mps.available are True
   - Assert gpu_available is True

4. `test_check_gpu_nvidia_smi_query_failure_graceful`:
   - Mock nvidia-smi present but query subprocess fails
   - Assert cuda.available is True but no model/memory info
   - Assert function doesn't raise

5. Update existing `test_check_gpu_is_graceful` if needed to account for new structure
  </action>
  <verify>
Run: `pytest tests/unit/bootstrap/test_checks.py -k gpu -v` - all GPU tests pass
  </verify>
  <done>
Tests cover MPS detection on Apple Silicon, CUDA detection, combined detection, and graceful failure handling
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/unit/bootstrap/test_checks.py -k gpu -v` — all GPU detection tests pass
2. `bioimage-mcp doctor --json | python -c "import sys,json; d=json.load(sys.stdin); print(d['checks'])"` — GPU check shows cuda and mps keys
3. `ruff check src/bioimage_mcp/bootstrap/checks.py` — no lint errors
</verification>

<success_criteria>
- check_gpu() detects both NVIDIA (via nvidia-smi) and Apple Silicon MPS (via sysctl)
- Details dict has unified structure with cuda and mps sub-objects
- All existing tests continue to pass
- New MPS detection tests added and passing
- doctor --json output includes GPU detection details for both platforms
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-runtime/01-01-SUMMARY.md`
</output>
