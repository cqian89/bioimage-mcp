---
phase: 13-dynamic-introspection-cache-reuse
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/trackpy/bioimage_mcp_trackpy/entrypoint.py
  - tests/unit/tools/test_trackpy_dynamic_introspection_cache.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "tools.trackpy meta.list writes ~/.bioimage-mcp/cache/dynamic/tools.trackpy/introspection_cache.json when a project_root with env lockfiles is available (via env var or cwd heuristic)."
    - "If project_root cannot be determined, tools.trackpy meta.list still succeeds but simply bypasses caching (no crash)."
  artifacts:
    - path: "tools/trackpy/bioimage_mcp_trackpy/entrypoint.py"
      provides: "Robust project_root detection for lockfile-based caching (env var + cwd + manifest fallback)."
    - path: "tests/unit/tools/test_trackpy_dynamic_introspection_cache.py"
      provides: "Unit tests covering project_root detection fallbacks and confirming cache file creation."
  key_links:
    - from: "tools/trackpy/bioimage_mcp_trackpy/entrypoint.py"
      to: "bioimage_mcp.registry.dynamic.discovery.discover_functions"
      via: "project_root argument is non-None when env var/cwd yields a project root"
      pattern: "discover_functions\(.*project_root="

---

<objective>
Fix tools.trackpy cache writes in real `bioimage-mcp list` runs by making project_root detection reliable outside the repo layout.

Purpose: Ensure lockfile-hash caching is actually used (and cache file is written) when trackpy runs from an installed tool environment.
Output: Updated trackpy entrypoint project_root detection + unit tests that verify the cache file is created.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-dynamic-introspection-cache-reuse/13-UAT.md
@.planning/phases/13-dynamic-introspection-cache-reuse/13-02-SUMMARY.md

@tools/trackpy/bioimage_mcp_trackpy/entrypoint.py
@src/bioimage_mcp/registry/dynamic/discovery.py
@tests/unit/tools/test_trackpy_dynamic_introspection_cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make trackpy project_root detection robust (env var -> cwd -> manifest fallback)</name>
  <files>tools/trackpy/bioimage_mcp_trackpy/entrypoint.py</files>
  <action>
Update `handle_meta_list()` so it can find the repo project root even when `TRACKPY_TOOL_ROOT` is not inside the repo.

Implementation requirements:
- Add an env var override: if `BIOIMAGE_MCP_PROJECT_ROOT` is set and points to an existing directory, use it as project_root.
- Otherwise, attempt `_find_project_root(Path.cwd())`.
- Otherwise, fall back to the current behavior (`_find_project_root(manifest_path.parent)`).
- Keep the contract: if project_root is None, `discover_functions(..., project_root=None)` must still work and meta.list must still return ok/result.
- Do not change cache directory location (must remain `~/.bioimage-mcp/cache/dynamic/<tool_id>/introspection_cache.json`).
  </action>
  <verify>python -m compileall tools/trackpy/bioimage_mcp_trackpy/entrypoint.py</verify>
  <done>handle_meta_list uses an env var/cwd-based project_root when available, enabling lockfile-hash caching in installed layouts.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests that validate project_root fallback behavior and cache file creation</name>
  <files>tests/unit/tools/test_trackpy_dynamic_introspection_cache.py</files>
  <action>
Extend `tests/unit/tools/test_trackpy_dynamic_introspection_cache.py` with tests that cover real project_root detection (without patching `_find_project_root`).

Test requirements:
- Continue patching `Path.home()` to a temp location.
- Continue patching `TRACKPY_TOOL_ROOT` to a temp manifest location.
- Do NOT patch `_find_project_root` in these new tests.

Add at least one of the following scenarios (prefer both if still small):
1) CWD heuristic:
   - Create a temp `project_root` with `envs/bioimage-mcp-trackpy.lock.yml`.
   - `chdir` into `project_root / "a" / "b"` (within 5 parents of project_root).
   - Call `handle_meta_list({})` twice; assert `~/.bioimage-mcp/cache/dynamic/tools.trackpy/introspection_cache.json` exists after first call and that introspection is not re-run on second call.
2) Env var override:
   - Set `BIOIMAGE_MCP_PROJECT_ROOT` to the temp `project_root`.
   - `chdir` to a directory that is NOT under project_root.
   - Call `handle_meta_list({})` and assert the cache file is created.

Make assertions about:
- cache file existence in the patched home directory
- cache hit/miss behavior (introspect_module call count)
  </action>
  <verify>pytest tests/unit/tools/test_trackpy_dynamic_introspection_cache.py -q</verify>
  <done>Tests prove trackpy can find project_root via cwd/env var and reliably writes introspection_cache.json.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/tools/test_trackpy_dynamic_introspection_cache.py -q`
- `pytest -q`
</verification>

<success_criteria>
- After running `bioimage-mcp list`, `~/.bioimage-mcp/cache/dynamic/tools.trackpy/introspection_cache.json` exists.
- tools.trackpy continues to return canonical meta.list result shape and does not fail when project_root is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dynamic-introspection-cache-reuse/13-04-SUMMARY.md`
</output>
