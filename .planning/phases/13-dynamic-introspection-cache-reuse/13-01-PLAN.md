---
phase: 13-dynamic-introspection-cache-reuse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/base/bioimage_mcp_base/entrypoint.py
  - tests/unit/tools/test_base_dynamic_introspection_cache.py
autonomous: true

must_haves:
  truths:
    - "Calling tools.base meta.list twice reuses cached dynamic introspection when the env lockfile hash is unchanged."
    - "If the env lockfile cannot be found, tools.base meta.list still works and simply does not use the cache."
    - "Dynamic introspection cache is written under ~/.bioimage-mcp/cache/dynamic/<tool_id> (not under the repo)."
  artifacts:
    - path: tools/base/bioimage_mcp_base/entrypoint.py
      provides: "meta.list wires IntrospectionCache + project_root into discover_functions"
    - path: tests/unit/tools/test_base_dynamic_introspection_cache.py
      provides: "unit coverage for cache wiring + hit/miss behavior"
  key_links:
    - from: tools/base/bioimage_mcp_base/entrypoint.py
      to: bioimage_mcp.registry.dynamic.discovery.discover_functions
      via: "handle_meta_list(..., cache=..., project_root=...)"
      pattern: "discover_functions\\(manifest, ADAPTER_REGISTRY, cache=.*project_root=.*\\)"
---

<objective>
Wire lockfile-gated dynamic introspection caching into the base tool pack's meta.list so repeated list calls reuse results across runs.

Purpose: Make dynamic discovery fast/stable by reusing cached results when the environment lockfile is unchanged.
Output: tools.base meta.list passes cache + project_root into discover_functions and has tests proving reuse/invalidation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tools/base/bioimage_mcp_base/entrypoint.py
@src/bioimage_mcp/registry/dynamic/discovery.py
@src/bioimage_mcp/registry/dynamic/cache.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire IntrospectionCache + project_root into tools.base meta.list</name>
  <files>tools/base/bioimage_mcp_base/entrypoint.py</files>
  <action>
Update handle_meta_list to reuse the dynamic discovery cache.

- Add a local helper (same heuristic as src/bioimage_mcp/api/discovery.py:_find_project_root) to compute project_root from a starting Path.
- Compute project_root starting from the base tool manifest location (tools/base/manifest.yaml parent directory).
- Define the default dynamic cache directory as: Path.home() / ".bioimage-mcp" / "cache" / "dynamic" / manifest.tool_id
  - Rationale: safe for tool envs (no config access) and stable across runs.
- Build IntrospectionCache(cache_dir) and pass it into discover_functions:
  - discover_functions(manifest, ADAPTER_REGISTRY, cache=cache, project_root=project_root)
- Preserve existing behavior:
  - If project_root cannot be found or lockfile is missing, discover_functions should behave as today (no cache used, no failure).
  </action>
  <verify>python -m compileall tools/base/bioimage_mcp_base/entrypoint.py</verify>
  <done>
handle_meta_list constructs cache_dir under ~/.bioimage-mcp/cache/dynamic/<tool_id>/ and calls discover_functions with cache and project_root.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for cache wiring and reuse/invalidation</name>
  <files>tests/unit/tools/test_base_dynamic_introspection_cache.py</files>
  <action>
Create a focused unit test module that validates both wiring (args passed) and behavior (hit/miss) without importing heavy adapters.

Test A: handle_meta_list passes cache + project_root
- Patch/monkeypatch discover_functions so it records received kwargs.
- Patch Path.home() to a tmp_path to avoid writing to the real home directory.
- Patch project_root detection to return a known Path (tmp_path) to make assertions deterministic.
- Patch manifest load (yaml.safe_load or ToolManifest.model_validate) to a minimal manifest with a single dynamic_source to avoid real discovery.
- Assert discover_functions called once and cache/project_root are not None and are Paths.

Test B: cache hit/miss + invalidation
- Use the real discover_functions + real IntrospectionCache.
- Provide a temp project_root containing envs/<env_id>.lock.yml.
- Provide a mock adapter in adapter_registry whose discover() increments a counter and returns a minimal FunctionMetadata.
- Call handle_meta_list twice with unchanged lockfile content and assert adapter.discover called once total (second call is a cache hit).
- Change lockfile content, call handle_meta_list again, and assert adapter.discover called again (cache miss due to lockfile hash change).
  </action>
  <verify>pytest tests/unit/tools/test_base_dynamic_introspection_cache.py -q</verify>
  <done>
Unit tests prove: cache + project_root are passed into discover_functions, and lockfile hash drives cache reuse/invalidation.
  </done>
</task>

</tasks>

<verification>
- ruff check .
- ruff format --check .
- pytest tests/unit/tools/test_base_dynamic_introspection_cache.py -q
</verification>

<success_criteria>
- tools.base meta.list uses ~/.bioimage-mcp/cache/dynamic/tools.base/ and avoids recomputing dynamic discovery when lockfile hash is unchanged.
- Tests fail if cache wiring is removed or lockfile gating breaks.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dynamic-introspection-cache-reuse/13-01-SUMMARY.md`
</output>
