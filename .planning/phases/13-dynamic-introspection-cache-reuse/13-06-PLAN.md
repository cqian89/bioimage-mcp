---
phase: 13-dynamic-introspection-cache-reuse
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/cli.py
  - src/bioimage_mcp/bootstrap/list.py
  - tests/unit/bootstrap/test_list_tool_filter.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "User can run `bioimage-mcp list --tool <tool_id>` to list a single tool"
    - "`--tool` filtering works for both table output and `--json` output"
  artifacts:
    - path: "src/bioimage_mcp/cli.py"
      provides: "`list` subcommand supports `--tool` and passes it to list_tools()"
    - path: "src/bioimage_mcp/bootstrap/list.py"
      provides: "list_tools(tool=...) filters tool_details by tool_id"
    - path: "tests/unit/bootstrap/test_list_tool_filter.py"
      provides: "Unit coverage for `--tool` argument + filtering"
  key_links:
    - from: "src/bioimage_mcp/cli.py"
      to: "src/bioimage_mcp/bootstrap/list.py"
      via: "args.tool -> list_tools(tool=...)"
      pattern: "list_tools\\(.*tool="
---

<objective>
Close UAT gap: implement `bioimage-mcp list --tool ...` so users can target a single tool (e.g. trackpy) during cache debugging.

Purpose: Makes UAT Test 3 reproducible without scanning all tools.
Output: CLI arg support + bootstrap filtering + unit test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-dynamic-introspection-cache-reuse/13-UAT.md
@.planning/debug/cli-list-tool-trackpy.md

@src/bioimage_mcp/cli.py
@src/bioimage_mcp/bootstrap/list.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `--tool` option and wire through to list_tools()</name>
  <files>src/bioimage_mcp/cli.py, src/bioimage_mcp/bootstrap/list.py</files>
  <action>
Add a `--tool` string option to the `bioimage-mcp list` CLI.

Implementation requirements:
- In `src/bioimage_mcp/cli.py`, add `list_cmd.add_argument("--tool", ...)` and pass it to `list_tools()` from `_handle_list`.
- In `src/bioimage_mcp/bootstrap/list.py`, update `list_tools()` signature to accept `tool: str | None` (or `tool_filter`).
- Apply filtering to the computed `tool_details` list so that only matching tool IDs are rendered.
- Accept both forms:
  - Exact tool_id: `tools.trackpy`
  - Short name: `trackpy` (normalize to `tools.trackpy` when filtering)
- Keep behavior stable when `--tool` is omitted.
- If the filter matches no tools, render an empty list (table prints the existing "No tools found" message; `--json` prints `{\"tools\": []}`).
  </action>
  <verify>
1) `bioimage-mcp list --tool tools.base --json` prints exactly one tool with id `tools.base`.
2) `bioimage-mcp list --tool base --json` prints exactly one tool with id `tools.base`.
  </verify>
  <done>
`bioimage-mcp list --tool ...` is accepted by the CLI and the output is filtered to the requested tool.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit test for `--tool` filtering</name>
  <files>tests/unit/bootstrap/test_list_tool_filter.py</files>
  <action>
Create a focused unit test that validates `--tool` filtering without invoking subprocess discovery.

Testing approach (pick the simplest that matches existing testing style):
- Option A (preferred): test `list_tools(tool=...)` filtering by monkeypatching `load_config()` + `load_manifests()` to return two fake manifests (`tools.base`, `tools.trackpy`) and assert only the requested tool is rendered.
- Option B: test `cli._build_parser()` parses `list --tool tools.trackpy` and that `_handle_list` calls `list_tools(tool=...)` (patch list_tools import).

Keep the test hermetic: no reliance on actual tool envs, no writing to `~/.bioimage-mcp`.
  </action>
  <verify>`pytest tests/unit/bootstrap/test_list_tool_filter.py -q`</verify>
  <done>
Unit test passes and fails if `--tool` regresses.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/unit/bootstrap/test_list_tool_filter.py -q`
- Manual: `bioimage-mcp list --tool tools.trackpy` (table output) filters to a single row.
</verification>

<success_criteria>
- UAT Test 3 gap is closed: CLI supports `--tool` and enables targeted runs.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dynamic-introspection-cache-reuse/13-06-SUMMARY.md`
</output>
