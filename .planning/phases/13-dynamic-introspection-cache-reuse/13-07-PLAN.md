---
phase: 13-dynamic-introspection-cache-reuse
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/registry/dynamic/discovery.py
  - tests/unit/registry/test_dynamic_discovery.py
  - tests/unit/tools/test_base_dynamic_introspection_cache.py
  - tests/unit/tools/test_trackpy_dynamic_introspection_cache.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "If `introspection_cache.json` is deleted, a subsequent discovery run recreates it (no silent cache bypass)"
    - "Changing a tool `manifest.yaml` invalidates tool-pack dynamic introspection cache and forces a refresh"
  artifacts:
    - path: "src/bioimage_mcp/registry/dynamic/discovery.py"
      provides: "Cache key includes manifest checksum and has a sentinel when lockfile hash is unavailable"
    - path: "tests/unit/registry/test_dynamic_discovery.py"
      provides: "Unit coverage for cache key semantics (lockfile + manifest)"
    - path: "tests/unit/tools/test_trackpy_dynamic_introspection_cache.py"
      provides: "Regression coverage for trackpy cache regeneration behavior"
  key_links:
    - from: "src/bioimage_mcp/registry/dynamic/discovery.py"
      to: "src/bioimage_mcp/registry/dynamic/cache.py"
      via: "IntrospectionCache.get/put with composite key"
      pattern: "cache\\.(get|put)\\(.*manifest\\.manifest_checksum"
---

<objective>
Close UAT gaps: (1) cache regeneration when the lockfile hash is missing and (2) manifest edits invalidating dynamic introspection caches.

Purpose: Ensures tool-pack caches under `~/.bioimage-mcp/cache/dynamic/<tool_id>/introspection_cache.json` are always writable and are invalidated when the manifest changes.
Output: Updated cache key logic + updated tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/13-dynamic-introspection-cache-reuse/13-UAT.md
@.planning/debug/trackpy-introspection-cache-regeneration.md
@.planning/debug/manifest-cache-miss-no-update.md

@src/bioimage_mcp/registry/dynamic/discovery.py
@src/bioimage_mcp/registry/dynamic/cache.py
@tools/base/bioimage_mcp_base/entrypoint.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make IntrospectionCache key include manifest checksum + sentinel fallback</name>
  <files>src/bioimage_mcp/registry/dynamic/discovery.py</files>
  <action>
Update `discover_functions()` so dynamic introspection caching is never silently disabled and is invalidated on manifest changes.

Required behavior:
- When `cache` is provided, always attempt cache read/write (do not gate on `lockfile_hash` being truthy).
- Compute a stable composite cache key that includes the manifest checksum.
  - If a lockfile hash is available: use it as the env component.
  - If not: use a sentinel like `no-lockfile` as the env component.
  - Always include a short manifest checksum component (e.g. first 16 of `manifest.manifest_checksum`) in the key.
  - Example key string: `<env_component>:<manifest_checksum_16>`
- Use this composite key when calling `IntrospectionCache.get(...)` and `IntrospectionCache.put(...)`.

Constraints:
- Keep lockfile hashing logic as-is when the lockfile exists.
- Do not change the cache file format in `src/bioimage_mcp/registry/dynamic/cache.py`; only change the key passed into it.
  </action>
  <verify>
`pytest tests/unit/registry/test_dynamic_discovery.py -q`
  </verify>
  <done>
discover_functions caches results even when the lockfile is missing, and manifest changes force a cache miss.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update/add tests to lock behavior (regen + manifest invalidation)</name>
  <files>tests/unit/registry/test_dynamic_discovery.py, tests/unit/tools/test_base_dynamic_introspection_cache.py, tests/unit/tools/test_trackpy_dynamic_introspection_cache.py</files>
  <action>
Update existing tests and add a focused regression assertion for manifest invalidation.

Required test coverage:
- `tests/unit/registry/test_dynamic_discovery.py`:
  - Add a test that pre-populates IntrospectionCache under key for `(lockfile_hash + checksum_A)`.
  - Call `discover_functions()` with the same lockfile but a different `manifest_checksum` (checksum_B).
  - Assert the adapter is invoked (cache miss) and the new key is written.
- `tests/unit/tools/test_trackpy_dynamic_introspection_cache.py`:
  - Update any assumptions/comments that caching is bypassed when lockfile hash is empty.
  - Ensure assertions still validate: deletion of cache file results in it being recreated after meta.list.
- `tests/unit/tools/test_base_dynamic_introspection_cache.py`:
  - Keep existing lockfile-driven invalidation assertions.
  - If necessary, extend the test to assert manifest checksum affects cache key (one additional assertion is enough).
  </action>
  <verify>
`pytest tests/unit/registry/test_dynamic_discovery.py tests/unit/tools/test_base_dynamic_introspection_cache.py tests/unit/tools/test_trackpy_dynamic_introspection_cache.py -q`
  </verify>
  <done>
Unit tests pass and cover both UAT major issues: cache regeneration without lockfile and cache invalidation on manifest change.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/unit/registry/test_dynamic_discovery.py tests/unit/tools/test_base_dynamic_introspection_cache.py tests/unit/tools/test_trackpy_dynamic_introspection_cache.py -q`
- Manual repro (optional):
  - Delete `~/.bioimage-mcp/cache/dynamic/tools.trackpy/introspection_cache.json`
  - Run `bioimage-mcp list` and confirm the file is recreated
  - Edit a tool `manifest.yaml` and confirm the tool-pack cache file updates after `bioimage-mcp list`
</verification>

<success_criteria>
- UAT Test 4 gap closes: cache file is regenerated even if the lockfile is missing.
- UAT Test 5 gap closes: manifest edits invalidate dynamic introspection cache and refresh the cache file on disk.
</success_criteria>

<output>
After completion, create `.planning/phases/13-dynamic-introspection-cache-reuse/13-07-SUMMARY.md`
</output>
