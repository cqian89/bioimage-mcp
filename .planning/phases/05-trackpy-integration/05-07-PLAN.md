---
phase: 05-trackpy-integration
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/bootstrap/checks.py
  - src/bioimage_mcp/bootstrap/doctor.py
  - tests/unit/bootstrap/test_doctor_output.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Run `bioimage-mcp doctor`. Should report READY and exit 0 when the only failing check is missing conda-lock."
    - "Run `bioimage-mcp doctor --json`. Should return ready=true while including a conda_lock check with ok=false."
  artifacts:
    - path: "src/bioimage_mcp/bootstrap/checks.py"
      provides: "CheckResult supports required/optional checks; conda_lock is optional"
      contains: "required"
    - path: "src/bioimage_mcp/bootstrap/doctor.py"
      provides: "Doctor readiness ignores optional failures and prints them as warnings"
      contains: "WARNINGS"
    - path: "tests/unit/bootstrap/test_doctor_output.py"
      provides: "Regression coverage for optional failing checks"
  key_links:
    - from: "src/bioimage_mcp/bootstrap/doctor.py"
      to: "src/bioimage_mcp/bootstrap/checks.py"
      via: "CheckResult.required"
      pattern: "r\\.required"

---

<objective>
Make `bioimage-mcp doctor` treat missing `conda-lock` as a warning (not a NOT READY gate), so Trackpy installs do not fail readiness solely due to an optional dev tool.

Purpose: Restore correct readiness semantics for end users while preserving actionable remediation.
Output: Updated doctor/check logic + unit tests proving READY-with-warnings behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-trackpy-integration/05-UAT.md
@.planning/debug/doctor-not-ready.md

@src/bioimage_mcp/bootstrap/checks.py
@src/bioimage_mcp/bootstrap/doctor.py
@tests/unit/bootstrap/test_doctor_output.py
</context>

<tasks>

<task type="auto">
  <name>Adjust readiness to ignore optional checks</name>
  <files>src/bioimage_mcp/bootstrap/checks.py</files>
  <files>src/bioimage_mcp/bootstrap/doctor.py</files>
  <action>
Add first-class support for optional checks and ensure `conda_lock` is optional:

- Extend `CheckResult` (dataclass) with `required: bool = True`.
- In `check_conda_lock()`, keep `ok=False` when missing, but set `required=False` on the returned CheckResult.
- In `doctor.doctor()` compute readiness using only required checks, e.g. `ready = all(r.ok for r in results if r.required)`.
- In human-readable output:
  - Always print the registry summary as today.
  - If `ready` is false, keep current remediation printing for failing checks.
  - If `ready` is true but there are failing optional checks, print an additional warnings section (header string MUST include `WARNINGS`) listing the failing optional checks and their remediation.
- In JSON output:
  - Keep the same top-level shape, but ensure `ready` reflects required checks only.
  - `checks` entries must include the new `required` field.

Avoid changing the meaning of other checks (network/env_manager/etc.) unless needed to make tests pass.
  </action>
  <verify>pytest tests/unit/bootstrap/test_checks.py tests/unit/bootstrap/test_doctor_output.py</verify>
  <done>
`bioimage-mcp doctor` no longer prints NOT READY solely because `conda-lock` is missing, while still surfacing install guidance as a warning.
  </done>
</task>

<task type="auto">
  <name>Add unit coverage for READY-with-warnings behavior</name>
  <files>tests/unit/bootstrap/test_doctor_output.py</files>
  <action>
Update and extend doctor output tests:

- Add a new test where `run_checks()` returns:
  - at least one required check with `ok=True`
  - one optional failing check (use `name="conda_lock"`, `ok=False`, `required=False`, with non-empty remediation)
  - Assert: exit_code == 0, output includes `READY`, and output includes a `WARNINGS` section containing the `conda_lock` remediation.
- Add/extend a JSON-output test to assert payload `ready` is true in the same scenario and includes the failing optional check in `checks`.

Keep existing tests readable; prefer explicit `CheckResult(...)` objects over list multiplication when mixing required/optional.
  </action>
  <verify>pytest tests/unit/bootstrap/test_doctor_output.py</verify>
  <done>
Unit tests lock in the semantics: optional failures do not flip `ready` to false, but still appear in output.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/unit/bootstrap/test_checks.py tests/unit/bootstrap/test_doctor_output.py`
</verification>

<success_criteria>
- `bioimage-mcp doctor` returns exit code 0 when only optional checks fail (including missing conda-lock).
- The human-readable output still surfaces missing conda-lock as a warning with remediation.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trackpy-integration/05-07-SUMMARY.md`.
</output>
