---
phase: 05-trackpy-integration
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/api/discovery.py
  - tests/integration/test_discovery_enrichment.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Run `bioimage-mcp describe trackpy.locate`. params_schema includes diameter (and is not an empty properties dict)."
    - "DiscoveryService schema enrichment accepts both meta.describe response shapes: ok/result and ok/outputs.result."
  artifacts:
    - path: "src/bioimage_mcp/api/discovery.py"
      provides: "meta.describe request uses command=execute and response parsing supports outputs.result"
      contains: "fn_id\": \"meta.describe"
    - path: "tests/integration/test_discovery_enrichment.py"
      provides: "Regression tests for schema cache + outputs.result parsing"
  key_links:
    - from: "src/bioimage_mcp/api/discovery.py"
      to: "tools/trackpy/bioimage_mcp_trackpy/entrypoint.py"
      via: "execute_tool(meta.describe)"
      pattern: "\\\"command\\\"\\s*:\\s*\\\"execute\\\""
    - from: "src/bioimage_mcp/api/discovery.py"
      to: "SchemaCache"
      via: "cache.set"
      pattern: "cache\\.set\\("

---

<objective>
Fix `describe` schema enrichment for Trackpy by aligning `meta.describe` calls with the tool-pack execute protocol and parsing worker-style responses.

Purpose: Make `bioimage-mcp describe trackpy.locate` return a correct parameter schema (e.g., diameter) instead of falling back to an empty manifest schema.
Output: Updated DiscoveryService meta.describe integration + regression tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-trackpy-integration/05-UAT.md
@.planning/debug/trackpy-schema-empty.md

@src/bioimage_mcp/api/discovery.py
@tools/trackpy/bioimage_mcp_trackpy/entrypoint.py
@tests/integration/test_discovery_enrichment.py
</context>

<tasks>

<task type="auto">
  <name>Normalize meta.describe request + response parsing in DiscoveryService</name>
  <files>src/bioimage_mcp/api/discovery.py</files>
  <action>
Update the `DiscoveryService.describe_function()` meta.describe enrichment block to work with tool packs that use the NDJSON "execute" protocol:

- When building the `meta.describe` request for `execute_tool(...)`, include:
  - `command: "execute"`
  - `ordinal: 0`
  - keep existing `fn_id: "meta.describe"`, `params.target_fn`, and `work_dir`.
- After `execute_tool(...)`, support BOTH success shapes:
  - legacy: `{ok: true, result: {...}}`
  - worker-style: `{ok: true, outputs: {result: {...}}}` (also allow `{command: "execute_result", ok: true, outputs: ...}`)
  - Implement a small normalization helper in-place (no new module) that extracts `result_dict` as a dict, preferring `response["result"]` then `response.get("outputs", {}).get("result")`.
- Use `result_dict` to set:
  - `params_schema`
  - `introspection_source` (if present)
  - and write to `SchemaCache` as today.

Do not change the public response shape of `describe_function` (it should still return `params_schema` etc.), only fix enrichment and caching.
  </action>
  <verify>pytest tests/integration/test_discovery_enrichment.py::test_describe_function_uses_json_cache</verify>
  <done>
With a worker-style meta.describe response, DiscoveryService still enriches and caches `params_schema`.
  </done>
</task>

<task type="auto">
  <name>Fix and extend discovery enrichment regression tests</name>
  <files>tests/integration/test_discovery_enrichment.py</files>
  <action>
Repair the currently failing integration test and add coverage for the worker-style response shape:

- In `test_describe_function_uses_json_cache`:
  - Replace the incorrect assertions about `schema`/`params_schema`.
  - Assert that `params_schema` is present and that its `properties.sigma.type == "number"`.
  - Optionally assert the recorded `request` includes `fn_id == "meta.describe"` and now includes `command == "execute"`.
- Add a second test (or extend the first with a second call using a different fake) where the monkeypatched `execute_tool` returns:
  - `{ok: true, outputs: {result: {params_schema: ..., tool_version: ..., introspection_source: ...}}}`
  - Assert that `describe_function` still returns the enriched `params_schema` and that caching prevents a second `execute_tool` call.

Keep the tests fully self-contained (no conda env, no real subprocess), continuing to monkeypatch `bioimage_mcp.api.discovery.execute_tool`.
  </action>
  <verify>pytest tests/integration/test_discovery_enrichment.py</verify>
  <done>
Integration tests pass and protect against regressions where meta.describe responses are wrapped under outputs.result.
  </done>
</task>

</tasks>

<verification>
- `pytest tests/integration/test_discovery_enrichment.py`
- (optional manual) `bioimage-mcp describe trackpy.locate | jq '.params_schema.properties.diameter'`
</verification>

<success_criteria>
- `bioimage-mcp describe trackpy.locate` shows non-empty `params_schema.properties` including `diameter`.
- Schema enrichment works regardless of whether the tool returns meta.describe data under `result` or under `outputs.result`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-trackpy-integration/05-08-SUMMARY.md`.
</output>
