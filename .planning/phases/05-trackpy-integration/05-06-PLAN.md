---
phase: 05-trackpy-integration
plan: 06
type: execute
wave: 4
depends_on: ["05-03", "05-05"]
files_modified:
  - tests/smoke/test_trackpy_e2e.py
autonomous: true
gap_closure: true
must_haves:
  truths:
    - "Trackpy locate and link functions work end-to-end in real environment"
    - "Batch processing runs without errors"
  artifacts:
    - path: "tests/smoke/test_trackpy_e2e.py"
      provides: "End-to-end verification"
      contains: "test_locate_link_batch"
  key_links:
    - from: "tests/smoke/test_trackpy_e2e.py"
      to: "trackpy.locate"
      via: "client.run('trackpy.locate', ...)"
---

<objective>
Verify full Trackpy integration with end-to-end smoke tests.

Purpose: Ensure that after all fixes (installer, runner, entrypoints), the actual science workflow functions correctly.
Output: A new smoke test file verifying locate, link, and batch operations.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-trackpy-integration/05-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Create Trackpy E2E Smoke Tests</name>
  <files>tests/smoke/test_trackpy_e2e.py</files>
  <action>
    Create `tests/smoke/test_trackpy_e2e.py` to test real workflow:
    
    1. Use `pytest.mark.requires_env("bioimage-mcp-trackpy")`.
    2. Implement `test_locate_link_batch` with **concrete** calls:
    
    ```python
    @pytest.mark.requires_env("bioimage-mcp-trackpy")
    async def test_locate_link_workflow(live_server):
        # 1. LOCATE
        # Create synthetic image (numpy array or existing fixture path)
        # Call 'trackpy.locate' via live_server.call_tool
        locate_res = await live_server.call_tool("run", {
            "fn_id": "trackpy.locate",
            "inputs": {"image": ..., "diameter": 11}
        })
        assert locate_res["ok"]
        features_ref = locate_res["outputs"]["features"] # artifact ref
        
        # 2. LINK
        # Call 'trackpy.link' passing the artifact ref from locate
        link_res = await live_server.call_tool("run", {
            "fn_id": "trackpy.link",
            "inputs": {"features": features_ref, "search_range": 5}
        })
        assert link_res["ok"]
        tracks_ref = link_res["outputs"]["tracks"]
        
        # 3. VERIFY
        # Inspect artifact content if possible, or ensure no errors
        assert tracks_ref
    ```
    
    3. Assert that results are returned and formatted correctly (no worker crashes).
  </action>
  <verify>
    `pytest tests/smoke/test_trackpy_e2e.py -v`
  </verify>
  <done>
    End-to-end workflow confirmed working in real environment.
  </done>
</task>

</tasks>

<verification>
Run `pytest tests/smoke/test_trackpy_e2e.py -v`
</verification>

<success_criteria>
- [ ] Trackpy locate/link workflow runs successfully in CI/smoke environment
</success_criteria>

<output>
After completion, create `.planning/phases/05-trackpy-integration/05-06-SUMMARY.md`
</output>
