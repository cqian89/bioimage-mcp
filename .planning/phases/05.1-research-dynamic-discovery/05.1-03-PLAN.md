---
phase: 05.1-research-dynamic-discovery
plan: 3
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - tools/trackpy/bioimage_mcp_trackpy/introspect.py
  - tools/trackpy/bioimage_mcp_trackpy/entrypoint.py
  - tests/unit/tools/test_trackpy_meta_protocol.py
autonomous: true

must_haves:
  truths:
    - "trackpy meta.describe returns tool_version + introspection_source and uses string error messages"
    - "trackpy meta.list/meta.describe shapes are test-verified without requiring trackpy to be installed in the dev env"
  artifacts:
    - path: tools/trackpy/bioimage_mcp_trackpy/introspect.py
      provides: "meta.describe result includes tool_version"
    - path: tests/unit/tools/test_trackpy_meta_protocol.py
      provides: "Hermetic unit tests for trackpy meta.* handler shapes"
  key_links:
    - from: tools/trackpy/bioimage_mcp_trackpy/entrypoint.py
      to: specs/024-meta-discovery-standardization/spec.md
      via: "meta.describe error/success contract"
      pattern: "tool_version"
---

<objective>
Bring the trackpy tool pack's meta.list/meta.describe responses into alignment with the canonical discovery protocol.

Purpose: Remove protocol drift and ensure trackpy sets the standard for upcoming Scipy/ndimage dynamic discovery work.
Output: trackpy meta.* conformance + hermetic unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@specs/024-meta-discovery-standardization/spec.md

@tools/trackpy/bioimage_mcp_trackpy/entrypoint.py
@tools/trackpy/bioimage_mcp_trackpy/introspect.py
</context>

<tasks>

<task type="auto">
  <name>Include tool_version in trackpy meta.describe results (numpydoc path)</name>
  <files>tools/trackpy/bioimage_mcp_trackpy/introspect.py</files>
  <action>
Update `tools/trackpy/bioimage_mcp_trackpy/introspect.py`:

- Add a small helper (e.g. `_get_trackpy_version()`) that returns the installed trackpy version using `importlib.metadata.version("trackpy")`, falling back to "unknown".
- Update `introspect_function()` to include:
  - `tool_version`: string
  - keep existing `params_schema` and `introspection_source` (currently "numpydoc")

Do not change the core shape (`params_schema` object schema) other than adding the new required field.
  </action>
  <verify>python -m compileall tools/trackpy/bioimage_mcp_trackpy/introspect.py</verify>
  <done>`introspect_function()` returns a dict containing `params_schema`, `tool_version`, and `introspection_source`.</done>
</task>

<task type="auto">
  <name>Normalize trackpy meta.describe error shape (string) and enrich meta.list metadata</name>
  <files>tools/trackpy/bioimage_mcp_trackpy/entrypoint.py</files>
  <action>
Update `tools/trackpy/bioimage_mcp_trackpy/entrypoint.py`:

- `handle_meta_describe()`:
  - If `target_fn` missing: return `{"ok": False, "error": "target_fn required"}` (error is a string).
  - If introspection raises: return `{"ok": False, "error": "Introspection failed: ..."}` (string).
  - On success: ensure `result` contains the fields required by the canonical spec.

- `handle_meta_list()`:
  - Keep function entries as-is, but add optional top-level `result.tool_version` and `result.introspection_source` to align with the canonical spec.

Do not change the worker wrapper logic in `_handle_request()`.
  </action>
  <verify>python -m compileall tools/trackpy/bioimage_mcp_trackpy/entrypoint.py</verify>
  <done>trackpy meta.describe uses string errors and returns tool_version; meta.list includes optional version/source metadata.</done>
</task>

<task type="auto">
  <name>Add hermetic unit tests for trackpy meta.list/meta.describe response shapes</name>
  <files>tests/unit/tools/test_trackpy_meta_protocol.py</files>
  <action>
Create `tests/unit/tools/test_trackpy_meta_protocol.py` that:

- Imports `bioimage_mcp_trackpy.entrypoint`.
- Monkeypatches `bioimage_mcp_trackpy.entrypoint.introspect_module` and `bioimage_mcp_trackpy.entrypoint.introspect_function` to avoid importing trackpy.
- Asserts:
  - `handle_meta_list({})` returns `{ok: true, result: {functions: [...]}}` and includes optional `tool_version`/`introspection_source` if added.
  - `handle_meta_describe({})` returns `{ok: false, error: <string>}`.
  - `handle_meta_describe({target_fn: ...})` returns `{ok: true, result: {params_schema, tool_version, introspection_source}}`.

Keep the test hermetic: no subprocess execution and no conda env dependency.
  </action>
  <verify>pytest tests/unit/tools/test_trackpy_meta_protocol.py -q</verify>
  <done>Unit tests pass and fail if trackpy meta.* handler shapes drift from the canonical spec.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/tools/test_trackpy_meta_protocol.py -q`
</verification>

<success_criteria>
- trackpy meta.describe responses include `tool_version` and always use string errors.
- A hermetic unit test locks in the protocol shape.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-research-dynamic-discovery/05.1-03-SUMMARY.md`
</output>
