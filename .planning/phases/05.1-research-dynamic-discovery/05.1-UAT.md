---
status: diagnosed
phase: 05.1-research-dynamic-discovery
source: 05.1-01-SUMMARY.md, 05.1-02-SUMMARY.md, 05.1-03-SUMMARY.md, 05.1-04-SUMMARY.md
started: 2026-01-25T11:20:00Z
updated: 2026-01-25T11:35:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

[testing complete]

## Tests

### 1. Trackpy List Metadata
expected: Calling `bioimage-mcp list trackpy` (or MCP equivalent `tools/list` filtered) returns tools with `tool_version` and `introspection_source` fields populated.
result: issue
reported: "User: MCP list correctly lacks extra fields (sufficient). CLI list incorrectly lacks tool_version/introspection_source (user says it should include them)."
severity: minor

### 2. Trackpy Describe Metadata
expected: Calling `bioimage-mcp describe trackpy.locate` (or equivalent) returns description including `tool_version`.
result: pass

### 3. Trackpy Error Shape
expected: Calling the MCP `describe` tool for `trackpy.nonexistent` returns a plain string error message, not a structured object or stack trace.
result: pass

### 4. Cellpose List Metadata
expected: Calling the MCP `list` tool for 'cellpose' returns tools with `module` and `io_pattern` fields populated (in addition to standard fields).
result: issue
reported: "User: list output missing introspection_source, module, and io_pattern. Standardization required."
severity: major

### 5. Cellpose Error Shape
expected: Calling the MCP `describe` tool for `cellpose.nonexistent` returns a plain string error message (or structured error consistent with Trackpy).
result: pass

### 6. Protocol Robustness
expected: Discovery system successfully loads and validates tools from both `trackpy` and `cellpose` without warnings about invalid schemas in the logs/output.
result: pass

## Summary

total: 6
passed: 4
issues: 2
pending: 0
skipped: 0

## Gaps

<!-- YAML format for plan-phase --gaps consumption -->
- truth: "CLI `bioimage-mcp list` should include tool_version and introspection_source."
  status: failed
  reason: "User reported: CLI command should include such information, but currently lacks it (or behaves differently)."
  severity: minor
  test: 1
  root_cause: "`src/bioimage_mcp/bootstrap/list.py` only builds minimal `tool_details`. It never reads `tool_version` or `introspection_source` from the manifest/functions."
  artifacts:
    - path: "src/bioimage_mcp/bootstrap/list.py"
      issue: "Omits tool_version and introspection_source fields"
  missing:
    - "Extraction of tool_version from manifest"
    - "Aggregation of introspection_source from functions"
    - "Column formatting for new fields in table/JSON output"
  debug_session: "ses_40acbe6b0ffe34K2eWhAP5tItx"

- truth: "Cellpose list output should include module and io_pattern (and potentially introspection_source for standardization)."
  status: failed
  reason: "User reported: Missing introspection_source, module, and io_pattern."
  severity: major
  test: 4
  root_cause: "Core registry/storage drops `module` and `io_pattern` after discovery. SQLite schema and ToolIndex payload lack these fields."
  artifacts:
    - path: "src/bioimage_mcp/storage/sqlite.py"
      issue: "Missing module/io_pattern columns"
    - path: "src/bioimage_mcp/registry/index.py"
      issue: "Payload omits module/io_pattern"
  missing:
    - "Persistence columns for module/io_pattern in SQLite"
    - "Return paths in ToolIndex/list_functions payload"
  debug_session: "ses_40acbe697ffe8eIzi7Z96Ype5v"
