---
phase: 05.1-research-dynamic-discovery
plan: 4
type: execute
wave: 2
depends_on: ["05.1-01"]
files_modified:
  - tools/cellpose/bioimage_mcp_cellpose/entrypoint.py
  - tests/unit/tools/test_cellpose_meta_protocol.py
autonomous: true

must_haves:
  truths:
    - "cellpose meta.list provides minimally-standard function entries (including module + io_pattern)"
    - "cellpose meta.describe always returns the canonical fields and uses string error messages"
  artifacts:
    - path: tools/cellpose/bioimage_mcp_cellpose/entrypoint.py
      provides: "Protocol-conformant cellpose meta.list/meta.describe handlers"
    - path: tests/unit/tools/test_cellpose_meta_protocol.py
      provides: "Hermetic unit tests for cellpose meta.* handler shapes"
  key_links:
    - from: tools/cellpose/bioimage_mcp_cellpose/entrypoint.py
      to: specs/024-meta-discovery-standardization/spec.md
      via: "meta.list/meta.describe contract"
      pattern: "meta.describe"
---

<objective>
Bring the cellpose tool pack's meta.list/meta.describe responses into alignment with the canonical discovery protocol.

Purpose: Prevent drift between tool packs and ensure core can rely on consistent meta.* payloads.
Output: cellpose meta.* conformance + hermetic unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@specs/024-meta-discovery-standardization/spec.md

@tools/cellpose/bioimage_mcp_cellpose/entrypoint.py
@tools/cellpose/manifest.yaml
</context>

<tasks>

<task type="auto">
  <name>Normalize cellpose meta.describe error handling and enrich meta.list entries</name>
  <files>tools/cellpose/bioimage_mcp_cellpose/entrypoint.py</files>
  <action>
Update `tools/cellpose/bioimage_mcp_cellpose/entrypoint.py`:

- `handle_meta_describe()`:
  - Ensure unknown/missing `target_fn` returns `{ok: false, error: <string>}`.
  - Keep success response fields: `params_schema`, `tool_version`, `introspection_source`.

- `handle_meta_list()`:
  - Update each function entry to include the canonical minimal keys (`fn_id`, `name`, `summary`) and add:
    - `module` (e.g. `cellpose.models`, `cellpose.train`, `cellpose.cache`)
    - `io_pattern`:
      - constructors (`CellposeModel`, `DenoiseModel`) -> `pure_constructor`
      - training (`cellpose.train.train_seg`) -> `training`
      - everything else -> `generic`
  - Optionally add `result.tool_version` (use `_get_cellpose_version()`) and `result.introspection_source` (use "manual") at the meta.list top level.

Do not change non-meta execution behavior.
  </action>
  <verify>python -m compileall tools/cellpose/bioimage_mcp_cellpose/entrypoint.py</verify>
  <done>cellpose meta.describe uses string errors; meta.list entries include module + io_pattern using valid `IOPattern` values.</done>
</task>

<task type="auto">
  <name>Add hermetic unit tests for cellpose meta.list/meta.describe response shapes</name>
  <files>tests/unit/tools/test_cellpose_meta_protocol.py</files>
  <action>
Create `tests/unit/tools/test_cellpose_meta_protocol.py` that:

- Imports `bioimage_mcp_cellpose.entrypoint` without requiring cellpose to be installed.
- Monkeypatches:
  - `_introspect_cellpose_fn` to return a deterministic schema
  - `_get_cellpose_version` to return a deterministic version string

Asserts:
- `handle_meta_describe({"target_fn": "cellpose.models.CellposeModel.eval"})` returns `{ok: true, result: {params_schema, tool_version, introspection_source}}`.
- `handle_meta_describe({})` returns `{ok: false, error: <string>}`.
- `handle_meta_list(...)` returns `{ok: true, result: {functions: [...]}}` and entries include `module` and `io_pattern`.

Keep test hermetic: no subprocess, no conda.
  </action>
  <verify>pytest tests/unit/tools/test_cellpose_meta_protocol.py -q</verify>
  <done>Unit tests pass and fail if cellpose meta.* shapes drift from the canonical spec.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/tools/test_cellpose_meta_protocol.py -q`
</verification>

<success_criteria>
- cellpose meta.list/meta.describe payloads align with the canonical protocol (required fields + string errors).
- A hermetic unit test locks in the protocol shape.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-research-dynamic-discovery/05.1-04-SUMMARY.md`
</output>
