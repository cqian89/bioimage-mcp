---
phase: 05.1-research-dynamic-discovery
plan: 5
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/bootstrap/list.py
  - tests/unit/bootstrap/test_list_output.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "CLI `bioimage-mcp list` includes tool_version and introspection_source per tool (table + JSON)"
  artifacts:
    - path: src/bioimage_mcp/bootstrap/list.py
      provides: "CLI tool listing with tool_version + aggregated introspection_source"
    - path: tests/unit/bootstrap/test_list_output.py
      provides: "Hermetic tests locking CLI list output fields"
  key_links:
    - from: src/bioimage_mcp/bootstrap/list.py
      to: src/bioimage_mcp/registry/loader.py
      via: "load_manifests() -> ToolManifest.tool_version/functions[].introspection_source"
      pattern: "load_manifests\("
---

<objective>
Close UAT gap where the CLI tool listing omits discovery metadata.

Purpose: Keep CLI visibility aligned with the standardized discovery protocol (version + introspection provenance).
Output: `bioimage-mcp list` prints/exports `tool_version` and `introspection_source`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-research-dynamic-discovery/05.1-UAT.md
@specs/024-meta-discovery-standardization/spec.md

@src/bioimage_mcp/bootstrap/list.py
@src/bioimage_mcp/registry/loader.py
@src/bioimage_mcp/registry/manifest_schema.py
@tests/unit/bootstrap/test_list_output.py
</context>

<tasks>

<task type="auto">
  <name>Add tool_version + aggregated introspection_source to CLI list output</name>
  <files>src/bioimage_mcp/bootstrap/list.py</files>
  <action>
Update `src/bioimage_mcp/bootstrap/list.py`:

- When building each `tool_details` entry, include:
  - `tool_version`: `m.tool_version`.
  - `introspection_source`: aggregated from `m.functions[*].introspection_source`.
    - Compute `sources = sorted({s for s in (fn.introspection_source for fn in m.functions) if s})`.
    - For JSON output, include `introspection_source` as `sources` (list of strings).
    - For table output, format as `", ".join(sources)`; if empty, render `""`.

- Update the table header and row formatting to include these columns:
  - `Version` (tool_version)
  - `Introspection` (aggregated introspection_source)

Keep existing semantics for env status detection (installed/partial).
  </action>
  <verify>python -m compileall src/bioimage_mcp/bootstrap/list.py</verify>
  <done>`bioimage-mcp list --json` includes `tool_version` and `introspection_source` fields for each tool entry.</done>
</task>

<task type="auto">
  <name>Update unit tests to assert CLI list includes tool_version + introspection_source</name>
  <files>tests/unit/bootstrap/test_list_output.py</files>
  <action>
Update `tests/unit/bootstrap/test_list_output.py` to be hermetic against the current manifest-based implementation:

- Replace the DB fixture with a temporary tool manifest root:
  - Write a minimal manifest YAML to `tmp_path` that includes:
    - `tool_version: "1.2.3"`
    - at least one function entry containing `introspection_source: "python_api"`.
  - Monkeypatch `bioimage_mcp.bootstrap.list.load_config()` to return an object with:
    - `tool_manifest_roots = [tmp_path]`

- Monkeypatch `detect_env_manager()` and `_get_installed_envs()` so the env is treated as installed.

Assertions:
- Table output contains the new column headers and prints `1.2.3` and `python_api`.
- JSON output parses and includes:
  - `tools[0].tool_version == "1.2.3"`
  - `tools[0].introspection_source == ["python_api"]` (or superset if you include multiple functions).

Remove/adjust any assertions that depend on the old DB-backed implementation (e.g., unavailable status).
  </action>
  <verify>pytest tests/unit/bootstrap/test_list_output.py -q</verify>
  <done>Tests fail if CLI list ever omits `tool_version` / `introspection_source` again.</done>
</task>

</tasks>

<verification>
- `pytest tests/unit/bootstrap/test_list_output.py -q`
</verification>

<success_criteria>
- UAT truth #1 passes: CLI `bioimage-mcp list` includes `tool_version` and `introspection_source`.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-research-dynamic-discovery/05.1-05-SUMMARY.md`
</output>
