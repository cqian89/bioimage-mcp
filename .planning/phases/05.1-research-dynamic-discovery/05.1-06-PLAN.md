---
phase: 05.1-research-dynamic-discovery
plan: 6
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bioimage_mcp/storage/sqlite.py
  - src/bioimage_mcp/registry/index.py
  - src/bioimage_mcp/registry/loader.py
  - src/bioimage_mcp/registry/manifest_schema.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "MCP `list` returns function nodes that include module + io_pattern (and introspection_source when present)"
    - "Registry storage persists module + io_pattern so list output does not regress after restart"
  artifacts:
    - path: src/bioimage_mcp/storage/sqlite.py
      provides: "SQLite schema supports module + io_pattern persistence"
    - path: src/bioimage_mcp/registry/index.py
      provides: "Upsert/list plumbing for module + io_pattern + introspection_source"
    - path: src/bioimage_mcp/registry/loader.py
      provides: "Dynamic discovery converts FunctionMetadata.module/io_pattern into manifest Function fields"
    - path: src/bioimage_mcp/registry/manifest_schema.py
      provides: "Function model carries module/io_pattern metadata"
  key_links:
    - from: src/bioimage_mcp/registry/loader.py
      to: src/bioimage_mcp/registry/manifest_schema.py
      via: "Function(...) construction from FunctionMetadata"
      pattern: "Function\("
    - from: src/bioimage_mcp/registry/index.py
      to: src/bioimage_mcp/api/discovery.py
      via: "list_functions() feeds ToolIndex payload for MCP list"
      pattern: "list_functions\(\)"
---

<objective>
Close UAT gap where MCP list output drops `module` and `io_pattern` metadata (especially for cellpose).

Purpose: Preserve standardized meta.list enrichment end-to-end (tool env -> loader -> registry -> MCP list payload).
Output: Function nodes returned by `tools/list` include `module`, `io_pattern`, and `introspection_source` when available.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.1-research-dynamic-discovery/05.1-UAT.md
@specs/024-meta-discovery-standardization/spec.md

@src/bioimage_mcp/storage/sqlite.py
@src/bioimage_mcp/registry/index.py
@src/bioimage_mcp/api/discovery.py
@src/bioimage_mcp/registry/loader.py
@src/bioimage_mcp/registry/manifest_schema.py
@src/bioimage_mcp/bootstrap/serve.py
</context>

<tasks>

<task type="auto">
  <name>Persist module + io_pattern in SQLite and registry index plumbing</name>
  <files>
src/bioimage_mcp/storage/sqlite.py
src/bioimage_mcp/registry/index.py
  </files>
  <action>
Update storage + registry so function metadata can be stored and retrieved:

- `src/bioimage_mcp/storage/sqlite.py`:
  - Extend the `functions` table schema to include:
    - `module TEXT`
    - `io_pattern TEXT`
  - Add a lightweight schema migration in `init_schema()` so existing DBs are upgraded:
    - Query `PRAGMA table_info(functions)`.
    - If `module` missing, run `ALTER TABLE functions ADD COLUMN module TEXT`.
    - If `io_pattern` missing, run `ALTER TABLE functions ADD COLUMN io_pattern TEXT`.
  - Keep the migration idempotent (safe to run every startup).

- `src/bioimage_mcp/registry/index.py`:
  - Extend `RegistryIndex.upsert_function(...)` to accept optional `module: str | None` and `io_pattern: str | None`.
  - Store them in the `INSERT INTO functions(...)` statement.
  - Update `RegistryIndex.list_functions()` to SELECT and return `module`, `io_pattern`, and `introspection_source`.

Keep backward compatibility: callers that don't pass `module/io_pattern` should still work and store NULLs.
  </action>
  <verify>pytest tests/contract/test_list.py -q</verify>
  <done>DB schema contains module/io_pattern columns and list_functions returns them without breaking existing list contract tests.</done>
</task>

<task type="auto">
  <name>Plumb module + io_pattern from discovery into MCP list payload (and lock with tests)</name>
  <files>
src/bioimage_mcp/registry/loader.py
src/bioimage_mcp/registry/manifest_schema.py
src/bioimage_mcp/bootstrap/serve.py
src/bioimage_mcp/registry/index.py
  </files>
  <action>
Wire discovery metadata end-to-end:

- `src/bioimage_mcp/registry/manifest_schema.py`:
  - Add optional fields to `Function`:
    - `module: str | None = None`
    - `io_pattern: str | None = None`
  - Keep them optional so existing manifests remain valid.

- `src/bioimage_mcp/registry/loader.py`:
  - When converting `FunctionMetadata` -> `Function`, set:
    - `module=meta.module`
    - `io_pattern=str(meta.io_pattern.value)`
  - When applying `FunctionOverlay.io_pattern`, also keep `Function.io_pattern` in sync with the overlay value.

- `src/bioimage_mcp/bootstrap/serve.py`:
  - Pass `module=fn.module` and `io_pattern=fn.io_pattern` into `service.upsert_function(...)`.

- `src/bioimage_mcp/registry/index.py` + `src/bioimage_mcp/registry/index.py:ToolIndex`:
  - Extend `_HierarchyNode` to store `module`, `io_pattern`, and `introspection_source` for function nodes.
  - In `ToolIndex.build_hierarchy()` / `_insert_path(...)`, pass these values from the `functions` list rows.
  - In `_to_payload(...)` for `node.type == "function"`, include:
    - `module` (if present)
    - `io_pattern` (if present)
    - `introspection_source` (if present)

Add a new hermetic unit test (create if it doesn't exist):
- `tests/unit/api/test_list_function_metadata_fields.py`
  - Create an in-memory DB, `init_schema(conn)`, upsert one tool + one function with:
    - `module="cellpose.models"`
    - `io_pattern="pure_constructor"`
    - `introspection_source="python_api"`
  - Call `DiscoveryService(conn).list_tools(path="cellpose.models")`.
  - Assert the returned function node includes all three fields.

Do not require real tool envs or subprocess calls in this test.
  </action>
  <verify>pytest tests/unit/api/test_list_function_metadata_fields.py -q</verify>
  <done>UAT truth #4 passes: MCP list output includes `module` + `io_pattern` (and `introspection_source`) for function nodes.</done>
</task>

</tasks>

<verification>
- `pytest tests/contract/test_list.py -q`
- `pytest tests/unit/api/test_list_function_metadata_fields.py -q`
</verification>

<success_criteria>
- Function nodes returned by MCP `list` include `module` and `io_pattern` fields when present in the registry.
- Those fields persist in SQLite and remain present after server restart/re-index.
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-research-dynamic-discovery/05.1-06-SUMMARY.md`
</output>
